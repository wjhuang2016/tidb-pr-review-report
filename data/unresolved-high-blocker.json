{
  "generated_at": "2026-03-19T14:32:36.917813+08:00",
  "source_results_dir": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results",
  "worthy_deep_review_total": 334,
  "unresolved_high_blocker_total": 69,
  "blocker_total": 10,
  "high_total": 59,
  "all_findings_within_worthy_scope": true,
  "entries": [
    {
      "pr": 40634,
      "subject": "ddl: enable modify column on partitioned table (#40634)",
      "commit": "939455cf22303ff77ebd55f6f2f472ea5d659359",
      "modules": [
        "pkg/ddl"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "`MODIFY COLUMN` on partitioned tables is enabled, but partitioning-column safety checks don’t account for `PARTITION BY KEY`: lossy conversions on KEY-partitioning columns (e.g., shrinking string length, reducing time/datetime FSP, or other conversions that can change value under non-strict SQL mode) can change the hashed partition key while the DDL reorg rewrites rows in-place without moving them across physical partitions. This can leave rows stored in the “wrong” partition, causing incorrect results under partition pruning/point-get.",
      "impact": "A successful `ALTER TABLE ... MODIFY COLUMN` on a KEY-partitioning column can change stored values (truncation/rounding/overflow-as-warning in non-strict SQL mode) while rows remain physically in their old partitions. Since KEY partition pruning/point-get routes by hashing the new values, affected rows can become unreachable or silently missed by queries that prune to the “correct” partition, yielding wrong results and breaking partition invariants.",
      "fix": "For `PARTITION BY KEY` partitioning columns, disallow any modify that can change the hashed key unless full row re-location/repartition is implemented.\n- Minimal safe rule: if the modified column is a KEY-partitioning column, only allow changes that are guaranteed lossless for all existing rows (e.g. require `noReorgDataStrict(...) == true`, and reject flen/decimal decreases and other potentially lossy conversions).\n- Alternatively, add a mandatory precheck scan (regardless of `sql_mode`) to ensure **no truncation/overflow/rounding** can occur (similar to `checkModifyColumnData` but extended to time/datetime fsp, etc.); reject if any row would change value.",
      "verify": "Add a targeted regression test:\n1) Create `PARTITION BY KEY` table on `VARCHAR` (or `DATETIME(6)`), insert rows whose values would be truncated/rounded by a proposed `MODIFY COLUMN`.\n2) Set `sql_mode=''` (to allow warnings) and run the `ALTER TABLE ... MODIFY COLUMN`.\n3) Assert either: (a) DDL is rejected with an `ErrUnsupportedModifyColumn`-style error, or (b) if allowed, point queries/pruning still find rows (which would require row re-location across partitions).\nExample scenarios: shrink `VARCHAR(10)->VARCHAR(5)` with long values; reduce `DATETIME(6)->DATETIME(0)` with microsecond values.",
      "evidence": "- Partitioning-column gate allows lossy changes for `PARTITION BY KEY` because the only “no length decrease” guard is under `len(pi.Columns) == 0` (non-COLUMNS partitioning). For KEY partitioning `pi.Columns` is non-empty, so `MODIFY COLUMN` that can truncate/round values is not blocked: `pkg/ddl/modify_column.go:1539`.\n- Reorg path rewrites rows in-place within each existing physical partition (no row move): `updateColumnWorker.BackfillData` does `txn.Set(rowRecord.key, rowRecord.vals)` where `rowRecord.key` is the existing record key from the current physical table: `pkg/ddl/column.go:846`, `pkg/ddl/column.go:886`. Partition iteration for `ActionModifyColumn` uses `typeUpdateColumnWorker` on partitions: `pkg/ddl/column.go:541`, `pkg/ddl/column.go:560`.\n- KEY partition routing hashes the (collation-aware) value: `LocateKeyPartition` CRC32s `Datum.ToHashKey()` for partition columns: `pkg/table/tables/partition.go:360`. `ToHashKey()` uses `collate.GetCollator(...).Key(d.GetString()/ToString())`, so any value change changes the hash input: `pkg/types/datum.go:2157`.\n- This PR explicitly enables value-changing modify-column on partitioning columns in non-strict mode (e.g. truncation warnings) for RANGE COLUMNS: `pkg/ddl/tests/partition/db_partition_test.go:2943` (shows DDL can succeed with truncation warnings), demonstrating the system does allow lossy conversions under SQL-mode control.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master` (f0813a3696): `checkPartitionColumnModifiable` logic is unchanged there.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-40634.json",
      "worthy_scope": true,
      "summary": "Found a blocker: KEY-partitioning columns can undergo value-changing `MODIFY COLUMN` while rows are rewritten in-place (no repartition), risking permanently mispartitioned rows and wrong results under key-partition pruning. Not fixed in later history up to `upstream/master`."
    },
    {
      "pr": 53277,
      "subject": "ddl: Reorganize partition supporting global index (#53277)",
      "commit": "be86a2594eefa8ede5bd247d235ab7c43e68dc2a",
      "modules": [
        "pkg/ddl",
        "pkg/table/tables"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "`findNextNonTouchedPartitionID` has a loop whose DroppingDefinitions membership check is evaluated only once (the `notFoundErr` condition is never recomputed). If the immediate next partition is a dropped partition, the loop will keep advancing to the end and return 0, skipping remaining non-touched partitions. This can leave rebuilt global indexes incomplete after `REORGANIZE PARTITION` when dropped partitions are in the middle of `pi.Definitions`.",
      "impact": "For `REORGANIZE PARTITION` with global unique indexes, if the partition immediately following the current scan position is in `DroppingDefinitions`, the function can incorrectly skip all remaining partitions and return 0. This can terminate the “non-touched partitions” global-index backfill early, leaving the new global index missing entries for later non-touched partitions. Consequences include incorrect query results when the global index is chosen/forced and potentially broken uniqueness enforcement for keys that exist only in skipped partitions.",
      "fix": "Rewrite `findNextNonTouchedPartitionID` to re-check DroppingDefinitions membership for each updated `pid`. Example shape:\n- `pid := next(definitions, curr)`\n- while `pid` is in `DroppingDefinitions`: `pid = next(definitions, pid)`\nReturn the first `pid` not in DroppingDefinitions (or 0 at end). Avoid the one-shot `notFoundErr` condition; use a `for { ... }` with an inner membership check or add a `post` clause that recomputes the membership check each iteration.",
      "verify": "Repro-style verification:\n1) Create a range-partitioned table with ≥3 partitions (so there are non-touched partitions after the reorganized segment).\n2) Enable global index; add a global unique index.\n3) Insert rows into partitions both before and after the partition(s) to be reorganized.\n4) `ALTER TABLE ... REORGANIZE PARTITION` on a middle partition segment.\n5) After DDL, run `ADMIN CHECK TABLE` and a forced index read like `SELECT /*+ USE_INDEX(t, idx) */ ...` to ensure rows from later non-touched partitions are reachable via the rebuilt global index.",
      "evidence": "`pkg/ddl/index.go:2406` defines `findNextNonTouchedPartitionID`. The loop `for _, notFoundErr := findNextPartitionID(pid, pi.DroppingDefinitions); notFoundErr == nil; { ... }` never updates `notFoundErr` (lines `2414-2420`), so it only checks DroppingDefinitions membership once and then keeps advancing `pid` in `pi.Definitions` until `pid==0`. Callers: `pkg/ddl/index.go:2291` (`getNextPartitionInfo` case 4 uses it for “non-touched partitions”), and `pkg/ddl/partition.go:3633` (`reorgPartitionDataAndIndex` uses it to choose the first non-touched partition for global-index backfill).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed as of `upstream/master` in this repo: `upstream/master:pkg/ddl/index.go` still contains the same one-shot `notFoundErr` loop in `findNextNonTouchedPartitionID`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-53277.json",
      "worthy_scope": true,
      "summary": "Blocker: global-index backfill iteration for “non-touched partitions” is broken by a one-shot DroppingDefinitions membership check in `findNextNonTouchedPartitionID`, which can stop the scan early and miss partitions; issue still present on `upstream/master`."
    },
    {
      "pr": 55129,
      "subject": "domain,infoschema: avoid v1/v2 switch when loading snapshot infoschema (#55129)",
      "commit": "52b4c8a27b46288ce4fcfc8e5bc8552a6c134233",
      "modules": [
        "pkg/domain",
        "pkg/infoschema",
        "pkg/meta",
        "pkg/sessionctx/variable"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "Snapshot infoschema full-load can build infoschema v1 while `tidb_schema_cache_size>0`, but schema/table fetching is still optimized for v2 and only loads “must-load” table infos; v1 builder then omits most tables, yielding an incomplete snapshot infoschema.",
      "impact": "If `tidb_schema_cache_size` is enabled (>0) while the current/latest infoschema is still v1 (possible during the documented delayed-effect window), a snapshot infoschema cache miss can full-load using v1 but only “must-load” table infos, omitting most tables. Snapshot/stale-read, BR, and DDL code paths relying on `GetSnapshotInfoSchema` can see missing tables/metadata (e.g., unexpected `ErrTableNotExists`) or incorrect behavior.",
      "fix": "When `useV2` is false (building infoschema v1), force the full-table meta path (`m.ListTables`) even if `SchemaCacheSize>0` (e.g., plumb `useV2` into `fetchAllSchemasWithTables`/`fetchSchemasWithTables` and gate `GetAllNameToIDAndTheMustLoadedTableInfo` on it; or pass an effective `schemaCacheSize=0` when `useV2=false`).",
      "verify": "Manual scenario: start with `tidb_schema_cache_size=0` (v1), ensure snapshot TS older than infoschema cache coverage, `SET GLOBAL tidb_schema_cache_size='512MB'`, then immediately run a stale-read (`AS OF TIMESTAMP` / snapshot) query against a non-must-load table; expect table resolution to succeed (currently can fail on cache miss).",
      "evidence": "Snapshot loads can choose v1 regardless of `tidb_schema_cache_size`: `pkg/domain/domain.go:321`, `pkg/domain/domain.go:327`, `pkg/domain/domain.go:328`, `pkg/domain/domain.go:539`. Full-load table fetch still switches to must-load-only when `SchemaCacheSize>0`: `pkg/domain/domain.go:490`, `pkg/meta/meta.go:1081`. v1 builder only materializes `di.Deprecated.Tables` and ignores `TableName2ID` unless v2: `pkg/infoschema/builder.go:934`, `pkg/infoschema/builder.go:953`. Runtime toggle window is explicitly expected: `pkg/sessionctx/variable/sysvar.go:3152`.",
      "later_fixed": false,
      "later_fix_evidence": "Checked upstream/master at `f0813a369666ac6314e76f5e573e982fe50c8a09`: snapshot keep-old v1/v2 logic and schemaCacheSize-driven must-load table fetch still exist (`upstream/master:pkg/infoschema/issyncer/loader.go:181`, `upstream/master:pkg/infoschema/issyncer/loader.go:493`, `upstream/master:pkg/infoschema/issyncer/loader.go:606`).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-55129.json",
      "worthy_scope": true,
      "summary": "Found 1 Blocker in snapshot infoschema loading: a cache-miss full load can build v1 while `tidb_schema_cache_size>0` but still uses v2’s must-load-only meta fetch, producing an incomplete snapshot schema; not fixed in upstream/master."
    },
    {
      "pr": 55847,
      "subject": "infoschema, meta: fix wrong auto id after `rename table` (#55847)",
      "commit": "59040d08753e1d396417206a5234984ed38d428b",
      "modules": [
        "pkg/infoschema",
        "pkg/meta",
        "pkg/meta/autoid"
      ],
      "severity": "Blocker",
      "confidence": "Medium",
      "what": "`Allocator.Transfer` + `AutoIDAccessor.CopyTo` migrates and switches autoid ownership to the new schema during infoschema diff apply, but cross-schema rename explicitly allows concurrent DML; in a multi-node mixed-schema window this can split allocations across old/new meta keys and can non-monotonically overwrite the target autoid, risking duplicate auto-increment IDs or wrong base.",
      "impact": "In a multi-TiDB deployment under cross-schema rename with concurrent DML (especially small `auto_id_cache`/high insert rate or delayed schema reload on some nodes), different nodes may allocate from different autoid slots (`DB:<old>` vs `DB:<new>`), and later `CopyTo` can overwrite a higher target with a lower source. Worst case: overlapping auto-increment values across nodes, primary key conflicts/insert failures, or incorrect auto-increment base after reload.",
      "fix": "Avoid switching the allocator’s underlying meta key during the period where old/new schema versions can both execute DML:\n- Keep allocations using the original stable key (e.g. honor `AutoIDSchemaID`) for the overlap window, and only migrate/switch to the new schema key after the rename is fully synced and no pre-rename transactions can still allocate IDs.\n- If migration must happen opportunistically, make `CopyTo` monotonic (`new = max(existingNew, currOld)` within the same txn) and guard the allocator state mutation with `alloc.mu` (or equivalent) to avoid races; also ensure no nodes can allocate from both keys concurrently (requires a strict MDL/DDL gating point, not just schema reload).",
      "verify": "Add an integration-style test that runs two TiDB domains (or two mock servers) against the same store:\n1) Create `db1.t` with `auto_id_cache=1` (or very small).\n2) Start a long transaction on node A that keeps inserting into `db1.t`.\n3) On node B, execute `RENAME TABLE db1.t TO db2.t` and immediately run inserts into `db2.t`.\n4) Artificially delay schema reload/infoschema apply on one node (failpoint or lease tick) to widen the mixed-state window.\n5) Assert no duplicate auto IDs/PK conflicts and that auto-id base survives `DROP DATABASE db1` + forced full reload.",
      "evidence": "This commit makes auto-ID storage/ownership move during infoschema diff apply:\n- `pkg/infoschema/builder.go:685` calls `alloc.Transfer(dbInfo.ID, tableID)` for reused allocators while applying table updates (rename table keeps old allocators via reuse path).\n- `pkg/meta/autoid/autoid.go:321` `(*allocator).Transfer` runs `CopyTo(newDBID,newTableID)` in a new txn then mutates `alloc.dbID/alloc.tbID` (no allocator lock around the ID fields).\n- `pkg/meta/meta_autoid.go:85` `(*autoIDAccessor).CopyTo` does `Get()` then unconditional `HSet` to the target DB key (no `max(existing, curr)`), so it can overwrite a newer target value with an older source value.\n- DDL still sets `tblInfo.AutoIDSchemaID` explicitly to allow concurrent use during cross-schema rename (`pkg/ddl/table.go:847`), and there is an existing concurrency test showing old-txn DML on the old name overlapping with new-name DML while rename is still running (`pkg/ddl/db_rename_test.go:348`), i.e. overlap is supported.\n- But allocator creation no longer honors `AutoIDSchemaID` (removed `TableInfo.GetAutoIDSchemaID` in `pkg/meta/model/table.go`, and `pkg/meta/autoid/autoid.go:673` now uses the passed `dbID` directly), meaning different nodes (or the same node before/after reload) can end up allocating against different `(dbID,tableID)` autoid slots during the overlap window.\nSecond confirmation: `pkg/table/tables/tables.go:248` stores `autoid.Allocators` by value and `Allocators(nil)` returns the same allocator pointers (`pkg/table/tables/tables.go:1585`), so `Transfer` mutates allocator state shared across schema versions on a node; across nodes, `Transfer` timing is not synchronized.\nLater history check: on `upstream/master`, `applyCreateTable` still calls `Transfer` and `Transfer` still updates `alloc.dbID/alloc.tbID`; `CopyTo` only added a `curr==0` skip but still overwrites non-zero values without a monotonic check, so this concurrency hazard is not clearly eliminated.",
      "later_fixed": false,
      "later_fix_evidence": "`upstream/master` still has `pkg/infoschema/builder.go` calling `alloc.Transfer(...)` during diff apply and `pkg/meta/autoid/autoid.go` mutating `alloc.dbID/alloc.tbID` after `CopyTo`. `pkg/meta/meta_autoid.go` `CopyTo` only skips `curr==0` but does not enforce `max(existing,target)` for non-zero values, so non-monotonic overwrite remains possible.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-55847.json",
      "worthy_scope": true,
      "summary": "Reviewed 59040d0 vs first parent for infoschema/meta autoid rename handling. Found a Blocker-risk concurrency/consistency issue: the fix migrates+switches autoid meta during infoschema diff apply via unconditional copy, which can split allocations across DB keys and overwrite target values in multi-node mixed-schema windows; not evidently addressed in later upstream/master."
    },
    {
      "pr": 56786,
      "subject": "ddl: Corrected index management during REORGANIZE PARTITION (#56786)",
      "commit": "938ed70fec61e260a5d047e11458ca16bfbaa648",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/table/tables"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "`PartitionInfo.DDLChangedIndex` is not cleared on successful `ActionReorganizePartition` completion. Because it is persisted in table meta and later reused by both `tables.newPartitionedTable()` (to decide which indexes exist on dropping/adding partitions) and rollback conversion (to decide which indexes to drop), stale entries can break subsequent REORGANIZE PARTITION executions and can even cause rollback to drop an index that pre-existed the current DDL.",
      "impact": "Metadata/state-machine corruption risk across *subsequent* REORGANIZE PARTITION runs:\n- After a reorg that duplicates indexes and leaves `DDLChangedIndex` populated, the next reorg may mis-build per-partition index sets (missing indexes on partitions in `DroppingDefinitions`/`AddingDefinitions`), affecting DML index maintenance and potentially uniqueness enforcement.\n- Worse, if a later reorg is cancelled/rolls back while `DDLChangedIndex` still contains entries for an existing unique index that is *not* duplicated in the new run (e.g., a prior global→local transition), `convertReorgPartitionJob2RollbackJob` can wrongly `removeIndexInfo` for that index, effectively dropping a pre-existing index during rollback.",
      "fix": "Treat `PartitionInfo.DDLChangedIndex` as per-job ephemeral state:\n- Reset it at the start of `onReorganizePartition` (StateNone) before any possible rollback can occur (e.g., `tblInfo.Partition.DDLChangedIndex = nil`).\n- Clear it on the successful `ActionReorganizePartition` completion path when `DDLState/DDLAction` are reset (either call a targeted clearer or set `DDLChangedIndex=nil`).\nThis prevents stale IDs from affecting later reorgs/rollbacks.",
      "verify": "Add a regression test exercising staleness across two DDLs:\n1) Create a partitioned table with a unique index that can be toggled global/local.\n2) Run `REORGANIZE PARTITION ... UPDATE INDEXES` to force an index duplication and complete successfully.\n3) Run a second `REORGANIZE PARTITION` and inject a failpoint to force rollback in an early state.\n4) Assert the original index set remains intact after rollback and `admin check table` passes; additionally assert table meta has `Partition.DDLChangedIndex` cleared post-success.",
      "evidence": "- `pkg/ddl/partition.go:3272-3289` initializes/populates `tblInfo.Partition.DDLChangedIndex` but only if the map is nil (no reset path).\n- `pkg/ddl/partition.go:3542-3546` clears `DroppingDefinitions`/`AddingDefinitions` and resets `DDLState`/`DDLAction`, but does not clear `DDLChangedIndex` on successful `ActionReorganizePartition` completion.\n- `pkg/table/tables/partition.go:131-174` uses `pi.DDLChangedIndex` to split indexes into `DroppingDefinitionIndices` vs `AddingDefinitionIndices`; stale entries change which indexes exist on partitions in `DroppingDefinitions`/`AddingDefinitions`.\n- `pkg/ddl/rollingback.go:372-398` drops any unique index whose ID is marked `isNew==true` (unless `pi.DDLState==StateDeleteReorganization`). With stale `DDLChangedIndex`, a pre-existing index can be treated as “new” and removed during rollback.\n- `pkg/meta/model/table.go:879-887` has the intended cleanup (`pi.DDLChangedIndex=nil`) in `ClearReorgIntermediateInfo`, but the success path for `ActionReorganizePartition` does not call it (only `pkg/ddl/partition.go:3600` for non-reorg types, and `pkg/ddl/partition.go:2198` for rollback-like paths).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master` (`f0813a369666...`): upstream still resets `DDLState/DDLAction` without clearing `DDLChangedIndex`, and rollback conversion logic still drops indexes solely based on `pi.DDLChangedIndex` membership.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56786.json",
      "worthy_scope": true,
      "summary": "Diff introduces `PartitionInfo.DDLChangedIndex` to track old/new index copies during REORGANIZE PARTITION, rewires index state transitions (old -> `StateWriteOnly`, new -> `StatePublic`) and rollback handling to use that map, and updates `tables.newPartitionedTable()` to build per-partition index lists for Adding/DroppingDefinitions. Main remaining risk is persisting `DDLChangedIndex` past successful completion, which can poison later reorg/rollback logic."
    },
    {
      "pr": 57770,
      "subject": "ddl/ingest: remove backend context manager and refactor code (#57770)",
      "commit": "db7843a5c4d601af304d28690e7ba8d6037fc721",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/ingest",
        "pkg/lightning/backend/local"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "`IngestIfQuotaExceeded` can import+reset engines while writers are concurrently appending, because flush locks are released before `unsafeImportAndResetAllEngines()`. This can race with local backend import/reset and lead to data loss or corruption in add-index ingest.",
      "impact": "Potential data correctness break (missing/inconsistent index entries) or hangs/crashes if quota-triggered imports occur while other backfill workers continue writing into engines being imported/reset.",
      "fix": "Hold each engine’s `flushLock` (write lock) across the entire flush+import+reset critical section (mirroring the pre-refactor behavior), or introduce an explicit “stop writes” barrier so no `Writer.AppendRows` can run while `unsafeImportAndResetAllEngines()` is executing.",
      "verify": "Force mid-job imports by setting a small `@@tidb_ddl_disk_quota` and running ingest add-index with multiple workers; verify the resulting index with `ADMIN CHECK TABLE/INDEX`, and watch for import/reset errors, stalls, or inconsistent row counts.",
      "evidence": "In this commit, `pkg/ddl/ingest/backend.go:172-205` calls `flushEngines()` then proceeds to `unsafeImportAndResetAllEngines()` without holding any `engineInfo.flushLock`. `pkg/ddl/ingest/backend.go:236-246` shows `flushEngines()` unlocks each engine immediately after `ei.Flush()`. Writers only coordinate via `engineInfo.flushLock` RLock (`pkg/ddl/index.go:2176-2185`, `pkg/ddl/ingest/engine.go:218-222`), so writes can resume while import/reset runs. Local backend reset is destructive (`pkg/lightning/backend/local/engine_mgr.go:412-452` closes+cleans+reopens engine) and `Writer.AppendRows` does not respect the engine import/close mutex (`pkg/lightning/backend/local/engine.go:1250-1280`). Prior to this refactor, flush locks were held across import via deferred unlock in `Flush()` (`pkg/ddl/ingest/backend.go@365a722c:172-205`).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master` (f0813a3696): `pkg/ddl/ingest/backend.go` still unlocks per-engine inside `flushEngines()` and runs import/reset without holding `flushLock`; `pkg/lightning/backend/local/engine.go` `Writer.AppendRows` still doesn’t check engine import/close lock state.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-57770.json",
      "worthy_scope": true,
      "summary": "Refactors DDL ingest backend lifecycle by removing the shared backend context manager, introducing `BackendCtxBuilder`, reshaping flush/import/checkpoint APIs, and switching disk tracking to per-backend usage trackers; also changes TS handling to set TS before import and adjusts checkpoint watermark advancement."
    },
    {
      "pr": 59407,
      "subject": "ddl: split partition tables subtasks like normal tables (#59407)",
      "commit": "b55e62b7714a2171aea0b5ab0146c14488f5798f",
      "modules": [
        "pkg/ddl"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "`generatePlanForPhysicalTable` can silently return partial/empty subtask plans by swallowing planning-time errors (notably `allocNewTS` failures and potentially `getTableRange` errors). With this PR, partitioned tables now use this function, so an intermittent PD/GetTS or snapshot-range error can lead to an incomplete add-index backfill without the framework noticing.",
      "impact": "Silent plan truncation or empty-plan generation can skip some (or all) record key ranges, causing add-index backfill to miss rows and leave the new index incomplete (wrong query results; potential uniqueness/consistency issues). This PR newly routes partition tables through this plan generator, expanding the blast radius from non-partition tables to partitioned tables as well.",
      "fix": "In `generatePlanForPhysicalTable`: (a) check `err` from `getTableRange` before the `(nil,nil)` empty-table fast-path; (b) on `allocNewTS` failure return `(true, err)` (or `(true, errors.Trace(err))`) so `RunWithRetry` actually retries; (c) build metas in a local slice inside the retry closure and only assign/append to the outer slice when the closure completes successfully to avoid partial plans leaking; (d) for `shouldRetry`, return a retryable non-nil error (sentinel) instead of `(true, nil)`.",
      "verify": "Add a unit/integration test (or failpoint) that forces `allocNewTS` to fail after N successful allocations during plan generation; assert `OnNextSubtasksBatch` returns a non-nil error and does not return a partial `metas` slice. Add a test forcing `getTableRange` to return `(nil,nil,err)` (e.g., injected iterateSnapshotKeys error) and assert the error propagates instead of producing an empty plan.",
      "evidence": "`pkg/ddl/backfilling_dist_scheduler.go` `generatePlanForPhysicalTable`: (1) `startKey, endKey, err := getTableRange(...); if startKey == nil && endKey == nil { return nil, nil }` runs before `if err != nil`, so `(nil,nil,err)` is treated as empty and the error is dropped. (2) Inside `handle.RunWithRetry(...)`, TS allocation failures do `importTS, err := allocNewTS(...); if err != nil { return true, nil }`. `handle.RunWithRetry` returns immediately on `err == nil` (`pkg/disttask/framework/handle/handle.go` in this commit), so this does not retry and can return a partially-filled `subTaskMetas` slice as a “successful” plan.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed on `upstream/master`: `pkg/ddl/backfilling_dist_scheduler.go` still returns `(true, nil)` on `allocNewTS` failure and still checks `(startKey==nil && endKey==nil)` before `err`; `pkg/dxf/framework/handle/handle.go` still stops retrying when `err == nil` regardless of the retryable bool.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-59407.json",
      "worthy_scope": true,
      "summary": "Partition tables now use the same region-splitting plan generator as normal tables, but the planner can silently drop errors and return partial/empty subtask lists, risking incomplete add-index backfill; the issue remains present on upstream/master."
    },
    {
      "pr": 61200,
      "subject": "ddl, disttask: import into integrate table mode (#61200)",
      "commit": "b6628ea83528ed90e7ea91c006c4a67d0c97f302",
      "modules": [
        "pkg/ddl",
        "pkg/disttask/importinto",
        "pkg/planner/core",
        "pkg/disttask/framework/storage"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "`IMPORT INTO` classic-kernel submission can leave a table permanently in `TableModeImport` if task creation/commit fails after the mode switch, because `ddl.AlterTableMode` is not transactional with `TaskManager.WithNewTxn` and there is no rollback/compensation on error.",
      "impact": "In classic kernel, a transient failure after `AlterTableMode(...Import...)` (e.g., `planner.Run` error or SQL commit error) can leave the target table stuck in `TableModeImport` with no corresponding DXF task/job committed, so cleanup routines never run. The table then becomes effectively unusable for normal DML/DDL (protected-table-mode errors), and even rerunning `IMPORT INTO` is likely blocked by the same table-mode check.",
      "fix": "Make table-mode transitions failure-safe relative to task creation:\n- Option A (minimal): if `ddl.AlterTableMode(...Import...)` succeeds but `submitTask2DXF` (or the surrounding txn) fails, best-effort revert via `ddl.AlterTableMode(...Normal...)` in the error path (outside the txn, since DDL is independent).\n- Option B (more robust): submit/create the DXF task first, and only after successful task creation/commit, switch table mode to `Import`; ensure tasks won’t start until the mode switch completes (e.g., gate task start on a persisted flag).",
      "verify": "- Add/enable a deterministic failpoint to force `submitTask2DXF` (or the txn `commit`) to fail after `AlterTableMode(...Import...)` succeeds.\n- Run `IMPORT INTO` and assert: (1) the statement returns error, and (2) table mode is `Normal` afterward (no `ErrProtectedTableMode` on a simple `SELECT`/`INSERT`).\n- Also verify the same for standalone-task path and for failures during classic-kernel submit.",
      "evidence": "- `pkg/disttask/importinto/job.go:93-116`: inside `taskManager.WithNewTxn`, it calls `ddl.AlterTableMode(...TableModeImport...)` (line 102) and then submits the DXF task (line 113). Any later error in task submission/txn commit returns error.\n- `pkg/disttask/framework/storage/task_table.go:210-236`: `WithNewTxn` is a SQL `begin` + `commit/rollback` wrapper; errors after the mode change can rollback the job/task records.\n- `pkg/ddl/executor.go:6717-6767`: `AlterTableMode` uses `doDDLJob2` → `DoDDLJobWrapper`, which enqueues the DDL job via `e.deliverJobTask(jobW)` and waits for a submit result; the enqueue is done by DDL job submitter transactions (not the caller’s SQL txn).\n- `pkg/ddl/job_submitter.go:76-136`: `submitLoop` drains `limitJobCh` and `addBatchDDLJobs2Table` persists jobs separately.\n- Impact amplifier: `pkg/planner/core/optimizer.go:258-278` rejects most statements on non-normal tables via `dbutil.CheckTableModeIsNormal`, and `pkg/util/dbutil/table.go:48-52` errors when mode != `TableModeNormal`.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master` (`bab4993b6fd61de478062151bb615a0e09c0aecb`): `pkg/dxf/importinto/job.go:95-121` still alters table mode to `Import` before task submission inside `WithNewTxn`, with no compensating revert on later failure.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-61200.json",
      "worthy_scope": true,
      "summary": "Found one Blocker in classic-kernel `IMPORT INTO` table-mode integration: `ddl.AlterTableMode(...Import...)` is executed before DXF task submission inside a SQL txn, but the DDL job is enqueued/committed independently. If task submission/commit fails afterward, the table can remain stuck in protected `TableModeImport` with no cleanup trigger. This behavior is still present on `upstream/master`."
    },
    {
      "pr": 63465,
      "subject": "ddl: make some `MODIFY COLUMN` skip row reorg (#63465)",
      "commit": "a84aea05598923701b8b8607b22f6719d7859f31",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/table",
        "pkg/tablecodec"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "Rollback cleanup for skip-row-reorg `MODIFY COLUMN` can clear an existing `NOT NULL` constraint when only `ChangingFieldType` was used as an in-progress marker (i.e., when NULLability wasn’t being changed).",
      "impact": "A failed `ALTER TABLE ... MODIFY COLUMN` (skip-row-reorg / checked path) can unexpectedly flip an existing `NOT NULL` column to nullable after rollback, allowing NULL writes and violating prior constraints. This is a metadata integrity issue (schema changes despite DDL failure).",
      "fix": "In rollback helpers (`rollbackModifyColumnJob`, `rollbackModifyColumnJobWithReorg`, `rollbackModifyColumnJobWithIndexReorg`), stop clearing `mysql.NotNullFlag` as part of “modify-column in-progress flag cleanup”. Only clear the in-progress markers (`PreventNullInsertFlag`, `ChangingFieldType`). If there are cases where `NotNullFlag` is set early in some path, guard the deletion narrowly (e.g., only when `PreventNullInsertFlag` was set by the job / when NULLability actually changed).",
      "verify": "Repro idea (strict mode): `CREATE TABLE t(a INT NOT NULL); INSERT INTO t VALUES (1000); ALTER TABLE t MODIFY COLUMN a TINYINT NOT NULL;` (expect failure). Then verify `SHOW CREATE TABLE t` still shows `NOT NULL` and `INSERT INTO t VALUES (NULL)` still fails; with this bug, NULL insert may succeed post-rollback.",
      "evidence": "Rollback for no-row-reorg paths clears `mysql.NotNullFlag` whenever `hasModifyFlag` is true: `pkg/ddl/modify_column.go:397` + `pkg/ddl/modify_column.go:399`, and `hasModifyFlag` is true when `ChangingFieldType` is set: `pkg/ddl/modify_column.go:96`. But `ChangingFieldType` is set for lossy/checked skip-reorg changes even when NULLability is unchanged: `pkg/ddl/modify_column.go:658` + `pkg/ddl/modify_column.go:661` + `pkg/ddl/modify_column.go:662`. Clearing `NotNullFlag` weakens NULL enforcement in DML (`pkg/table/column.go:501`). Same pattern exists in other rollback helpers too (`pkg/ddl/modify_column.go:419`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked later history up to `upstream/master` (bab4993b6fd6): rollback code still clears `mysql.NotNullFlag` under `hasModifyFlag`, so this is not fixed later.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-63465.json",
      "worthy_scope": true,
      "summary": "Reviewed `a84aea0` vs parent with focus on DDL/metadata behavior around new skip-row-reorg / index-only-reorg paths for `MODIFY COLUMN`. Found one Blocker: rollback cleanup can drop an existing `NOT NULL` flag when the job used `ChangingFieldType` (but didn’t intend to change NULLability), causing schema integrity violation after a failed DDL."
    },
    {
      "pr": 63965,
      "subject": "ddl: some code refactor for lossy ddl optimization (#63965)",
      "commit": "a9bb0133e256477a277949a2ca4289d8111bd8a5",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/table"
      ],
      "severity": "Blocker",
      "confidence": "High",
      "what": "Cancel/rollback path for `ActionModifyColumn` assumes refactored `ModifyColumnType` enum values, but does not handle the legacy `mysql.TypeNull` (6) value used by older jobs, which can leave a job stuck in `JobStateCancelling`.",
      "impact": "Compatibility regression for in-flight or queued modify-column jobs created by older TiDB versions (where `ModifyColumnType` may be `mysql.TypeNull`): `ADMIN CANCEL DDL JOBS` can get stuck with the job remaining in `JobStateCancelling`, repeatedly retried by the worker and potentially blocking subsequent DDL jobs.",
      "fix": "In `rollingbackModifyColumn`, normalize legacy values before the switch: if `args.ModifyColumnType == mysql.TypeNull` (or any unknown/non-enum value), recompute it using the same logic as runtime execution (e.g. `args.ModifyColumnType = getModifyColumnType(args, tblInfo, oldCol, job.SQLMode)`), then proceed so the function always sets `job.State` to `Cancelled` or `Rollingback` deterministically.",
      "verify": "Add a unit test that constructs a `model.Job` in `JobStateCancelling` with `ModifyColumnArgs.ModifyColumnType = mysql.TypeNull` (legacy), then runs the cancel conversion (`convertJob2RollbackJob` / `rollingbackModifyColumn`) and asserts `job.State` becomes `JobStateCancelled` or `JobStateRollingback` (never remains `JobStateCancelling`). Cover both `needChangeColumnData == false` and `== true` shapes (with/without `args.ChangingColumn`).",
      "evidence": "`pkg/ddl/rollingback.go:132` switches on `args.ModifyColumnType` but has no case for legacy `mysql.TypeNull` (6); the default path returns `ErrCancelledDDLJob` without changing `job.State` (so a cancelling job can remain `JobStateCancelling`). Second confirmation: cancel flow in `pkg/ddl/job_worker.go:837-843` calls `convertJob2RollbackJob` for `JobStateCancelling` and sets `updateRawArgs = job.IsRollingback()`; if `rollingbackModifyColumn` leaves state as cancelling, the job is persisted still cancelling and will be retried, potentially wedging the DDL queue. The refactor itself acknowledges legacy `mysql.TypeNull` via normalization in `pkg/ddl/modify_column.go:170-178`, but that normalization is not applied in the cancellation/rollback path.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master`: `upstream/master:pkg/ddl/rollingback.go` still lacks a `mysql.TypeNull` compatibility branch and still falls through with `return ErrCancelledDDLJob` after the switch; `git log a9bb0133..upstream/master -- pkg/ddl/rollingback.go` shows only refactors/renames and unrelated changes, no compat handling added.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-63965.json",
      "worthy_scope": true,
      "summary": "Reviewed commit a9bb0133 relative to its first parent, focusing on DDL/modify-column state machine + metadata behavior. Found a Blocker: cancellation/rollback for legacy modify-column jobs can wedge because `rollingbackModifyColumn` switches on the new `ModifyColumnType` values but does not handle legacy `mysql.TypeNull` (6), leaving jobs in `JobStateCancelling`."
    },
    {
      "pr": 52873,
      "subject": "pkg/infoschema: revert cache policy ref (#52873)",
      "commit": "6eb1712feff6b2232f8b5c33acea60e812057e8f",
      "modules": [
        "pkg/infoschema"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`ALTER/REPLACE PLACEMENT POLICY` may not refresh partition-level placement bundles when the altered policy is referenced only by partitions (table-level `PlacementPolicyRef` is nil), leaving partition bundles stale in infoschema.",
      "impact": "After `ALTER PLACEMENT POLICY`, infoschema `ruleBundleMap` can remain stale for partitions that reference the altered policy independently (table has no table-level policy). Any logic consuming `PlacementBundleByPhysicalTableID(partitionID)` (e.g., txn_scope / placement validation paths) can use outdated leader-DC constraints, potentially allowing/denying local transactions incorrectly and diverging from PD’s actual placement rules.",
      "fix": "When `updatePolicies` is non-empty, also detect tables whose **partitions’** `PlacementPolicyRef.ID` is in `updatePolicies` and mark the parent table ID for bundle rebuild (do this in both `completeUpdateTables` and `completeUpdateTablesV2` to keep v1/v2 consistent). Alternatively, on `ActionAlterPlacementPolicy`, precompute dependent tables/partitions (similar to `getPlacementPolicyDependedObjectsIDs`) and mark those tables directly to avoid a full scan.",
      "verify": "Create a partitioned table where only a partition has `PlacementPolicyRef` (table-level nil). Alter that policy’s leader constraints/DC label via `ALTER PLACEMENT POLICY`. After infoschema reload via schema diff, assert `PlacementBundleByPhysicalTableID(partitionID).GetLeaderDC(...)` reflects the new policy; also exercise a local `txn_scope` write path to confirm enforcement matches the updated policy.",
      "evidence": "Placement-policy change only records the policy ID for bundle refresh (`pkg/infoschema/builder_misc.go:47`). The V2 delta-path refresh logic only matches `updatePolicies` against **table-level** `tblInfo.PlacementPolicyRef` (`pkg/infoschema/infoschema_v2.go:905`) and never checks partition definitions’ `PlacementPolicyRef`. Partition bundles are derived from policy `PlacementSettings` (`pkg/ddl/placement/bundle.go:642`, `pkg/ddl/placement/bundle.go:695`), and `onAlterPlacementPolicy` explicitly rebuilds/sends bundles for depended partitions (`partIDs`) (`pkg/ddl/placement_policy.go:238`, `pkg/ddl/placement_policy.go:273`). Partition placement policy can be set without table-level policy (`pkg/ddl/table.go:1768`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked upstream/master (f0813a369666): `completeUpdateTablesV2` still only checks `tblInfo.PlacementPolicyRef` against `updatePolicies` (no partition-level policyRef matching).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-52873.json",
      "worthy_scope": true,
      "summary": "This commit removes the infoschema v2 `policyRefMap` cache and reverts delta bundle refresh to a scan-based approach. A remaining correctness gap exists: placement-policy changes only trigger bundle rebuild for tables with table-level policy refs, not for partitions that reference the policy independently, so partition bundles can remain stale after `ALTER PLACEMENT POLICY`."
    },
    {
      "pr": 53217,
      "subject": "ddl: sync schema version using watch, notify sessions on owner node by job id (#53217)",
      "commit": "5d990c60e4a2928148cde450cd37b38acded2724",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/syncer"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "`SyncJobSchemaVerLoop` now performs a full-prefix etcd `Get`/`Watch` over `/tidb/ddl/all_schema_by_job_versions/` and caches all job-version keys, but the code only deletes these keys for `JobStateSynced` jobs. Terminal rollback/cancel paths (and any deletion failures) can leave per-job keys behind indefinitely, making the watched keyspace and cache unbounded over time and risking MDL schema-sync stalls or resource blowups.",
      "impact": "With the new full-prefix cache, any leftover per-job keys (e.g., rollback-done/cancelled jobs or transient cleanup failures) accumulate under the watched prefix and are loaded/cached cluster-wide. Over time this can bloat etcd response sizes and in-memory `jobNodeVersions`, increasing startup/retry cost and risking `SyncJobSchemaVerLoop` churn or failure, which can in turn stall MDL-based `OwnerCheckAllVersions` and block DDL progress.",
      "fix": "Preferably delete `/tidb/ddl/all_schema_by_job_versions/<jobID>/` once a job reaches any terminal state that no longer needs MDL coordination (at least `RollbackDone`/`Cancelled` in addition to `Synced`), after the final schema-version/MDL sync point. If deletion-on-terminal is hard, scope the watch/cache to only currently-waited jobIDs (on-demand watch per jobID) or attach an expiry/lease strategy for per-job keys to prevent unbounded retention.",
      "verify": "1) Enable MDL, create terminal jobs that end in `RollbackDone`/`Cancelled`, then check etcd for lingering `/tidb/ddl/all_schema_by_job_versions/<jobID>/` keys. 2) Accumulate many such job prefixes and confirm `SyncJobSchemaVerLoop` initial `Get` remains bounded (no oversized responses), and that owner-side `OwnerCheckAllVersions` still completes under load.",
      "evidence": "New behavior: `pkg/ddl/syncer/syncer.go:497` does `etcdCli.Get(ctx, s.jobNodeVerPrefix, WithPrefix())` where `jobNodeVerPrefix = util.DDLAllSchemaVersionsByJob + \"/\"` (`pkg/ddl/syncer/syncer.go:231-233`), so it loads *all* `/tidb/ddl/all_schema_by_job_versions/<job>/<tidb>` keys and watches the whole prefix (`pkg/ddl/syncer/syncer.go:516-520`). Keys are written without lease under MDL via `PutKVToEtcdMono` (`pkg/ddl/syncer/syncer.go:320-333`). Cleanup is gated by `job.State == model.JobStateSynced` at call sites (`pkg/ddl/job_table.go:446`, `pkg/ddl/job_table.go:484`) and in `cleanMDLInfo` (`pkg/ddl/ddl_worker.go:654-660`); rollback/cancel paths don’t set `JobStateSynced` (`pkg/ddl/ddl_worker.go:943-946`, `pkg/ddl/ddl_worker.go:977-981`). Second confirmation (behavior change vs parent): previous MDL path only `Get`-ed `/.../<jobID>/` (`parent 1c4a9c6434 pkg/ddl/syncer/syncer.go:299-329`), so stale keys for other jobs didn’t amplify cost.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed as of `upstream/master` (f0813a3696…): `upstream/master:pkg/ddl/job_scheduler.go#L684` still has the TODO and only deletes when `job.State == model.JobStateSynced`; the full-prefix Get/Watch remains in `upstream/master:pkg/ddl/schemaver/syncer.go#L497`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-53217.json",
      "worthy_scope": true,
      "summary": "Commit introduces per-job DDL completion notifications and an etcd-watch-based cache for MDL job schema-version syncing. High risk: the new full-prefix job-version cache can grow without bound if per-job keys aren’t reliably cleaned for non-`Synced` terminal outcomes, potentially impacting MDL-based DDL progress and resource usage."
    },
    {
      "pr": 53301,
      "subject": "*: keep in-memory resident for model.TableInfo with special attributes (#53301)",
      "commit": "625516b2757332e4689f2ed5d6e31722235fa754",
      "modules": [
        "pkg/ddl",
        "pkg/infoschema"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`infoschema` v2 `ListTablesWithSpecialAttribute` can miss special-attribute tables after cross-database rename because it deduplicates resident entries using only `tableID`, while resident entries are keyed/ordered by `(dbName, tableID, schemaVersion)`, allowing a tomb entry for the old DB to suppress the live entry for the new DB.",
      "impact": "`InfoSchema.ListTablesWithSpecialAttribute` can incorrectly omit TTL/TiFlash (and `AllSpecialAttribute`) tables after a cross-database `RENAME TABLE`/`ALTER TABLE ... RENAME TO other_db.*`, which can stop TTL cache/worker discovery and TiFlash refresh from seeing the table, leading to stale/missing background management behavior and inconsistent metadata visibility for these features.",
      "fix": "Change dedup to match the actual resident key: dedup by `(dbName, tableID)` (or `(dbName, tableID, schemaVersion)` if needed), not by `tableID` alone. Minimal fix: track `lastDB` + `lastTableID` and only skip when both match, so cross-DB rename entries for the same tableID don’t suppress each other.",
      "verify": "Add a regression test (e.g. extend `pkg/infoschema/test/infoschemav2test/v2_test.go`) that: (1) creates a TTL-enabled table (or TiFlash replica table) in `db2`, (2) renames it into `db1`, (3) asserts `InfoSchema().ListTablesWithSpecialAttribute(infoschema.AllSpecialAttribute)` returns it under `db1` and not under `db2`, for both `tidb_schema_cache_size > 0` (v2) and `= 0` (v1).",
      "evidence": "In `pkg/infoschema/infoschema_v2.go:1066` (`(*infoschemaV2).ListTablesWithSpecialAttribute`), dedup is keyed only by `lastTableID` (`pkg/infoschema/infoschema_v2.go:1076-1080`) even though resident entries are ordered/keyed by `dbName` then `tableID` (`compareTableInfoItem` in `pkg/infoschema/infoschema_v2.go:263-278`). For cross-DB rename, the same `tableID` can appear under two different `dbName` values at the same `schemaVersion`: drop path inserts a tomb in old DB (`applyDropTableV2` calls `infoData.remove` with `dbInfo.Name.L` in `pkg/infoschema/infoschema_v2.go:934-960`; `Data.remove` writes a tomb resident entry in `pkg/infoschema/infoschema_v2.go:216-226`), and create path inserts the live entry in new DB (`Builder.addTable` uses `di.Name.L` in `pkg/infoschema/builder.go:951-960`; `Data.add` writes the live resident entry in `pkg/infoschema/infoschema_v2.go:187-204` when `hasSpecialAttributes` is true). If the tomb entry is visited first and no other resident items with different tableIDs intervene, `lastTableID` causes the subsequent live entry (same tableID, different dbName) to be skipped.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` as of this workspace: `pkg/infoschema/infoschema_v2.go` still orders resident items by `dbName` first (`compareTableInfoItem`) and still dedups only by `lastTableID` in `ListTablesWithSpecialAttribute`, so the edge case appears unfixed upstream.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-53301.json",
      "worthy_scope": true,
      "summary": "Adds `InfoSchema.ListTablesWithSpecialAttribute` and keeps `model.TableInfo` resident for “special attribute” tables in infoschema v2; found a High-severity correctness edge case where v2 listing can drop TTL/TiFlash tables after cross-database renames due to tableID-only dedup over a `(dbName, tableID, schemaVersion)`-keyed resident index."
    },
    {
      "pr": 53544,
      "subject": "ddl: Reorganize Partition failure/rollback tests and fixes (#53544)",
      "commit": "f2ed8228ffc0cee144453ab334cdfe4aa5dac4a5",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Backward-compatibility break in persisted metadata: changing `PartitionInfo.NewPartitionIDs` from an untagged field (JSON key `NewPartitionIDs`) to a tagged snake_case key (`new_partition_ids`) prevents new code from decoding legacy metadata during in-flight truncate-partition DDL, and can bypass truncating-partition write blocking.",
      "impact": "If a cluster upgrades (or runs mixed binaries during rolling upgrade) while any truncate-partition DDL with global index is in-flight (where `pi.NewPartitionIDs` is non-empty), newer nodes will fail to decode the legacy `NewPartitionIDs` field and treat it as empty.\nThat can incorrectly allow writes/updates into truncating (non-public) partitions, breaking the DDL state-machine guarantees and potentially causing data/index inconsistency or DDL rollback/failure.",
      "fix": "Keep backward decoding compatibility for the old key during upgrades/mixed-version windows:\n- Implement `UnmarshalJSON` for `model.PartitionInfo` to accept both `\"new_partition_ids\"` and legacy `\"NewPartitionIDs\"`.\n- Alternatively, add a legacy shadow field (e.g., `LegacyNewPartitionIDs []int64 `json:\"NewPartitionIDs,omitempty\"``) and normalize after unmarshal (still requires some hook to merge).",
      "verify": "- Add a unit test that unmarshals a `TableInfo` JSON blob containing `\"NewPartitionIDs\":[...]` (legacy) into current structs and asserts `pi.NewPartitionIDs` is populated.\n- Simulate rolling-upgrade behavior: persist table meta with legacy key, restart a node on the new binary, and ensure DML into truncating partitions is still rejected during truncate-partition DDL.",
      "evidence": "- Metadata encoding changes the JSON key for an existing persisted field: `pkg/meta/model/table.go:712` changed `NewPartitionIDs []int64` (first parent) to `NewPartitionIDs []int64 `json:\"new_partition_ids,omitempty\"``.\n- First-parent definition confirms old key would be `NewPartitionIDs`: `git show f2ed8228^:pkg/meta/model/table.go` shows no struct tag for `NewPartitionIDs`.\n- TableInfo is persisted via `encoding/json`: `pkg/meta/meta.go:706` uses `json.Marshal(tableInfo)`.\n- `encoding/json` uses the explicit tag name as the only struct-field key; folded matching is case-only and does not ignore underscores (so `NewPartitionIDs` won’t match `new_partition_ids`): confirmed by stdlib `encoding/json` implementation (`.../src/encoding/json/encode.go:1146` uses tag name; `.../src/encoding/json/fold.go:16` folds case but keeps `_`).\n- Runtime DML gating during truncate-partition relies on this field being present: `pkg/table/tables/partition.go:1619` and `pkg/table/tables/partition.go:1758` call `t.Meta().Partition.HasTruncatingPartitionID(...)`, which reads `pi.NewPartitionIDs` (`pkg/meta/model/table.go:820`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `remotes/upstream/master:pkg/meta/model/table.go:880` and it still uses `json:\"new_partition_ids,omitempty\"` with no legacy-key decode hook (no `UnmarshalJSON`), so the cross-version decode gap remains.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-53544.json",
      "worthy_scope": true,
      "summary": "Reviewed f2ed8228ff vs first parent for DDL partition reorg/rollback behavior and related metadata. Core reorganize-partition rollback logic looks consistent with the new failpoint-driven tests, but there is a High-severity metadata compatibility hazard: renaming the persisted JSON key for `PartitionInfo.NewPartitionIDs` can break mixed-version/upgrade correctness for in-flight truncate-partition DDL."
    },
    {
      "pr": 53548,
      "subject": "ddl: decouple job scheduler from 'ddl' and make it run/exit as owner changes (#53548)",
      "commit": "04c66ee9508c0bfb573ef3effa3e6321494c8d51",
      "modules": [
        "pkg/ddl",
        "pkg/owner"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "FLASHBACK CLUSTER: `splitRegionsByKeyRanges` retries `SplitRegions` in a tight infinite loop without checking context cancellation. After this change, it is invoked with the scheduler/worker context (canceled on owner change), so an owner change/shutdown during StageWriteOnly can spin/hang and block scheduler exit.",
      "impact": "If an owner change (or DDL shutdown) happens while FLASHBACK CLUSTER is splitting regions, the old owner can enter a tight retry loop repeatedly failing `SplitRegions`, consuming CPU and potentially hammering PD, while also preventing the owner-scoped scheduler from exiting/cleaning up promptly.",
      "fix": "In pkg/ddl/cluster.go `splitRegionsByKeyRanges`, stop retrying when `ctx.Err()!=nil` (return/break) and add at least a small sleep/exponential backoff on repeated errors to avoid hot spinning. Consider returning an error so FLASHBACK CLUSTER can surface cancellation cleanly and retry on the new owner.",
      "verify": "Unit: use a mock `kv.SplittableStore` where `SplitRegions` returns `ctx.Err()` once canceled; assert `splitRegionsByKeyRanges` returns promptly (no spin). Integration/failpoint: trigger owner change during FLASHBACK CLUSTER StageWriteOnly and confirm the old owner's scheduling loop exits and the job can be resumed by the new owner.",
      "evidence": "pkg/ddl/cluster.go:612-623 loops `for { _, err := s.SplitRegions(ctx,...); if err==nil break }` with no ctx.Err/break/backoff; called with `w.ctx` in pkg/ddl/cluster.go:725. Worker ctx for owner DDL jobs is derived from the scheduler ctx created in pkg/ddl/job_table.go:96-109 and canceled on owner retire via pkg/ddl/job_table.go:170-178 (triggered by pkg/owner/manager.go:230-235).",
      "later_fixed": false,
      "later_fix_evidence": "Checked upstream/master: `splitRegionsByKeyRanges` still retries indefinitely without a ctx.Err guard (upstream/master `pkg/ddl/cluster.go` shows the same inner `for { SplitRegions(ctx) ... }` loop).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-53548.json",
      "worthy_scope": true,
      "summary": "Refactors DDL scheduling into an owner-scoped `jobScheduler` started/stopped via `owner.Listener`, and propagates scheduler/worker context into multiple DDL paths. Found a High-risk cancellation bug in FLASHBACK CLUSTER region-splitting that can spin forever when the owner changes."
    },
    {
      "pr": 53821,
      "subject": "infoschema: make build bundle faster for infoschema v2 (#53821)",
      "commit": "87d6f0f138a7a37a4255478f33270b39de022c70",
      "modules": [
        "pkg/infoschema/infoschema_v2.go",
        "infoschema v2 placement bundles"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Full placement-bundle rebuild in infoschema v2 can miss some placement-policy tables after cross-database `RENAME TABLE`, due to `ListTablesWithSpecialAttribute` iterating `tableInfoResident` ordered by dbName and deduping only consecutive `tableID`s (with tomb handling after dedup). In certain key-order adjacency cases, the old-db tomb record can cause the new-db live record to be skipped, leaving `ruleBundleMap` incomplete.",
      "impact": "On a full infoschema v2 reload (deltaUpdate=false), `ruleBundleMap` may miss placement bundles for affected tables/partitions. This breaks placement-policy/txn-scope semantics: `PlacementBundleByPhysicalTableID` returns false, causing local-txn commit checks to error (`pkg/session/session.go:4040`) or txn-scope verification to behave incorrectly (`pkg/distsql/request_builder.go:702`). The failure can persist until another bundle rebuild/update happens.",
      "fix": "Make `ListTablesWithSpecialAttribute` choose the latest non-tomb record per `tableID` by comparing `schemaVersion` across *all* dbNames (e.g., collect best-per-tableID in a map, then group by dbName), or change `tableInfoResident` ordering/indexing to be `tableID`-first for correct dedup and group results afterward. Avoid relying on dbName-ordered iteration + `lastTableID` for correctness when dbName can change across versions.",
      "verify": "Add a regression test that: (1) creates two DBs where old DB name sorts after new DB name, (2) creates a table with placement policy in old DB, (3) `RENAME TABLE oldDB.t TO newDB.t`, (4) forces a full infoschema v2 rebuild (deltaUpdate=false / full load path), then asserts `PlacementBundleByPhysicalTableID(tableID)` is present and `ListTablesWithSpecialAttribute(PlacementPolicyAttribute)` returns the table under the new DB.",
      "evidence": "`pkg/infoschema/infoschema_v2.go:1002` switches full bundle rebuild to scan `ListTablesWithSpecialAttribute(PlacementPolicyAttribute)` instead of all schemas/tables.\nPlacement-policy tables are now inserted into `Data.tableInfoResident` because `hasSpecialAttributes` includes `PlacementPolicyAttribute` (`pkg/infoschema/infoschema_v2.go:187`, `pkg/infoschema/infoschema_v2.go:1074`).\n`tableInfoResident` is ordered by `dbName` first (`pkg/infoschema/infoschema_v2.go:263`). The listing logic iterates in reverse and “dedups” only consecutive same `tableID` *before* tomb handling (`pkg/infoschema/infoschema_v2.go:1081`).\nCross-database `RENAME TABLE` keeps the same `TableID` (`pkg/ddl/ddl_api.go:7203`), and v2 diff application writes a tomb record into `tableInfoResident` using the *old* dbName (`pkg/infoschema/builder.go:370`, `pkg/infoschema/infoschema_v2.go:934`, `pkg/infoschema/infoschema_v2.go:216`).\nIf the old-db tomb item and new-db live item become adjacent in the reverse iteration order (e.g., minimal setup with only the moved table as the only “resident” entry in each DB), `lastTableID` can cause the live record to be skipped, so the tableID never reaches `updateTableBundles` and its bundle is missing. Full reload reuses existing `do.infoCache.Data` (`pkg/domain/domain.go:317`), so the tomb/live combination can persist into the full-rebuild path.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` at `f0813a369666ac6314e76f5e573e982fe50c8a09`: `updateInfoSchemaBundlesV2` still rebuilds bundles from `ListTablesWithSpecialAttribute(PlacementPolicyAttribute)`, and the v2 resident list still uses dbName-first ordering with lastTableID-based dedup (no evident fix for cross-db rename adjacency).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-53821.json",
      "worthy_scope": true,
      "summary": "Change speeds up infoschema v2 placement bundle rebuild by using the resident “special tables” index and a new `PlacementPolicyAttribute` filter. However, the new full-rebuild path now depends on `ListTablesWithSpecialAttribute`’s dbName-ordered scan + `lastTableID` dedup, which can skip a placement-policy table after cross-database renames in certain iteration-adjacency cases, producing missing placement bundles on full reload; this pattern is still present on upstream/master."
    },
    {
      "pr": 54008,
      "subject": "ddl: clean ingest temp data dir in background loop (#54008)",
      "commit": "53901e8f90ada8a8d575a94cb715b9aa9b56fe45",
      "modules": [
        "pkg/ddl/ddl.go",
        "pkg/ddl/index.go",
        "pkg/ddl/job_table.go",
        "pkg/ddl/ingest/backend_mgr.go",
        "pkg/ddl/ingest/env.go"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "Potential regression: stale ingest temp dirs are no longer cleaned just-in-time before the ingest disk pre-check, because cleanup moved to a background loop that first runs only after a 1-minute tick. This creates a window where `PreCheckUsage()` can fail due to stale-dir disk bloat and the add-index job can be cancelled (non-retryable) even though the new cleanup loop would have freed space soon.",
      "impact": "On nodes where stale ingest temp dirs consume significant disk (e.g., after crash / large ingest backfill), a fast add-index job can be cancelled if it hits the disk pre-check before the next 1-minute cleanup tick, even though cleanup could have freed space shortly. This is a user-visible DDL availability regression (spurious job cancellations/retries) tied to timing/concurrency.",
      "fix": "Run one cleanup iteration immediately before starting the ticker loop (or right after env init) so stale dirs don’t survive long enough to trip `PreCheckUsage()`.\nOptional hardening: on `PreCheckUsage()` failure caused by stale-dir bloat, consider falling back to txn-merge instead of cancelling (if semantics allow).",
      "verify": "- Create a large stale job subdir under the ingest temp root (`${temp-dir}/tmp_ddl-{port}`) so free space drops below the 10% threshold, then start a fast add-index job shortly after TiDB start (or shortly after a previous ingest job completes).\n- Expected after fix: cleanup runs before disk pre-check, preventing a spurious `ErrIngestCheckEnvFailed` cancellation when stale dirs are the primary cause of low disk; job should proceed (or at least not be cancelled before cleanup has a chance to run).",
      "evidence": "- Cleanup trigger moved off the ingest availability check: `pkg/ddl/ingest/backend_mgr.go:100` now only runs `diskRoot.PreCheckUsage()`; first-parent version previously ran cleanup first (`pkg/ddl/ingest/backend_mgr.go:105` in first parent had `m.cleanupSortPath(ctx)`).\n- New cleanup loop runs on a 1-minute ticker with no initial run: `pkg/ddl/ddl.go:891`.\n- Disk check failure returns `ErrIngestCheckEnvFailed` when available disk <10%: `pkg/ddl/ingest/disk_root.go:118`.\n- That error path can cancel add-index at StateNone: `pkg/ddl/index.go:693` cancels when `!errorIsRetryable`, and `pkg/ddl/index.go:1011` only treats a narrow set of `*terror.Error` codes as retryable.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` (f0813a3696): cleanup is still ticker-based with no immediate run (`upstream/master:pkg/ddl/ddl.go` contains `CleanUpTempDirLoop` with a 1-minute ticker), and add-index backfill selection still performs a disk `PreCheckUsage()` without any synchronous temp-dir cleanup beforehand (`upstream/master:pkg/ddl/index.go` `pickBackfillType`).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-54008.json",
      "worthy_scope": true,
      "summary": "This commit moves ingest temp-dir cleanup out of `litBackendCtxMgr.CheckMoreTasksAvailable` and into a periodic DDL background loop (`CleanUpTempDirLoop`) that queries `mysql.tidb_ddl_job` to decide which per-job directories are stale. `InitGlobalLightningEnv` is refactored to accept a caller-generated ingest temp root and now returns an `ok` flag."
    },
    {
      "pr": 54185,
      "subject": "*: optimize the speed of full load (#54185)",
      "commit": "2934442a76ca56a6521a6be4c951a633227456cb",
      "modules": [
        "pkg/domain",
        "pkg/infoschema",
        "pkg/meta",
        "pkg/parser/model",
        "pkg/structure"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Schema-cache full-load fast path uses regex + partial unescaping for table names; JSON `\\uXXXX` escapes (e.g., from `<`, `>`, `&`) are not decoded, so `TableName2ID` keys can be wrong and infoschema v2 name lookups can fail.",
      "impact": "When `tidb_schema_cache_size>0`, full load can record table names containing HTML-escaped characters as literal `\\uXXXX` in `DBInfo.TableName2ID`, causing `TableByName`/`TableExists` to fail (tables become invisible/unreachable by name) and behavior to differ between schema-cache enabled vs disabled.",
      "fix": "Decode the captured JSON string with a real JSON/string unescape (e.g., wrap in quotes and `json.Unmarshal` into `string`, or use `strconv.Unquote`) so `\\uXXXX` sequences are decoded; keep the regex fast-path but replace ad-hoc `Unescape`. Add a regression test covering a table name containing `<`/`&` under schema-cache full load.",
      "verify": "1) Create a table with name containing `<` or `&` (backticked) and no must-load attributes (no partition/TTL/TiFlash/etc). 2) Set global `tidb_schema_cache_size` > 0 and trigger an infoschema full load (restart or force fallback to full load). 3) Confirm the table is still found via `TableByName`/`SHOW TABLES`/`SELECT` under schema-cache mode.",
      "evidence": "TableInfo is persisted via `encoding/json.Marshal` (`pkg/meta/meta.go:714`), and the repo explicitly notes default JSON escaping of \">\", \"<\", \"&\" to `\\u003e/\\u003c/\\u0026` unless disabled (`pkg/executor/trace.go:119`). The new fast-path extracts table name with regex + `Unescape` (`pkg/meta/meta.go:1030`, `pkg/meta/meta.go:1033`, `pkg/meta/meta.go:1065`), but `Unescape` only handles `\\\"` and `\\\\` (no `\\uXXXX` decoding). These extracted strings flow into infoschema-v2 btrees (`pkg/domain/domain.go:480`, `pkg/infoschema/builder.go:930`) and lookup matches exact `tbl.L` (`pkg/infoschema/infoschema_v2.go:471`). Identifier validation does not forbid `<`/`&` (`pkg/util/util.go:288`).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed as of `upstream/master` (f0813a3696): `pkg/meta/meta.go:1334` still defines `Unescape` with only `\\\"`/`\\\\` replacements, and `pkg/meta/meta.go:811` still uses `json.Marshal(tableInfo)` when persisting table metadata (so `\\u003c/\\u003e/\\u0026` escapes are still possible).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-54185.json",
      "worthy_scope": true,
      "summary": "This commit speeds up infoschema v2 full load by extracting table name→ID mappings from meta and only unmarshalling “special-attribute” tables. The new regex-based name extraction only partially unescapes JSON strings, so `\\uXXXX`-escaped characters can leak into `TableName2ID` and break table lookups under schema-cache mode."
    },
    {
      "pr": 54804,
      "subject": "pkg/parser: support SWITCH_GROUP syntax for runaway watch (#54804)",
      "commit": "c9baef45d40c1765203953ecb76c58f394c7b707",
      "modules": [
        "pkg/parser",
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/resourcegroup/runaway",
        "pkg/session",
        "pkg/executor"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Bootstrap schema upgrade adds `switch_group_name` to runaway watch system tables, but inserts rely on `INSERT ... VALUES` without column lists; during rolling upgrades, older TiDB binaries will start failing inserts after any newer node upgrades the tables.",
      "impact": "Rolling upgrade hazard: once any new TiDB upgrades the system tables, older TiDB servers still running will hit `column count doesn't match value count` on inserts into `mysql.tidb_runaway_watch` / `mysql.tidb_runaway_watch_done` (e.g., `ADD QUERY WATCH`, quarantine persistence/cleanup), leading to errors, inconsistent watch propagation, and degraded runaway/resource-control behavior.",
      "fix": "Make this rolling-upgrade safe via a 2-phase migration: (1) first release changes all writes to `mysql.tidb_runaway_watch(_done)` to use explicit column lists (and reads to avoid `SELECT *`), (2) only in a later bootstrap version add `switch_group_name`. If schema must change now, consider new v2 tables + dual-read/dual-write until old TiDBs are gone, or gate the `ALTER TABLE` until all TiDB nodes are upgraded.",
      "verify": "Simulate rolling upgrade: run an older TiDB and a newer TiDB concurrently; let the newer one perform the ver212 upgrade; then from the older TiDB trigger `AddRunawayWatch` paths (e.g., `ADD QUERY WATCH`, or a quarantining event) and confirm no insert errors and watch persistence/cleanup still works.",
      "evidence": "- Schema change: `pkg/session/bootstrap.go:3089` adds bootstrap ver212; `pkg/session/bootstrap.go:3093`/`pkg/session/bootstrap.go:3094` run `ALTER TABLE mysql.tidb_runaway_watch(_done) ADD COLUMN switch_group_name ...`.\n- Upgrade runs automatically when `ver < currentBootstrapVersion`: `pkg/session/bootstrap.go:1401`.\n- Old TiDB binaries insert without column lists: parent blob shows `INSERT ... VALUES` with fewer values (watch: `c9baef45d40c1765203953ecb76c58f394c7b707^:pkg/resourcegroup/runaway/record.go:112`; done: `c9baef45d40c1765203953ecb76c58f394c7b707^:pkg/resourcegroup/runaway/record.go:132`). New code still uses `INSERT ... VALUES` (watch: `pkg/resourcegroup/runaway/record.go:122`/`pkg/resourcegroup/runaway/record.go:133`; done: `pkg/resourcegroup/runaway/record.go:154`).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed on upstream/master: `upstream/master:pkg/session/upgrade_def.go:1852`/`upstream/master:pkg/session/upgrade_def.go:1853` still add `switch_group_name`, and `upstream/master:pkg/resourcegroup/runaway/record.go:133` still uses `INSERT ... VALUES` without column lists.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-54804.json",
      "worthy_scope": true,
      "summary": "Commit adds runaway action `SWITCH_GROUP(name)` across parser/DDL/executor/runaway-manager, persists `switch_group_name` via bootstrap ver212 system-table upgrades, and updates infoschema/show formatting to display the new action."
    },
    {
      "pr": 55025,
      "subject": "ddl: integrate fast create table into normal general DDL workflow (#55025)",
      "commit": "41bb8f57c7e634087b05f26d00a1097491a06d67",
      "modules": [
        "pkg/ddl"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Merged `ActionCreateTables` jobs drop shared `InvolvingSchemaInfo` (e.g. placement policy refs), allowing the DDL scheduler to run conflicting policy DDL concurrently and potentially commit tables with dangling placement policy references.",
      "impact": "With `tidb_enable_fast_create_table=ON`, two create-table statements that get merged into one `ActionCreateTables` job can lose shared-involving dependencies (notably placement policies). The scheduler may then run `DROP PLACEMENT POLICY p` concurrently with the merged create-tables job that still references `p`, enabling write-skew where the policy is dropped but the table(s) commit with a dangling `PlacementPolicyRef`, breaking metadata invariants and potentially leading to incorrect/failed placement rule handling and later DDL/infoschema issues.",
      "fix": "When merging into `ActionCreateTables`, build `combinedJob.InvolvingSchemaInfo` as a union/dedup of all original jobs’ `GetInvolvingSchemaInfo()` (preserving `Mode`), not just newly-created `{Database, Table}` entries. This keeps shared objects (e.g. placement policies) in the merged job so the scheduler can serialize conflicts correctly.",
      "verify": "Add a unit test for `mergeCreateTableJobsOfSameSchema` ensuring policy/shared entries are preserved in the merged job’s `InvolvingSchemaInfo`.\nAdd an integration test: enable fast create; submit two `CREATE TABLE ... PLACEMENT POLICY p` concurrently to force merge, concurrently `DROP PLACEMENT POLICY p`; assert the drop is blocked/cancelled as “in use” and no table ends up referencing a non-existent policy.",
      "evidence": "Fast-create merging is enabled in the normal submit-to-job-table path: `pkg/ddl/executor.go:10127`-`pkg/ddl/executor.go:10138` calls `mergeCreateTableJobs` when `EnableFastCreateTable` is on.\nThe merged `ActionCreateTables` job rebuilds `InvolvingSchemaInfo` using only `{Database, Table}` entries and drops any shared-involving objects from the original `ActionCreateTable` jobs: `pkg/ddl/executor.go:3202`-`pkg/ddl/executor.go:3246`.\nBut individual create-table jobs can include shared-involving placement policies: `pkg/ddl/executor.go:2962`-`pkg/ddl/executor.go:3003` adds `{Policy: ..., Mode: SharedInvolving}`.\nJob scheduler concurrency control relies on `InvolvingSchemaInfo`, and policy conflicts are only detected via `info.Policy`: `pkg/ddl/ddl_running_jobs.go:126`-`pkg/ddl/ddl_running_jobs.go:172`.\nDrop placement policy jobs involve only the policy key and are pre-checked via InfoSchema (won’t see pending/merged create-table jobs): `pkg/ddl/executor.go:9496`-`pkg/ddl/executor.go:9529`.\nSecond confirmation (write-skew risk): create-table policy existence checks are snapshot reads without locking the policy key: `pkg/ddl/placement_policy.go:162`-`pkg/ddl/placement_policy.go:177`; drop-policy “not in use” checks are also InfoSchema/meta snapshot based: `pkg/ddl/placement_policy.go:343`-`pkg/ddl/placement_policy.go:366`.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master` (f0813a369666): the merge logic still rebuilds `InvolvingSchemaInfo` with only `{Database, Table}` in `upstream/master:pkg/ddl/job_submitter.go:207`-`upstream/master:pkg/ddl/job_submitter.go:247`, still omitting shared-involving policy entries from original create-table jobs.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-55025.json",
      "worthy_scope": true,
      "summary": "Reviewed this commit vs first parent, focusing on DDL metadata/state-machine impacts of integrating fast create table. Found a High-severity concurrency/metadata issue: the new create-table job merging path rebuilds `InvolvingSchemaInfo` incompletely, dropping shared dependencies (notably placement policies), which can let conflicting DDLs run concurrently and create inconsistent metadata; this is not fixed in later upstream history."
    },
    {
      "pr": 55049,
      "subject": "ddl: record get owner TS and compare it before runReorgJob quit (#55049)",
      "commit": "aaca081cec39c6a120085c840acaf32c38885ab2",
      "modules": [
        "pkg/ddl/ddl.go",
        "pkg/ddl/job_scheduler.go",
        "pkg/ddl/reorg.go"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Owner-generation stamp uses `time.Now().Unix()` (seconds), so rapid owner re-elections can reuse the same `ownerTS`, defeating the stale-result filter in `runReorgJob()`.",
      "impact": "If the instance loses and regains DDL ownership within the same second, `ownerTS` can collide and stale `reorgFn` completion (often driven by canceled scheduler context / ownership change) can be misclassified as current, reintroducing the original owner-change bug (potentially causing incorrect DDL failure/rollback or incorrect state-machine progression).",
      "fix": "Use a monotonic owner-epoch instead of wall-clock seconds: e.g. store `ownerEpoch` as an `atomic.Int64` (or `atomicutil.Int64`) incremented on every `OnBecomeOwner()`, and record/compare that epoch in `reorgFnResult`. If wall time is preferred, at least switch to `time.Now().UnixNano()` plus a fallback atomic counter to guarantee uniqueness.",
      "verify": "Force rapid owner re-election (e.g. via owner-manager failpoints / resign loop) so `OnBecomeOwner()` runs twice within one second while a reorg goroutine completes; confirm current code can accept the stale result when the stamp collides, and confirm the proposed epoch-based fix always treats the old result as stale regardless of timing.",
      "evidence": "`pkg/ddl/job_scheduler.go`: `ownerListener.OnBecomeOwner()` sets `reorgCtx` owner stamp via `time.Now().Unix()` (second granularity). `pkg/ddl/reorg.go`: `runReorgJob()` tags `reorgFnResult.ownerTS` with `beOwnerTS := w.ddlCtx.reorgCtx.getOwnerTS()` and only treats results as stale when `res.ownerTS != curTS`. `pkg/owner/manager.go`: `campaignLoop()` can call `toBeOwner()` again immediately after `RetireOwner()` with no enforced delay, so multiple `OnBecomeOwner()` calls can occur within the same second. `upstream/master` still uses `time.Now().Unix()` for this stamp.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` (f0813a3696): the stamp/compare mechanism remains and still uses `time.Now().Unix()` in `pkg/ddl/job_scheduler.go`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-55049.json",
      "worthy_scope": true,
      "summary": "The change correctly tags reorg goroutine results with an owner stamp to ignore stale completions across DDL owner changes, but the chosen stamp (`time.Now().Unix()` seconds) is not a unique owner-epoch and can collide under rapid re-elections, allowing stale results through."
    },
    {
      "pr": 55264,
      "subject": "*: Require GLOBAL IndexOption for creating Global Index (#55264)",
      "commit": "2fb61a5eb23fbe2f2c5f02e71f45fc4193de1bea",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/index.go",
        "pkg/ddl/executor.go",
        "pkg/parser/model/ddl.go",
        "pkg/ddl/partition.go",
        "pkg/util/dbterror",
        "pkg/errno"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Forward-compat regression for in-flight DDL jobs: old queued add-index / add-primary-key jobs that relied on the legacy job-arg `global` boolean may be executed with `IndexInfo.Global=false` after this commit, because the worker no longer decodes/uses the legacy `global` flag and instead derives `IndexInfo.Global` solely from `IndexOption.Global`.",
      "impact": "Rolling upgrade / owner switch while an *old-format* DDL job is queued (or before it has appended the index into `tblInfo.Indices`) can create a **local** unique index instead of a **global** one on a partitioned table. That can silently weaken uniqueness guarantees across partitions (allowing duplicates and inconsistent metadata vs user intent).",
      "fix": "When decoding add-index / add-PK jobs, if the legacy trailing `global` arg exists and is `true`, propagate it into the new representation (e.g. initialize `indexOption` if nil and set `indexOption.Global=true`, or explicitly set `indexInfo.Global` from legacy arg for v1 jobs). Also consider setting the legacy `global` arg to `indexOption.Global` when enqueueing, to preserve mixed-version owner safety.",
      "verify": "1) On pre-#55264 TiDB, enqueue (but do not let complete) `ALTER TABLE p ADD UNIQUE INDEX idx(a)` on a partitioned table with `tidb_enable_global_index=ON` where `a` doesn’t include all partitioning columns.\n2) Upgrade to this commit while the job is still pending in `ADMIN SHOW DDL JOBS`.\n3) Let the job run and verify `information_schema.tidb_indexes` / table meta shows `IndexInfo.Global=true` (or job is rejected), and uniqueness is enforced across partitions (duplicates across partitions should fail).",
      "evidence": "- New behavior: `BuildIndexInfo` sets `idxInfo.Global` only from `indexOption.Global` (`pkg/ddl/index.go:317`, `pkg/ddl/index.go:357`).\n- Job arg decoding no longer reads the trailing legacy `global` bool (`pkg/ddl/index.go:564`, `pkg/ddl/index.go:579`).\n- Yet DDL jobs still carry a trailing `global` arg (now always `false`) (`pkg/ddl/executor.go:4541`, `pkg/ddl/executor.go:4697`).\n- First-parent code used that trailing `global` bool to set `IndexInfo.Global` (`4764397725a7381d9424fef14c812192fe7441fa:pkg/ddl/index.go:344`) and decoded it from RawArgs (`4764397725a7381d9424fef14c812192fe7441fa:pkg/ddl/index.go:582`).\n- `Job.DecodeArgs` truncates to the number of decode targets, so an extra legacy arg can be silently ignored when no longer decoded (`pkg/parser/model/ddl.go:767`).",
      "later_fixed": false,
      "later_fix_evidence": "Up to `upstream/master`, legacy v1 job arg `Global` is explicitly marked unused (`upstream/master:pkg/meta/model/job_args.go:1383`), and index building still uses `IndexOption.Global` via `BuildIndexInfo` (called from `checkAndBuildIndexInfo`) without consuming the legacy bool (`upstream/master:pkg/ddl/index.go:805`, `upstream/master:pkg/ddl/index.go:839`).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-55264.json",
      "worthy_scope": true,
      "summary": "Requires explicit `GLOBAL` IndexOption when a unique key on a partitioned table doesn’t include all partitioning columns; adds `UPDATE INDEXES` handling for partition reorg and shifts `IndexInfo.Global` derivation to `ast.IndexOption.Global` (legacy job-arg `global` is effectively deprecated)."
    },
    {
      "pr": 55891,
      "subject": "ddl, planner: avoid running queries on the non-Public state table (#55891)",
      "commit": "e1c44f730cba187ed4500e9b2efe9479430e635d",
      "modules": [
        "pkg/ddl",
        "pkg/infoschema",
        "pkg/planner/core",
        "pkg/executor",
        "pkg/meta"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Implicit foreign-key cascade DML is likely broken by the new “non-Public table => ErrTableNotExists” planner gate: the PR/comment states cascades should still modify `WriteOnly/DeleteOnly` tables, but FK cascades are executed via internal SQL planning that now rejects those tables.",
      "impact": "ON DELETE/UPDATE CASCADE (and SET NULL) can’t be applied when the child table is in `WriteOnly/DeleteOnly` (e.g., during DROP TABLE/CREATE TABLE(FK)/restore windows). The implicit cascade executor will fail at plan build with `ErrTableNotExists`, causing the outer DML to error/rollback and contradicting the stated/encoded behavior that implicit cascades should still modify the table.",
      "fix": "Either (a) explicitly allow internal FK-cascade planning/execution to access non-Public tables (e.g., gate the non-Public-table rejection in `tryLockMDLAndUpdateSchemaIfNecessary` on a dedicated internal-cascade flag such as `StmtCtx.InHandleForeignKeyTrigger`/context key), or (b) make FK trigger building treat `childTable.Meta().State != StatePublic` as non-cascade (e.g., downgrade to restrict-style FK check) to avoid planning a child-table DML that will be rejected.",
      "verify": "Add a failpoint-based test that pauses at child-table `WriteOnly/DeleteOnly` (e.g., during `DROP TABLE child`), then runs a parent-table DELETE/UPDATE that should cascade into the child and asserts it succeeds (and that explicit DML on the non-Public child still yields `Table doesn't exist`). Also cover the CREATE TABLE(FK) intermediate state similarly to ensure implicit cascades don’t fail with `ErrTableNotExists`.",
      "evidence": "Intent says implicit FK cascades should still modify non-Public tables: `pkg/ddl/create_table.go:214-216`. But planner now hard-rejects any non-Public table: `pkg/planner/core/preprocess.go:1848-1856` (returns `ErrTableNotExists` when `retTbl.Meta().State != StatePublic`). FK cascades are executed by generating internal DELETE/UPDATE and running `Preprocess` + `OptimizeForForeignKeyCascade`: `pkg/executor/foreign_key.go:787-791`, which builds plans via `PlanBuilder.buildDataSource` that always calls `tryLockMDLAndUpdateSchemaIfNecessary`: `pkg/planner/core/logical_plan_builder.go:4375`. Also, CREATE TABLE sets FK state public up-front (`pkg/ddl/create_table.go:90-91`), so CASCADE/SET NULL paths are chosen even while table state is `DeleteOnly` (`pkg/ddl/create_table.go:208-213`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master`: `pkg/planner/core/preprocess.go` still rejects non-Public tables in `tryLockMDLAndUpdateSchemaIfNecessary`, and `pkg/executor/foreign_key.go` still builds cascade plans via `Preprocess` + `OptimizeForForeignKeyCascade` (no visible bypass for FK triggers).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-55891.json",
      "worthy_scope": true,
      "summary": "This commit makes `WriteOnly/DeleteOnly` tables invisible to planner execution by returning `ErrTableNotExists`, changes CREATE TABLE(with FK) to use `DeleteOnly` as the intermediate state, and keeps dropped tables in infoschema until `StateNone` for FK-related metadata behavior; tests/mocks are updated accordingly."
    },
    {
      "pr": 56020,
      "subject": "resource_control: support set resource limit for background tasks (#56020)",
      "commit": "04608f499b41f5549ff6568222149cbe367b86fb",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/executor",
        "pkg/parser"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`BACKGROUND=NULL` / `BACKGROUND=()` is not honored: DDL code always re-initializes `ResourceGroupSettings.Background` to a non-nil empty struct even when the AST indicates “clear background settings”.",
      "impact": "`ALTER RESOURCE GROUP default BACKGROUND=NULL/()` cannot truly unset background settings; TiDB persists an empty `Background` object and will still send a (zero-valued) `BackgroundSettings` message to the resource manager. This can cause inconsistent metadata presentation (`information_schema` shows NULL while `SHOW CREATE RESOURCE GROUP` still includes `BACKGROUND=()`), and risks semantic differences if the resource manager distinguishes “unset” (nil) vs “present-but-empty” background settings.",
      "fix": "In `SetDirectResourceGroupSettings` (case `ast.ResourceGroupBackground`), make initialization conditional:\n- `if len(opt.BackgroundOptions)==0 { resourceGroupSettings.Background = nil; return nil }`\n- else `resourceGroupSettings.Background = &model.ResourceGroupBackgroundSettings{}` and apply options.\n(Alternative: restore the old lazy-init in `SetDirectResourceGroupBackgroundOption` and remove the unconditional init.)",
      "verify": "1) `ALTER RESOURCE GROUP default BACKGROUND=(TASK_TYPES='br,ddl', UTILIZATION_LIMIT=30)`; 2) `ALTER RESOURCE GROUP default BACKGROUND=NULL` (or `BACKGROUND=()`);\nExpected after fix: `SHOW CREATE RESOURCE GROUP default` omits `BACKGROUND` (or otherwise reflects it as unset), and the resource-manager `ResourceGroup` no longer carries `BackgroundSettings` when cleared.",
      "evidence": "`pkg/ddl/resource_group.go:238` sets `resourceGroupSettings.Background = nil` for `BACKGROUND=NULL` / `BACKGROUND=()` (i.e., `len(opt.BackgroundOptions)==0`), but `pkg/ddl/resource_group.go:241` immediately overwrites it with `resourceGroupSettings.Background = &model.ResourceGroupBackgroundSettings{}`; loop is empty so the pointer can never stay nil.\nFirst-parent behavior did not do this; it kept `Background=nil` for empty options and lazily allocated only when options exist: `git show 04608f499b41f5549ff6568222149cbe367b86fb^1:pkg/ddl/resource_group.go`.\nDownstream effects: `pkg/ddl/resourcegroup/group.go:71` treats non-nil `options.Background` as present and always sends `group.BackgroundSettings`; `SHOW CREATE RESOURCE GROUP` always prints `BACKGROUND=...` when `Background!=nil` via `pkg/executor/show.go:1613` -> `pkg/meta/model/resource_group.go:126`.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed on `upstream/master` as of `f0813a369666ac6314e76f5e573e982fe50c8a09`: `git show upstream/master:pkg/ddl/resource_group.go` still contains the same `len(opt.BackgroundOptions)==0` check followed by unconditional `resourceGroupSettings.Background = &model.ResourceGroupBackgroundSettings{}`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56020.json",
      "worthy_scope": true,
      "summary": "Adds `BACKGROUND ... UTILIZATION_LIMIT` plumbing across parser/DDL/meta/infoschema, but introduces a High-severity DDL semantics bug: `BACKGROUND=NULL/()` cannot clear background settings to nil because the code unconditionally allocates an empty background settings struct; this persists on `upstream/master`."
    },
    {
      "pr": 56130,
      "subject": "ddl: args v2 for add/drop/rename index, include PK/vector-index (#56130)",
      "commit": "89a55716191e9f3111eb04b6b68efff122bc899a",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "JobVersion1 finished add-index args lose the global-index flag: encoder uses `IndexArg.Global` instead of `IndexArg.IsGlobal`, so global indexes are treated as non-global in delete-range/GC paths.",
      "impact": "For JobVersion1 (current default), finished add-index jobs for global indexes will persist `isGlobal=false`, so `gc_delete_range` insertion treats global indexes as local (per-partition physical IDs). This can skip deleting temp/global-index key ranges (storage leak / incorrect GC range bookkeeping) and may cause prolonged space usage after add-index completion/cancel.",
      "fix": "In `pkg/meta/model/job_args.go` `(*ModifyIndexArgs).getFinishedArgsV1`, change `isGlobals[i] = arg.Global` to `isGlobals[i] = arg.IsGlobal` (and add a unit test with `IsGlobal=true`).",
      "verify": "- Unit: build a `Job{Version:JobVersion1, Type:ActionAddIndex}`; call `FillFinishedArgs(&ModifyIndexArgs{OpType:OpAddIndex, IndexArgs:[]*IndexArg{{IndexID:1, IsGlobal:true}}, PartitionIDs:[]int64{1}})`; encode/decode; `GetFinishedModifyIndexArgs` must return `IndexArgs[0].IsGlobal==true`.\n- Behavior: add a global index on a partitioned table and inspect `mysql.gc_delete_range` entries; they should reference `tableID` (not partition IDs) for that index’s temp ranges.",
      "evidence": "- Finished add-index args set globalness via `IsGlobal`: `pkg/ddl/index.go:1082` (`IsGlobal: indexInfo.Global`).\n- But v1 finished-args encoder records `isGlobals` from deprecated `Global`: `pkg/meta/model/job_args.go:1522` (`isGlobals[i] = arg.Global`).\n- Downstream delete-range logic relies on `IndexArg.IsGlobal`: `pkg/ddl/delete_range.go:363`.\n- First-parent behavior stored globalness explicitly: `f1034dc3d42b5015ae2fcb015c1a6da4f36afee8:pkg/ddl/index.go:1118` (`isGlobal = append(isGlobal, indexInfo.Global)`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `remotes/upstream/master@f0813a369666ac6314e76f5e573e982fe50c8a09`: still `pkg/meta/model/job_args.go:1649` uses `isGlobals[i] = arg.Global`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56130.json",
      "worthy_scope": true,
      "summary": "Found 2 High correctness issues in this commit’s DDL args refactor: (1) JobVersion1 finished add-index args drop the global-index flag due to `arg.Global` vs `arg.IsGlobal` mismatch, breaking delete-range/GC behavior for global indexes; (2) BDR unique-index denial checks only the first index arg, so merged multi-index add-index jobs can bypass the restriction. Neither is fixed in `remotes/upstream/master@f0813a3696`."
    },
    {
      "pr": 56194,
      "subject": "ddl: args v2 for lock/unlock/repair table and recover table/schema (#56194)",
      "commit": "75483d8f88c5d1435aef894444a6f35580d26474",
      "modules": [
        "pkg/ddl",
        "pkg/executor",
        "pkg/infoschema",
        "pkg/meta",
        "pkg/meta/model"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`JobVersion1` lock/unlock table DDL jobs are not backward-compatible across upgrade: this commit changes the persisted JSON field names for the lock/unlock args (CamelCase → snake_case tags), so older in-flight jobs may decode to empty args and then either never finish (lock) or finish without unlocking (unlock).",
      "impact": "Upgrading TiDB while an `ActionLockTable`/`ActionUnlockTable` job created by an older binary is still in the DDL queue can cause the job to be stuck (blocking subsequent DDL) or to “succeed” without actually unlocking, leaving metadata table locks behind and impacting availability.",
      "fix": "For `JobVersion1` lock/unlock jobs, keep backward-compatible decoding of the legacy `LockTablesArg` JSON shape (CamelCase keys) and convert into `model.LockTablesArgs` before execution; alternatively implement `UnmarshalJSON` on `model.LockTablesArgs` to accept both legacy CamelCase keys and current snake_case keys (and consider handling the `is_cleanup:omitempty` tag typo at the same time).",
      "verify": "Add a unit test that constructs a v1 job whose `RawArgs` is marshaled from the pre-change `LockTablesArg` (CamelCase keys) and assert `model.GetLockTablesArgs` returns the correct values; include a regression test that `onLockTables`/`onUnlockTables` makes progress and unlocks when fed such a job.",
      "evidence": "Legacy v1 encoding (first parent) persisted lock/unlock job args as `LockTablesArg` with CamelCase JSON keys via `Args: []any{arg}` (e.g. `75483d8f^:pkg/ddl/executor.go:5321-5332`, `75483d8f^:pkg/ddl/executor.go:5464-5472`).\nThis commit switches DDL worker decoding to `model.GetLockTablesArgs` (`pkg/ddl/table_lock.go:26-38`, `pkg/ddl/table_lock.go:226-238`), where v1 decoding unmarshals into `model.LockTablesArgs` that uses snake_case JSON tags (`pkg/meta/model/job_args.go:1201-1224`). Go `encoding/json` uses the tag name as the only match key and only case-folds (no camelCase↔snake_case normalization), so old persisted keys like `LockTables` will not populate `json:\"lock_tables\"` (see `$GOROOT/src/encoding/json/encode.go:typeFields` and `$GOROOT/src/encoding/json/decode.go` using `foldName`).\nWith zero-value decoded args, `onLockTables` may never reach `FinishTableJob` when `len(args.LockTables)==0` (`pkg/ddl/table_lock.go:59-101`), and `onUnlockTables` may finish immediately without unlocking anything when `len(args.UnlockTables)==0` (`pkg/ddl/table_lock.go:234-237`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master:pkg/meta/model/job_args.go:1072-1093`: `LockTablesArgs` still uses `json:\"lock_tables\"` (and `is_cleanup:omitempty`) and v1 decoding is still a plain `json.Unmarshal` via `job.decodeArgs(a)`, with no legacy-key compatibility handling.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56194.json",
      "worthy_scope": true,
      "summary": "Adds args-v2 helpers/types for lock/unlock/repair and recover table/schema jobs and wires DDL/executor/worker paths to use them; main remaining risk is a v1 on-disk job-args incompatibility for lock/unlock jobs due to new `LockTablesArgs` JSON tags breaking decoding of older persisted jobs."
    },
    {
      "pr": 56409,
      "subject": "*: support the vector index (#56409)",
      "commit": "6e8f27f9ce952cb402d2a4ecb9f5be6cd0aacf52",
      "modules": [
        "pkg/ddl",
        "pkg/infoschema",
        "pkg/meta/model",
        "pkg/domain/infosync",
        "pkg/store/helper",
        "pkg/parser"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Vector index DDL can wait indefinitely because TiFlash schema-sync and progress polling treat certain failures as success (non-2xx HTTP responses and empty `information_schema.tiflash_indexes` result sets).",
      "impact": "In misconfigured/partially upgraded clusters (e.g., TiFlash removed/unreachable, sync endpoint path mismatch, or TiFlash never reports the index row), `ALTER TABLE ... ADD VECTOR INDEX` can remain stuck in `StateWriteReorganization` indefinitely, blocking the DDL until manual cancellation and potentially causing repeated polling load.",
      "fix": "- Treat TiFlash schema sync as a real RPC: check `resp.StatusCode` (and optionally read error body) and return an error on non-2xx; consider fallback to the legacy endpoint if needed.\n- In polling, handle `len(rows)==0` as a retryable-but-bounded condition (e.g., retry with backoff for a limited time/attempts, then fail with a clear error) and/or add an upfront check for at least one live TiFlash store before entering reorg.\n- Optionally add `tidb_database`/`tidb_table` filters to the polling SQL to enable TiFlashSystemTableExtractor pushdown and reduce scan cost.",
      "verify": "1) Simulate TiFlash sync endpoint returning 404/500 and confirm DDL fails fast with a clear error instead of hanging.\n2) Set `TiFlashReplica.Count>0` while having zero TiFlash stores and confirm add vector index errors quickly.\n3) Force `information_schema.tiflash_indexes` to return empty for the target index and confirm bounded retries then error.",
      "evidence": "Schema sync ignores HTTP status: `pkg/store/helper/helper.go:912` does `InternalHTTPClient().Get(statURL)` then returns nil after `resp.Body.Close()` without checking `resp.StatusCode` or body. Sync can be a no-op when no TiFlash stores: `pkg/domain/infosync/info.go:1111` collects TiFlash stores and may pass an empty slice; `pkg/domain/infosync/tiflash_manager.go:175` returns nil when the slice is empty. Progress polling treats empty result as no error: `pkg/ddl/index.go:928` returns `errors.Trace(err)` when `len(rows)==0` (and `errors.Trace(nil)==nil`), so the job stays in `StateWriteReorganization` and keeps waiting. Preconditions don’t verify real TiFlash availability: `pkg/ddl/executor.go:4640` only checks `TiFlashReplica.Count > 0`.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed on `upstream/master` at `f0813a369666ac6314e76f5e573e982fe50c8a09`: `upstream/master:pkg/store/helper/helper.go` still closes the body without checking HTTP status; `upstream/master:pkg/ddl/index.go` (refactored as `checkColumnarIndexProcessOnce`) still returns `errors.Trace(err)` when `len(rows)==0`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56409.json",
      "worthy_scope": true,
      "summary": "Reviewed `6e8f27f9` vs first parent with focus on DDL/add-index/infoschema/meta. The commit adds VECTOR INDEX (HNSW) metadata (`IndexInfo.VectorInfo`), DDL job `ActionAddVectorIndex` that triggers TiFlash schema sync then polls `information_schema.tiflash_indexes`, and a new infoschema table `TIFLASH_INDEXES`. Found one High-severity correctness/state-machine risk: schema sync + polling can silently succeed/fail and lead to indefinite DDL waiting under certain TiFlash failure/misconfiguration modes."
    },
    {
      "pr": 56630,
      "subject": "ddl: add `lease not found` and `deadline exceed` to retryable errors (#56630)",
      "commit": "1352db677c89a43abc6bf963781811d60ff641ad",
      "modules": [
        "pkg/ddl",
        "pkg/owner",
        "pkg/util/dbterror",
        "pkg/disttask/framework/taskexecutor",
        "pkg/lightning/common",
        "tests/realtikvtest"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "Dist add-index disttask now retries nearly all non-`*terror.Error` errors (unknown errors default to retryable), which can keep subtasks in running state indefinitely (no retry budget) and leave DDL jobs stuck waiting for the global task on persistent infra/logic errors.",
      "impact": "In disttask add-index mode, persistent non-`*terror.Error` errors (e.g., TiKV gRPC Unknown non-retryable conditions, local IO/permission/disk errors, unexpected internal errors) can cause hot-loop retries + log spam and an add-index DDL job that never completes nor fails, requiring manual intervention/cancellation and potentially consuming cluster resources.",
      "fix": "Split retry policy for disttask: keep `common.IsRetryableError(err)` and add only targeted extensions (`dbterror.ReorgRetryableErrCodes` + `dbterror.ReorgRetryableErrMsgs`) but do NOT default to retry for unknown errors; or keep broad retry but add a bounded retry budget/backoff and fail the subtask/task after N attempts / elapsed time to avoid infinite running-state loops.",
      "verify": "In disttask mode, enable failpoint `github.com/pingcap/tidb/pkg/ddl/ingest/mockResetEngineFailed` with `return` (always) and confirm the global task/subtask transitions to failed (or stops after bounded retries) instead of retrying forever. Also inject/observe a TiKV DiskSpaceNotEnough gRPC error and confirm it is treated non-retryable (subtask failed, scheduler reverts task, DDL surfaces error).",
      "evidence": "Retry classifier defaults unknown (non-`*terror.Error`) errors to retryable: `pkg/ddl/index.go:1284`. Dist backfill executor uses it for subtask retries: `pkg/ddl/backfilling_dist_executor.go:224`. Framework behavior: retryable errors do not transition subtask to failed/canceled (`pkg/disttask/framework/taskexecutor/task_executor.go:616`), and executors re-check/run subtasks every 300ms (`pkg/disttask/framework/taskexecutor/manager.go:42`). This also overrides Lightning’s explicit non-retryable cases like DiskSpaceNotEnough (`pkg/lightning/common/retry.go:150`). The updated test switches `mockResetEngineFailed` to `1*return` and expects success (`tests/realtikvtest/addindextest3/ingest_test.go:542`), implying persistent failures would otherwise spin rather than fail fast.",
      "later_fixed": false,
      "later_fix_evidence": "Checked upstream/master at `f0813a369666ac6314e76f5e573e982fe50c8a09`: `pkg/ddl/backfilling_dist_executor.go` still uses `common.IsRetryableError(err) || isRetryableError(err)`, and `pkg/ddl/index.go` still returns retryable for non-`*terror.Error` errors (same semantics), so this behavior remains.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56630.json",
      "worthy_scope": true,
      "summary": "Adds message-based retry matching (`context deadline exceeded`, `requested lease not found`) and reuses a shared DDL retry helper across add-index + disttask backfill. Main risk: disttask backfill now treats all non-`*terror.Error` errors as retryable, overriding Lightning’s non-retryable classifications (e.g., DiskSpaceNotEnough) and enabling unbounded hot-loop retries that can leave add-index jobs stuck indefinitely; still present on upstream/master."
    },
    {
      "pr": 56702,
      "subject": "ddl: support dropping multiple foreign keys (#56702)",
      "commit": "e6459b33669fcc677b561ba8477ed02191f72d11",
      "modules": [
        "pkg/ddl",
        "tests/integrationtest/ddl"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Multi-schema `DROP FOREIGN KEY` subjobs are not rollback-safe: a later subjob failure (e.g., duplicate `DROP FOREIGN KEY` on the same name, or mixed DDL where a later operation fails) can leave earlier FK drops applied even though the overall `ALTER TABLE` fails.",
      "impact": "A multi-clause `ALTER TABLE` can return an error while still having permanently removed one or more foreign keys, causing non-atomic schema changes and unexpected loss of FK metadata/enforcement. This is especially risky for statements that logically “replace” a FK (drop old + add new) where the add can fail at execution time (data validation), leaving the old FK already dropped.",
      "fix": "- Add multi-schema precheck for duplicate `DROP FOREIGN KEY` names (similar to `checkOperateSameColAndIdx` for columns/indexes) so the statement fails before submitting/executing any subjob.\n- Consider aligning `DROP FOREIGN KEY` with other multi-schema-safe DDLs (e.g. `onDropColumn`/`onRenameIndex`) by marking the proxy job non-revertible when `job.MultiSchemaInfo.Revertible` and deferring the actual drop into the non-revertible batch stage where table-info snapshot restore is available.",
      "verify": "- Add an integration test under `tests/integrationtest/t/ddl/multi_schema_change.test`:\n  - Create a table with `fk1`.\n  - Run `-- error 1091` then `alter table ... drop foreign key fk1, drop foreign key fk1;`.\n  - `show create table` should still contain `fk1` (no partial drop).\n- Add a second test for mixed multi-schema: `drop foreign key old, add constraint new ...` where the add fails (e.g., by inserting violating rows first); verify the old FK remains after the error.",
      "evidence": "- `pkg/ddl/multi_schema_change.go:onMultiSchemaChange` rollback path skips any sub-job where `sub.IsFinished()`.\n- `pkg/meta/model/job.go:SubJob.IsFinished` returns true for `JobStateDone`, so a successfully executed `DROP FOREIGN KEY` subjob won’t be revisited during rollback.\n- `pkg/ddl/foreign_key.go:dropForeignKey` has no “undo” behavior; even when `job.IsRollingback()` it still *removes* the FK and just finishes with `JobStateRollbackDone`.\n- This commit enables `DROP FOREIGN KEY` as a multi-schema subjob (`pkg/ddl/multi_schema_change.go:fillMultiSchemaInfo` adds `case model.ActionDropForeignKey`) but does not record dropped FK names for duplicate detection, and `pkg/ddl/executor.go:DropForeignKey` only checks existence against the *current* table meta (not against already-appended subjobs).\n- Concrete failure mode: `ALTER TABLE t DROP FOREIGN KEY fk1, DROP FOREIGN KEY fk1` passes executor existence checks, first subjob drops `fk1`, second subjob cancels with `infoschema.ErrForeignKeyNotExists`, and rollback does not restore the already-done first drop.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master@f0813a369666ac6314e76f5e573e982fe50c8a09`: `pkg/ddl/executor.go:DropForeignKey` and `pkg/ddl/multi_schema_change.go:fillMultiSchemaInfo` still match this commit’s behavior (no duplicate-drop guard for FKs), and multi-schema rollback logic still skips finished subjobs via `sub.IsFinished()`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56702.json",
      "worthy_scope": true,
      "summary": "Adds support for `ALTER TABLE ... DROP FOREIGN KEY fk1, DROP FOREIGN KEY fk2` by allowing `ActionDropForeignKey` in multi-schema changes and adding an executor-time existence check for the FK name; includes an integration test for the multi-drop and missing-FK error case."
    },
    {
      "pr": 56864,
      "subject": "ddl: enable the ddl notifier by default (#56864)",
      "commit": "900cd39d5391a6d57e4ec45f1aa5038c17c561eb",
      "modules": [
        "pkg/ddl",
        "pkg/domain",
        "pkg/meta",
        "pkg/session"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Potential nil-store panic introduced by enabling notifier publish unconditionally: `asyncNotifyEvent` now always calls `notifier.PubSchemeChangeToStore` using `jobCtx.eventPublishStore`, but `EventPublishStore` remains optional in `ddl.NewDDL` options and the only non-nil check is `intest.Assert` (no-op in release builds).",
      "impact": "Any code path that constructs a DDL instance without `WithEventPublishStore` can hit a nil-interface method call during DDL job execution once `asyncNotifyEvent` is reached, causing a panic/DDL job abort and potentially stalling DDL progress on that owner. This is now a hard dependency for DDL correctness/availability rather than a best-effort notification path.",
      "fix": "Make `EventPublishStore` non-optional at construction (panic/return error early in `newDDL` if nil), or provide a safe default (e.g. open `mysql.tidb_ddl_notifier` table store) and/or add a runtime guard in `asyncNotifyEvent` to return a clear error when `jobCtx.eventPublishStore == nil` (avoid nil-interface panics).",
      "verify": "Construct a DDL via `ddl.NewDDL(...)` without `WithEventPublishStore` and execute a DDL that reaches `asyncNotifyEvent` (e.g. create table / add index) to ensure it does not panic and either (a) publishes successfully via a default store or (b) returns a clear error. Also verify normal `BootstrapSession` + `Domain.Init` path still creates/publishes to `mysql.tidb_ddl_notifier`.",
      "evidence": "`asyncNotifyEvent` unconditionally publishes to the notifier store (`pkg/ddl/ddl.go:590`, `pkg/ddl/ddl.go:597`) and only guards with `intest.Assert`, which is compiled as a no-op in non-intest/non-enableassert builds (`pkg/util/intest/no_assert.go:15`, `pkg/util/intest/no_assert.go:24`). `EventPublishStore` is optional in DDL options (`pkg/ddl/options.go:38`) and is passed through without a default (`pkg/ddl/ddl.go:675`). There are in-repo DDL instantiations that omit `WithEventPublishStore` (e.g. `pkg/ddl/restart_test.go:46`), while common DDL flows trigger `asyncNotifyEvent` (e.g. create table at `pkg/ddl/create_table.go:186`, add index at `pkg/ddl/index.go:1100`).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master`: `asyncNotifyEvent` still publishes unconditionally after `intest.Assert` (`upstream/master:pkg/ddl/ddl.go:717`) and at least one DDL constructor in tests still omits `WithEventPublishStore` (`upstream/master:pkg/ddl/restart_test.go:46`).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56864.json",
      "worthy_scope": true,
      "summary": "This commit enables DDL notifier by default: adds `meta.DDLNotifierTableVersion` and bootstraps `mysql.tidb_ddl_notifier`, always constructs `DDLNotifier` in `Domain.Init` and wires it to stats owner, and changes DDL job completion notifications to always publish `SchemaChangeEvent` rows to the notifier store."
    },
    {
      "pr": 56963,
      "subject": "ddl: mitigate the issue that multiple owner might exist during force to be owner (#56963)",
      "commit": "6679c7449d8edc45a635ce950f077d6ea8b58843",
      "modules": [
        "pkg/ddl",
        "pkg/owner",
        "pkg/session",
        "pkg/domain"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`ForceToBeOwner` swallows errors after retries (always returns nil), so upgrade-mode startup can proceed even when “force to be owner” did not succeed.",
      "impact": "In upgrade startMode, TiDB can continue bootstrapping/upgrading even if the “force to be owner” operation repeatedly fails (etcd errors, txn op limits, campaign timeout due to concurrent keys). That undermines the stated invariant that upgrade-related DDLs run on the new-version node, and can lead to: (1) upgrade DDL jobs being executed by an older-version DDL owner, (2) upgrade hanging due to no stable owner, or (3) larger multi-owner windows during election churn—risking metadata/state-machine inconsistencies.",
      "fix": "Propagate failure when retries are exhausted, e.g. after the retry loop `if err != nil { return errors.Trace(err) }`. Consider also using the passed-in ctx (instead of ignoring it) so callers can bound force-owner time, and (optionally) add a post-condition check (e.g. verify this session’s campaign key is present / verify elected owner ID) before returning success.",
      "verify": "Add a focused unit/integration test that forces `tryToBeOwnerOnce` to fail (e.g. inject etcd txn failure / campaign timeout) and assert `ForceToBeOwner` returns non-nil and `ddl.Start(Upgrade, ...)` fails fast; also validate a rolling-upgrade scenario where other nodes campaign concurrently and ensure upgrade does not proceed unless forcing owner succeeds.",
      "evidence": "`pkg/owner/manager.go:184` creates a force-owner session and retries `tryToBeOwnerOnce`; `pkg/owner/manager.go:208` retries but `pkg/owner/manager.go:218` always returns nil, discarding the last error. `pkg/owner/manager.go:225`, `pkg/owner/manager.go:250`, `pkg/owner/manager.go:261` show `tryToBeOwnerOnce` can fail on etcd Get/Txn/Campaign(timeout). `pkg/ddl/ddl.go:766` enters upgrade mode and `pkg/ddl/ddl.go:772` expects `ForceToBeOwner` to return an error to abort. `pkg/session/session.go:3719` starts domain with upgrade startMode and `pkg/session/session.go:3736` proceeds to `upgrade(s)` after a fixed sleep regardless of whether forcing owner actually succeeded.",
      "later_fixed": false,
      "later_fix_evidence": "Checked later history up to `upstream/master` at `f0813a369666ac6314e76f5e573e982fe50c8a09`: `pkg/owner/manager.go:179` still returns nil unconditionally after the retry loop (`pkg/owner/manager.go:209`).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-56963.json",
      "worthy_scope": true,
      "summary": "Reviewed commit vs first parent focusing on DDL ownership/upgrade bootstrap flow. Found 1 High-risk correctness/concurrency issue: `owner.Manager.ForceToBeOwner` discards retry failures, so upgrade-mode DDL startup can proceed without actually forcing ownership; this is still present on `upstream/master`."
    },
    {
      "pr": 57017,
      "subject": "*: let TempIndex support encode/decode partitionID flag (#57017)",
      "commit": "5da9d1a1ca1e8903f63cc9271e3d4507bc1fa0ac",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/ingest",
        "pkg/kv",
        "pkg/table/tables",
        "pkg/tablecodec"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "TempIndexValue encoding change (global unique index delete marker adds 'p'+partitionID bytes) is not forward-compatible: pre-#57017 TiDB cannot decode new-format temp-index values, risking rolling upgrade/downgrade failures when temp-index is active.",
      "impact": "Mixed-version TiDB clusters (rolling upgrade/downgrade) with an in-flight ADD GLOBAL UNIQUE INDEX using temp-index can break when upgraded nodes write the new-format temp-index delete markers and older nodes attempt to read them:\n- DDL merge-temp-index step can abort on decode error, potentially stalling/failing the DDL job.\n- DML that touches the temp index (unique checks / conflict handling) can error.\nThis is a compatibility hazard for online upgrades if DDL is running/allowed during the upgrade window.",
      "fix": "Gate emission of the new temp-index element encoding until the cluster is guaranteed homogeneous (e.g., via cluster/version capability in PD/meta, or by only enabling this format after a controlled version bump), otherwise continue emitting the old format during mixed-version windows. If gating is not feasible, document/guard that global-index temp-index merge (and DML that writes these temp values) must not run during rolling upgrade/downgrade.",
      "verify": "Repro in mixed versions: start `ALTER TABLE ... ADD UNIQUE INDEX ... GLOBAL` (ingest/temp-index path) on a partitioned table, upgrade only one TiDB node to the new version, keep another node on the pre-#57017 version (preferably DDL owner), generate concurrent UPDATE/DELETE to produce temp-index delete markers, and observe old node failing to decode temp-index values (DDL merge fails or DML errors). Also validate that after full upgrade (all nodes new), the scenario passes and merge completes.",
      "evidence": "New format is introduced in `pkg/tablecodec/tablecodec.go`:\n- `TempIndexValueElem.Encode` (deleted+distinct) now conditionally appends `TempIndexKeyTypePartitionIDFlag` ('p') + `codec.EncodeInt(partitionID)` when `TempIndexValueElem.Global` is true.\n- `TempIndexValueElem.DecodeOne` (TempIndexValueFlagDeleted) now checks `b[0] == TempIndexKeyTypePartitionIDFlag` and consumes 9 bytes before reading `KeyVer`, wrapping the handle via `kv.NewPartitionHandle(pid, v.Handle)`.\nThe writer is enabled by DML in `pkg/table/tables/index.go` `(*index).Delete`: when `c.idxInfo.Global` it sets `tempValElem.Global = true` and `tempValElem.Handle = kv.NewPartitionHandle(c.phyTblID, h)`, so upgraded nodes will emit the new bytes.\nForward-compat break: the first-parent decoder (`5da9d1a^1:pkg/tablecodec/tablecodec.go`) expects `[handle][key_version]` immediately; it has no branch for 'p'. If it reads a new-format element, it will treat 'p' as `KeyVer` and the remaining 8 bytes as the next element's flag stream, leading to decode failure (`invalid temp index value`) in `DecodeTempIndexValue`.\nReaders that hard-fail on decode include `pkg/ddl/index_merge_tmp.go` (merge step calls `tablecodec.DecodeTempIndexValue(rawValue)` and returns the error) and DML paths like `pkg/table/tables/index.go` / `fetchDuplicatedHandleForTempIndexKey` (decode error bubbles up).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master`: `pkg/tablecodec/tablecodec.go` on `upstream/master` still contains the same `TempIndexKeyTypePartitionIDFlag` encode/decode branch for temp-index deleted+distinct elements.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-57017.json",
      "worthy_scope": true,
      "summary": "Change correctly carries partition ID through temp-index delete markers for global unique indexes (via `TempIndexValueElem.Global` + `PartitionHandle`) and updates merge logic to interpret it. Main remaining risk is compatibility: the new temp-index value element format is not decodable by pre-#57017 nodes, which can break mixed-version rolling upgrade/downgrade if add-index temp-index is active."
    },
    {
      "pr": 57336,
      "subject": "ddl: support modify the related reorg config by SQL (#57336)",
      "commit": "afacd100e89f94341f0faa7bca2f4ec6d24ee41e",
      "modules": [
        "pkg/executor/operate_ddl_jobs.go",
        "pkg/meta/model/job.go",
        "pkg/planner/core/planbuilder.go",
        "pkg/planner/core/common_plans.go",
        "pkg/ddl/ddl.go"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`ADMIN ALTER DDL JOBS` incorrectly mutates `Job.AdminOperator`, which is also used as the pause/resume ownership guard (system vs end user). This breaks the DDL job pause/resume state-machine invariant, especially for system-paused jobs during upgrading.",
      "impact": "A SUPER user can run `ADMIN ALTER DDL JOBS ...` on a system-paused job (e.g., paused during cluster upgrade), which flips `AdminOperator` to end-user and:\n- lets end users resume jobs that were intentionally paused by system safeguards, and/or\n- prevents `Resume(All)JobsBySystem` from resuming those jobs later (job may stay stuck paused after upgrade).",
      "fix": "Do not overwrite `job.AdminOperator` in `ADMIN ALTER DDL JOBS` (leave pause/resume/cancel ownership intact). If audit is needed, add a separate field for “last altered by” instead of reusing `AdminOperator`.",
      "verify": "Regression test: create a job with `State=Paused` and `AdminOperator=AdminCommandBySystem`, run `ADMIN ALTER DDL JOBS ... thread/batch_size ...`, assert `AdminOperator` remains `AdminCommandBySystem`; then assert end-user resume still fails while `ResumeJobsBySystem/ResumeAllJobsBySystem` succeeds.",
      "evidence": "`pkg/executor/operate_ddl_jobs.go`: `AlterDDLJobExec.updateReorgMeta()` sets `job.AdminOperator = byWho` when altering `thread`/`batch_size`.\n`pkg/ddl/ddl.go`: `resumePausedJob()` enforces “paused job should only be resumed by who paused it” via `if job.AdminOperator != byWho { ... }`.\n`pkg/ddl/job_submitter.go`: during upgrading, user DDL jobs are paused by system via `pauseRunningJob(job, model.AdminCommandBySystem)`.\n`pkg/session/sync_upgrade.go`: `SyncNormalRunning()` calls `ddl.ResumeAllJobsBySystem()`, which relies on `AdminCommandBySystem` to resume system-paused jobs.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed as of `upstream/master` (e.g., `pkg/executor/operate_ddl_jobs.go` still assigns `job.AdminOperator = byWho` in `updateReorgMeta`).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-57336.json",
      "worthy_scope": true,
      "summary": "Adds `ADMIN ALTER DDL JOBS` (planner+executor) to update per-job reorg `thread`/`batch_size` in `mysql.tidb_ddl_job.job_meta`, plus `Job.IsAlterable()`. Found one High issue: the alter path overwrites `Job.AdminOperator`, which is also used to enforce who may resume a paused job (system vs end user), breaking upgrade-related pause/resume invariants."
    },
    {
      "pr": 57392,
      "subject": "ddl: add COMMENTS column to DDL jobs and enhance job reorg meta handling (#57392)",
      "commit": "5e6b8183c7245e1e9aa14dfb80e1c6dd2d39c46e",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/executor",
        "pkg/planner/core"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`ADMIN SHOW DDL JOBS` `COMMENTS` column is mis-associated with rows for `ActionMultiSchemaChange` jobs due to column-12 append order, causing comments to shift between the parent job row and subjob rows.",
      "impact": "`ADMIN SHOW DDL JOBS` output for multi-schema-change jobs shows incorrect per-row `COMMENTS` (parent row can show the first subjob’s comment; subjob rows can show the next subjob/parent comment). This can mislead operators about whether a specific subjob is running in ingest/DXF/cloud mode and about reorg parameters, harming diagnosability/operational correctness.",
      "fix": "In `appendJobToChunk`, append column 12 for the *parent* job row immediately after appending column 11 (STATE), before emitting any subjob rows. Then append column 12 for each subjob row inside the loop. (Or refactor to append all columns for a single row before starting the next row.)",
      "verify": "Add a unit test that builds a `model.Job{Type: ActionMultiSchemaChange}` with >=2 subjobs whose `showCommentsFromSubjob` values differ from `showCommentsFromJob`, call `appendJobToChunk` with `inShowStmt=true`, and assert `COMMENTS` (col 12) matches the expected row-by-row mapping. Also add an integration case like `ALTER TABLE ... ADD COLUMN, ADD INDEX` and validate `ADMIN SHOW DDL JOBS` prints comments on the correct subjob row.",
      "evidence": "`pkg/executor/show_ddl_jobs.go` `DDLJobRetriever.appendJobToChunk` appends the parent job row columns 0-11 first, then for `ActionMultiSchemaChange` appends column 12 for each subjob inside the loop (lines ~280-284), and only after the loop appends column 12 for the parent job (lines ~287-291). TiDB `chunk.Chunk` appends are positional per column, and `pkg/util/chunk/chunk.go` `appendSel` explicitly uses column 0 as the row-count standard (lines ~622-625). This means column 12 values become shifted relative to columns 0-11 for multi-schema-change output, so COMMENTS attaches to the wrong rows.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master:pkg/executor/show_ddl_jobs.go`; `appendJobToChunk` still appends subjob column 12 before the parent column 12 for `ActionMultiSchemaChange`, so the row-shift issue persists on upstream/master.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-57392.json",
      "worthy_scope": true,
      "summary": "Reviewed diff vs first parent focusing on DDL/add-index/infoschema/meta. Found a High-severity metadata correctness bug: for multi-schema-change jobs, `ADMIN SHOW DDL JOBS` `COMMENTS` is appended out-of-order and becomes shifted to the wrong rows; issue still present on `upstream/master`."
    },
    {
      "pr": 57468,
      "subject": "ddl: dynamically adjusting the concurrency and batch size of reorganization job (#57468)",
      "commit": "2f8d1f6f68f53beea9b81ba78671c9282f95c93b",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/disttask/operator",
        "pkg/executor"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Shrinking txn-backfill concurrency can cancel workers in a way that drops in-flight task results (including errors/panics), so reorg/add-index may continue and complete even though some backfill tasks failed.",
      "impact": "A backfill error/panic on a worker canceled during a downscale can be silently ignored, allowing the DDL reorg/add-index to continue and potentially finish with missing/incorrect index data; panics can also be lost because the deferred `ErrReorgPanic` result uses `sendResult`.",
      "fix": "Make downscaling graceful: avoid canceling `backfillWorker.ctx` for workers that may be processing a task. Prefer self-throttling (semaphore/permit) or have workers stop only before taking the next task (after successfully sending the current task’s result). If cancellation is kept, ensure the current task’s result/error is still delivered (or the task is re-queued) before the worker exits.",
      "verify": "Run ADD INDEX with failpoints (e.g. `mockBackfillRunErr` / a real duplicate-key error) while concurrently issuing `ADMIN ALTER DDL JOBS <job_id> THREAD=<smaller>` to trigger downscale; assert the job fails and the error is propagated/recorded (not silently succeeding). Then validate index completeness/consistency post-job.",
      "evidence": "Downscaling cancels workers (`pkg/ddl/backfilling_scheduler.go:325` -> `pkg/ddl/backfilling.go:301` -> `pkg/ddl/backfilling.go:294`). A canceled worker drops its result because `sendResult` prefers `<-w.ctx.Done()` (`pkg/ddl/backfilling.go:382`). Worker errors are only surfaced by the result-collector returning `result.err` (`pkg/ddl/backfilling.go:963`), so a dropped result means the error is never observed. This PR adds a periodic adjust loop that calls `scheduler.adjustWorkerSize()` at runtime (`pkg/ddl/backfilling.go:1030`), making mid-backfill downscales realistic.",
      "later_fixed": false,
      "later_fix_evidence": "",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-57468.json",
      "worthy_scope": true,
      "summary": "Implements runtime tuning of reorg concurrency/batch size for txn backfill and local ingest; found a High-risk error-propagation hole when downscaling txn backfill workers due to result-dropping on worker cancellation."
    },
    {
      "pr": 57766,
      "subject": "*: support cancel query like 'select * from information_schema.tables' (#57766)",
      "commit": "ce5ec3fa62ed62ecc459f18a0a1ca2f60135cb82",
      "modules": [
        "pkg/ddl",
        "pkg/domain",
        "pkg/executor",
        "pkg/infoschema",
        "pkg/meta",
        "pkg/planner/core"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`listTablesForEachSchema` incorrectly returns `errors.Trace(err)` (nil) when `ctx.Err()!=nil`, swallowing cancellation and allowing information_schema scans to “succeed” under cancellation.",
      "impact": "Cancelled `INFORMATION_SCHEMA.TABLES` (and other extractors that fall back to `listTablesForEachSchema`, e.g. `PARTITIONS`) can terminate early with *success* (often an empty/partial result set) instead of returning an interrupt error. This undermines the PR’s goal (“support cancel query…”) and can mislead clients into treating a killed query as a valid empty result.",
      "fix": "Change the cancellation branch to return the context error (e.g. `return nil, nil, errors.Trace(ctx.Err())`; consider `errors.Trace(context.Cause(ctx))` if you want to preserve cancel-cause contexts).",
      "verify": "Unit: cancel a `context.Context` and call `InfoSchemaTablesExtractor.ListSchemasAndTables` on an `*infoschema.infoSchema`, expect a non-nil error. Integration: run `select * from information_schema.tables` and `KILL QUERY`, expect an interrupt error (not OK/empty result).",
      "evidence": "In `pkg/planner/core/memtable_infoschema_extractor.go:706`+: `tables, err := is.SchemaTableInfos(ctx, s)` then `if ctx.Err() != nil { return nil, nil, errors.Trace(err) }`—but `err` is nil on this path, so the function returns a nil error. Second confirmation: `pkg/infoschema/infoschema.go:346` (`(*infoSchema).SchemaTableInfos`) ignores `ctx` and returns `tables, nil`, making the post-call `ctx.Err()` check the only cancellation signal here.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed on `upstream/master` (f0813a369666ac6314e76f5e573e982fe50c8a09): `listTablesForEachSchema` still has `if ctx.Err() != nil { return nil, nil, errors.Trace(err) }`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-57766.json",
      "worthy_scope": true,
      "summary": "The commit threads `context.Context` into `meta.ListTables` and adds cancellation checks to make INFORMATION_SCHEMA scans cancelable, but a planner-side cancellation check returns the wrong error and can swallow cancellations, causing killed INFORMATION_SCHEMA queries to return success (often empty/partial) instead of an interrupt error."
    },
    {
      "pr": 57813,
      "subject": "ddl: check context done in isReorgRunnable function (#57813)",
      "commit": "575310677da18511b9b98c72b48443b8471ade9b",
      "modules": [
        "pkg/ddl"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`backfillWorker.handleBackfillTask` checks reorg runnability using `d.ctx` (ddl/owner lifetime context) rather than the per-job step context that is canceled on job pause/cancel. Since backfill txn operations use `context.Background()`, this prevents timely interruption of long-running txn backfill loops on `ADMIN CANCEL/PAUSE`, and can delay scheduler/worker shutdown that waits on backfill workers.",
      "impact": "DDL add-index (txn backfill path) may ignore or significantly delay `ADMIN CANCEL DDL JOBS` / pause requests, continuing backfill work until the current region-range completes. This can prolong cluster load and can delay DDL job step exit (and `scheduler.close(true)`/worker shutdown) under cancellation scenarios.",
      "fix": "Pass a *job-step/scheduler* context (canceled on job pause/cancel) into `handleBackfillTask`’s runnable check instead of `d.ctx`.\n- Minimal: store the parent ctx passed to `newBackfillWorker(b.ctx, ...)` in the worker struct (e.g. `jobCtx context.Context`), and call `d.isReorgRunnable(w.jobCtx, false)`.\n- Keep `w.ctx` solely for worker lifecycle/scale-down cancellation so shrinking worker count doesn’t turn into a job-level error (`context.Canceled`).",
      "verify": "- Run a large add-index job using txn backfill (non-DXF/dist), then issue `ADMIN CANCEL DDL JOBS <job_id>` / `ADMIN PAUSE DDL JOBS <job_id>` while backfilling; confirm the job transitions promptly and backfill workers stop without waiting for an entire region-range.\n- Change `job.ReorgMeta` concurrency downward at runtime (triggering `adjustWorkerSize`); confirm shrinking workers doesn’t fail the job (no propagated `context.Canceled`).",
      "evidence": "- `pkg/ddl/backfilling.go:330` calls `d.isReorgRunnable(d.ctx, false)` inside the long-running per-batch loop of `backfillWorker.handleBackfillTask`.\n- `pkg/ddl/reorg.go:554` + `pkg/ddl/reorg.go:560` show `isReorgRunnable` only treats a job as non-runnable when the *passed-in* `ctx` is done (`return context.Cause(ctx)`); `d.ctx` is the ddl/owner lifetime context, not per-job.\n- Job pause/cancel is signaled via the *job step* context: `pkg/ddl/job_worker.go:827` creates `jobCtx.stepCtx` with `context.WithCancelCause`, and `pkg/ddl/job_worker.go:856` / `pkg/ddl/job_worker.go:862` cancel it with `ErrCancelledDDLJob` / `ErrPausedDDLJob...`.\n- Backfill txn work itself is not context-cancellable: e.g. `pkg/ddl/index.go:2284` starts the txn using `context.Background()` and `pkg/ddl/index.go:2320` uses `txn.LockKeys(context.Background(), ...)`, so the only timely stop point is the pre-batch runnable check.\n- Shutdown path can block waiting for workers: `pkg/ddl/backfilling_scheduler.go:341` cancels worker ctx in `closeBackfillWorkers`, then `pkg/ddl/backfilling_scheduler.go:343` waits for `b.wg.Wait()`; but `pkg/ddl/backfilling.go:403` only observes `w.ctx.Done()` between tasks, so if `handleBackfillTask` never sees job-step cancellation, close can be delayed until the current region-range finishes.\n- Note: worker ctx (`w.ctx`) cannot be blindly used as the job-runnable ctx because it is also canceled during dynamic worker-count decrease (`pkg/ddl/backfilling_scheduler.go:329`), which would otherwise surface as `context.Canceled` and fail the job.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `remotes/upstream/master`: `git show remotes/upstream/master:pkg/ddl/backfilling.go` still contains `err := d.isReorgRunnable(d.ctx, false)` (line 360 in that file snapshot), and add-index txn backfill still uses `context.Background()` for kv txn setup (e.g. `remotes/upstream/master:pkg/ddl/index.go` around the `addIndexTxnWorker.BackfillData` implementation).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-57813.json",
      "worthy_scope": true,
      "summary": "Found a High-severity cancellation/pause responsiveness issue in txn backfill: `handleBackfillTask` passes `d.ctx` into `isReorgRunnable`, so job-step cancel/pause signals are ignored during long-running per-region backfill loops; this can delay `ADMIN CANCEL/PAUSE` and block worker shutdown waiting for backfill completion."
    },
    {
      "pr": 57945,
      "subject": "ddl: handle context done after sending DDL jobs (#57945)",
      "commit": "6c39a81548c2dcae49790ea0093f565da1c53902",
      "modules": [
        "pkg/ddl/ddl.go",
        "pkg/ddl/executor.go",
        "pkg/ddl/job_submitter.go",
        "pkg/ddl/job_submitter_test.go"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Possible `ddl.Stop()` hang: `DoDDLJobWrapper` can return on DDL context cancel before receiving the job submission result, but `JobSubmitter.NotifyResult` uses blocking sends on unbuffered `ResultCh`; if cancellation happens while the submitter is still going to `NotifyResult`, it can block forever (no receiver), preventing `d.wg.Wait()` in `ddl.close()` from completing.",
      "impact": "Potential deadlock/hang during shutdown/`dom.DDL().Stop()` if DDL context is canceled while a job is mid-submission: `DoDDLJobWrapper` returns `context canceled`, but `JobSubmitter` can block forever sending the submit result, preventing `d.wg.Wait()` from completing. This can stall metadata subsystem shutdown and, in server shutdown paths, stall process exit.",
      "fix": "Make job submit notification non-blocking for the submitter when the receiver may exit: simplest is to make `ResultCh` buffered size 1 (e.g. `make(chan jobSubmitResult, 1)`) in `NewJobWrapper*` so `NotifyResult` can’t block; alternatively, change `NotifyResult` to `select { case ch <- res: default: }` with careful guarantees, or ensure `DoDDLJobWrapper` drains the channel on the `ctx.Done()` return path (e.g. background drain) to unblock the sender.",
      "verify": "Add a regression test using existing failpoint `afterGetJobFromLimitCh` (`pkg/ddl/job_submitter.go:87`) to pause `submitLoop` after dequeue but before `NotifyResult`, then trigger `dom.DDL().Stop()` (cancel ctx) while a session is in `DoDDLJobWrapper` waiting for the submit result; assert `Stop()` returns and the test doesn’t hang (run under `-race` in CI).",
      "evidence": "`pkg/ddl/executor.go:6590-6598` returns immediately on `e.ctx.Done()` while waiting for `jobW.ResultCh[0]`, so the receiver can disappear before a submission result is delivered.\n`pkg/ddl/job_submitter.go:101-126` always calls `jobW.NotifyResult(err)` after `addBatchDDLJobs2Table`, regardless of `s.ctx.Done()`.\n`pkg/ddl/ddl.go:226-267` creates unbuffered `ResultCh` and `NotifyResult` does a blocking send (`resultCh <- jobSubmitResult{...}`), so if the receiver returned, the submitter goroutine blocks forever.\n`pkg/ddl/ddl.go:1006-1010` cancels then waits on `d.wg.Wait()`, and `pkg/ddl/ddl.go:789-791` shows `submitLoop()` is in that waitgroup: a blocked `NotifyResult` can hang `ddl.Stop()`/`ddl.close()`.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` (ref `f0813a369666ac6314e76f5e573e982fe50c8a09`): `ResultCh` is still unbuffered in `pkg/ddl/ddl.go` and `DoDDLJobWrapper` still returns on `e.ctx.Done()` before receiving `ResultCh`; no mitigation found.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-57945.json",
      "worthy_scope": true,
      "summary": "Adds `e.ctx.Done()` handling when waiting for job submission results, plus a failpoint and test for submitting after DDL close. The new early-return path can leave `JobSubmitter.NotifyResult` blocked on unbuffered `ResultCh`, potentially hanging `ddl.Stop()` if cancellation races with in-flight job submission."
    },
    {
      "pr": 58062,
      "subject": "domain,infoschema: make infoschema activity block GC safepoint advancing (#58062)",
      "commit": "0ffac36ec16b0e12a4e3360910bf2da39d1fe523",
      "modules": [
        "pkg/infoschema",
        "pkg/domain/infosync",
        "pkg/domain"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`InfoCache.GetAndResetRecentInfoSchemaTS()` uses a non-atomic `Load()+Store()` reset, so concurrent `infoschemaV2.keepAlive()` updates can be lost, undermining the GC-safepoint pinning this change introduces.",
      "impact": "Can intermittently drop the smallest infoschema snapshot TS used near `ReportMinStartTS()` boundaries, causing TiDB to report a newer `minStartTS` than required; GC safepoint may advance past the actually-used infoschema TS, reintroducing \"GC life time is shorter than transaction duration\" failures for infoschema-v2 meta reads (the bug this PR is trying to mitigate).",
      "fix": "Change `pkg/infoschema/cache.go` `GetAndResetRecentInfoSchemaTS` to an atomic swap: `return h.Data.recentMinTS.Swap(now)`.",
      "verify": "Add a small concurrency unit test that repeatedly runs `GetAndResetRecentInfoSchemaTS()` in one goroutine while another calls `keepAlive()` with a smaller `is.ts`, asserting the reported value eventually reaches the smaller TS; run with `-race` to stress interleavings.",
      "evidence": "- New tracking path: `pkg/infoschema/infoschema_v2.go` `(*infoschemaV2).keepAlive()` updates `Data.recentMinTS` via CAS, and is called from `TableByID`, `TableByName`, `SchemaTableInfos`.\n- Reporting path: `pkg/domain/infosync/info.go` `(*InfoSyncer).ReportMinStartTS()` calls `schemaTS := is.infoCache.GetAndResetRecentInfoSchemaTS(currentVer.Ver)` and can lower `minStartTS`.\n- Reset implementation: `pkg/infoschema/cache.go`:\n  `ret := h.Data.recentMinTS.Load(); h.Data.recentMinTS.Store(now); return ret`\n  This is not atomic as a pair, so a concurrent `keepAlive()` CAS can be overwritten between the Load and Store and never be reflected in either the returned `ret` (this report) or the stored `now` (next round).\n- Concurrency is real: `pkg/domain/domain.go` `infoSyncerKeeper()` calls `ReportMinStartTS` on a ticker while user goroutines execute infoschema APIs.\n- Second confirmation: `go doc sync/atomic.Uint64` shows `Swap(new uint64) (old uint64)` exists, which would make the reset atomic and avoid losing concurrent updates.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` (f0813a369666ac6314e76f5e573e982fe50c8a09): `pkg/infoschema/cache.go` still uses `Load()` + `Store()` (no `Swap()`), so this is not fixed later.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-58062.json",
      "worthy_scope": true,
      "summary": "Adds infoschema-v2 activity TS tracking (`Data.recentMinTS`) and feeds it into `InfoSyncer.ReportMinStartTS()` to pin GC safepoint; introduces a High-severity lost-update race in the TS reset logic (`Load()+Store()` instead of atomic swap)."
    },
    {
      "pr": 58990,
      "subject": "dxf: support modify app param on scheduler part (#58990)",
      "commit": "62f9a39346ce58a7dfa6c7536c05b09ada8dd53e",
      "modules": [
        "pkg/ddl",
        "pkg/disttask/framework/proto",
        "pkg/disttask/framework/scheduler",
        "pkg/disttask/importinto",
        "pkg/disttask/example"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`TaskTypeExample` scheduler lacks an explicit `ModifyMeta` implementation; due to `BaseScheduler` embedding/forwarding `Extension`, calling `ModifyMeta` during task modification can recurse into itself and crash.",
      "impact": "If `TaskManager.ModifyTaskByID` is used with any non-`modify_concurrency` modification on `TaskTypeExample`, the scheduler can hit infinite recursion/stack overflow during `onModifying`, crashing the scheduler goroutine and potentially leaving the task stuck in `modifying` (until another owner recovers it).",
      "fix": "Add an explicit no-op `ModifyMeta` to `pkg/disttask/example/scheduler.go` (and any other scheduler impl using `BaseScheduler.Extension = self`), e.g. `func (*schedulerImpl) ModifyMeta(oldMeta []byte, _ []proto.Modification) ([]byte, error) { return oldMeta, nil }`.",
      "verify": "Create a `TaskTypeExample` task, call `TaskManager.ModifyTaskByID` with a non-concurrency modification (e.g. `ModifyMaxWriteSpeed`), and confirm the scheduler no longer stack-overflows and the task transitions out of `modifying` (and meta updates if implemented).",
      "evidence": "`scheduler.Extension` now requires `ModifyMeta` (`pkg/disttask/framework/scheduler/interface.go:134`) and `BaseScheduler.onModifying` invokes it for any non-concurrency modifications (`pkg/disttask/framework/scheduler/scheduler.go:423`). `pkg/disttask/example/scheduler.go` sets `s.BaseScheduler.Extension = s` (`pkg/disttask/example/scheduler.go:53`) but defines no explicit `ModifyMeta` method; because `BaseScheduler` embeds the `Extension` interface (`pkg/disttask/framework/scheduler/scheduler.go:85`), the promoted forwarding method will call back into `BaseScheduler.Extension`, causing self-recursive dispatch when meta modifications are requested.",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed on `upstream/master` (f0813a369666ac6314e76f5e573e982fe50c8a09): `pkg/dxf/example/scheduler.go:53` still assigns `BaseScheduler.Extension = s` and still lacks `ModifyMeta`, while the interface still requires it (`pkg/dxf/framework/scheduler/interface.go:151`).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-58990.json",
      "worthy_scope": true,
      "summary": "This commit wires non-concurrency task modifications through `scheduler.Extension.ModifyMeta` (persisting updated `task.Meta` via `ModifiedTask`) and adds new modification types for add-index app params. DDL backfill/import schedulers provide no-op `ModifyMeta`, but the Example scheduler omits an explicit `ModifyMeta`, creating a recursion/stack-overflow hazard when meta modifications are requested."
    },
    {
      "pr": 59009,
      "subject": "ddl: Implement TableMode feature (#59009)",
      "commit": "d9fb3a53376157be49b5a7941e6e689a33c81eb0",
      "modules": [
        "pkg/ddl",
        "pkg/infoschema",
        "pkg/meta/model",
        "pkg/util/dbutil",
        "pkg/planner/core"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "TableMode protection can be bypassed for `ALTER TABLE ... ADD FOREIGN KEY` when `foreign_key_checks=0`, allowing schema changes (and possibly auto-created indexes) on tables in Import/Restore mode.",
      "impact": "A session can bypass TableMode protection (Import/Restore) by setting `foreign_key_checks=0` and still mutate protected-table schema (and potentially auto-create indexes), violating the intended safety invariant during IMPORT INTO / BR restore windows and risking import/restore correctness.",
      "fix": "Add an explicit mode check on the *target* table for FK DDLs (e.g. in `pkg/ddl/executor.go` `CreateForeignKey`, and ideally also in the owner/job path) so protected tables reject `ADD FOREIGN KEY` regardless of `foreign_key_checks`. Alternatively, ensure the altered table is always recorded in ResolveContext for this statement so `core.CheckTableMode` applies.",
      "verify": "Put a table into `TableModeRestore`/`TableModeImport`, run `SET foreign_key_checks=0; ALTER TABLE <tbl> ADD FOREIGN KEY ...;` and confirm it returns `ErrProtectedTableMode` and does not apply FK/index metadata; also re-check the `foreign_key_checks=1` path.",
      "evidence": "`ALTER TABLE ... ADD FOREIGN KEY` sets `inCreateOrDropTable` during preprocess (`pkg/planner/core/preprocess.go:1741`), and `handleTableName` then early-returns without recording TableNameW (`pkg/planner/core/preprocess.go:1609`). The new guard only checks recorded tables (`pkg/planner/core/optimizer.go:252`), so it is skipped. The DDL path `CreateForeignKey` does not check table mode (`pkg/ddl/executor.go:5232`) and passes `ForeignKeyChecks` into the job. In the job, constraint validation is skipped when `fkCheck` is false (`pkg/ddl/foreign_key.go:672`), so no protected-table SELECT is executed to fail the job.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` (f0813a3696...): `resolveAlterTableStmt` still sets `inCreateOrDropTable` for FK adds and `CreateForeignKey` still lacks a table-mode check, so the bypass remains.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-59009.json",
      "worthy_scope": true,
      "summary": "Introduces TableMode metadata (Normal/Import/Restore), optimizer-time table-mode access checks, internal DDL ActionAlterTableMode, and exposes mode via `INFORMATION_SCHEMA.TABLES.TIDB_TABLE_MODE`."
    },
    {
      "pr": 59165,
      "subject": "ddl: reuse backend for DXF subtasks of same step (#59165)",
      "commit": "c7ce7902a87dc20cccb49a1a1e0fbed47fca0429",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/ingest",
        "pkg/lightning/backend/local"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "DXF local-sort read-index step can leak open engines on early errors: with backend reuse and `BackendCtx.Close()` no longer closing the backend, a `buildLocalStorePipeline` failure after `Register` returns without engine cleanup, leaving engines open in the shared `*local.Backend` and breaking subsequent retries/subtasks (Pebble lock/open-engine failures, potential step wedging).",
      "impact": "A transient/fatal error during pipeline construction after `Register` can leave engines and Pebble DBs open in the shared backend. Subsequent retries/subtasks on the same executor may fail opening engines due to file locks, wedge the DXF add-index step, leak goroutines/FDs until step cleanup, and in worst cases risk inconsistent engine state if repeated opens were to proceed.",
      "fix": "In DXF local-sort path, ensure cleanup runs on *all* error exits after engine registration when reusing a backend. Minimal fix: after `buildLocalStorePipeline` returns error, call `bCtx.FinishAndUnregisterEngines(ingest.OptCleanData)` (or at least `OptCloseEngines`) before returning. More robust: track whether `Register` succeeded and `defer` a cleanup-on-error; alternatively reset/close and recreate the shared backend on subtask failure to guarantee a clean slate for retries.",
      "verify": "Inject a failure after `backendCtx.Register` / during `NewReorgCopContext` (e.g. failpoint or invalid TZ to force `time.LoadLocation` error) in DXF local-sort add-index, then observe executor retry behavior: next `RunSubtask` should be able to `Register` without Pebble lock errors, and local sort dir/engines should be cleaned (`FinishAndUnregisterEngines(OptCleanData)` runs) before retry continues.",
      "evidence": "In `pkg/ddl/backfilling_read_index.go`, `(*readIndexExecutor).RunSubtask` builds a per-subtask `BackendCtx` using a shared `*local.Backend` (`Build(r.backendCfg, r.backend)`), then immediately returns on `buildLocalStorePipeline` error without any engine cleanup (`FinishAndUnregisterEngines` is only called after `executeAndClosePipeline`). `buildLocalStorePipeline` calls `backendCtx.Register(...)` before calling `NewAddIndexIngestPipeline(...)`, which can fail (it returns errors from `NewReorgCopContext`, e.g. timezone load errors via `reorgTimeZoneWithTzLoc`). With this PR, `litBackendCtx.Close()` in `pkg/ddl/ingest/backend.go` no longer closes the backend, so an early-return after `Register` leaves engines open inside the reused backend across retries/subtasks. Lightning local backend engine open is not re-entrant: `pkg/lightning/backend/local/engine_mgr.go` `openEngine()` calls `pebble.Open()` on the engine DB path and `LoadOrStore`, so a second `OpenEngine` on the same UUID before cleanup will typically hit Pebble lock errors (and/or risks duplicated ingest loops if it were to proceed).",
      "later_fixed": false,
      "later_fix_evidence": "Checked later history up to `pingcap/master@bab4993b6f`: `pkg/ddl/backfilling_read_index.go` still returns immediately on local pipeline build error without calling `FinishAndUnregisterEngines`, and `pkg/ddl/ingest/backend.go` `litBackendCtx.Close()` still does not close the backend; no later fix found for this error-path cleanup gap.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-59165.json",
      "worthy_scope": true,
      "summary": "Found one High issue: in DXF local-sort read-index executor, backend reuse + `BackendCtx.Close()` no longer closing the backend introduces an error-path engine leak (after `Register` but before `FinishAndUnregisterEngines`), which can wedge retries/subtasks via local engine (Pebble) reopen/lock behavior. Not fixed up to `pingcap/master@bab4993b6f`."
    },
    {
      "pr": 59343,
      "subject": "ddl: modify param at runtime for add-index on DXF local sort (#59343)",
      "commit": "54384b7cbb2a6716a6f4c777abe532b7252cc72d",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/disttask/framework/taskexecutor",
        "pkg/disttask/operator",
        "pkg/resourcemanager/pool/workerpool",
        "pkg/executor"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "DXF runtime concurrency modification does not update the StepExecutor’s stored `GetResource()` when applied during a running subtask. Because `readIndexStepExecutor.RunSubtask` re-reads concurrency from `GetResource().CPU.Capacity()` at each subtask start, a successful mid-subtask concurrency decrease can be lost for later subtasks, potentially rebuilding pipelines with stale (higher) concurrency and violating resource/slot assumptions.",
      "impact": "After a successful runtime concurrency decrease during a running subtask, later subtasks can still start with the old (higher) `GetResource().CPU.Capacity()` value, rebuilding pipelines with oversized worker pools. This breaks the intended “runtime param modification persists” semantics and can violate the framework’s resource/slot assumptions, risking oversubscription/OOM (especially for partitioned-table add-index where an executor runs many subtasks sequentially).",
      "fix": "Prefer framework-level fix so all StepExecutors behave correctly:\n- In `pkg/disttask/framework/taskexecutor/task_executor.go` `tryModifyTaskConcurrency`, after `ResourceModified` succeeds (both increase/decrease paths), call `e.stepExec.SetResource(newResource)` (optionally before calling `ResourceModified` as well).\n- Alternatively (local workaround), in `pkg/ddl/backfilling_read_index.go` `(*readIndexStepExecutor).ResourceModified`, call `r.SetResource(newResource)` after successful tuning so subsequent `RunSubtask` uses the updated resource.",
      "verify": "Manual scenario verification (no tests required):\n- Run DXF local-sort add-index on a partitioned table (multiple subtasks per executor).\n- While one subtask is running, issue `ADMIN ALTER DDL JOB ... THREAD = <smaller>`.\n- Confirm: the currently running subtask reduces worker pool sizes; then confirm *subsequent* subtasks start with the reduced concurrency (no reversion to old higher `GetResource()` value). Add temporary logs around `pkg/ddl/backfilling_read_index.go:123` and `ResourceModified` to compare observed concurrency across subtasks.",
      "evidence": "- `pkg/ddl/backfilling_read_index.go:123` builds each subtask pipeline with `concurrency := int(r.GetResource().CPU.Capacity())`.\n- `pkg/ddl/backfilling_read_index.go:204` `ResourceModified` tunes worker pools using `newResource` but never calls `r.SetResource(newResource)`.\n- DXF framework applies runtime concurrency via `tryModifyTaskConcurrency` (`pkg/disttask/framework/taskexecutor/task_executor.go:546`), which calls `e.stepExec.ResourceModified(...)` (`pkg/disttask/framework/taskexecutor/task_executor.go:554`, `pkg/disttask/framework/taskexecutor/task_executor.go:577`) and then records the new task concurrency via `concurrencyModifyApplied` (`pkg/disttask/framework/taskexecutor/task_executor.go:589`) but does not update the step executor’s stored resource via `SetResource`.\n- `SetResource` is only called in the between-subtask Run loop when it observes a task concurrency change (`pkg/disttask/framework/taskexecutor/task_executor.go:296`), which won’t trigger after `concurrencyModifyApplied` has already updated `e.task`.\n- `pkg/disttask/framework/taskexecutor/manager.go:455` shows `getStepResource` returns a fresh `*proto.StepResource` each time, so the old `GetResource()` pointer cannot “self-update” to the new CPU capacity.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` at `f0813a369666ac6314e76f5e573e982fe50c8a09`: the same pattern still exists (resource-modify path calls `ResourceModified` but does not update the stored `GetResource()` via `SetResource`; DDL step executors still read concurrency from `GetResource().CPU.Capacity()` when starting subtasks).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-59343.json",
      "worthy_scope": true,
      "summary": "Enables runtime param modification for DXF local-sort add-index by wiring `ModifyMeta` into the backfill scheduler and adding `TaskMetaModified`/`ResourceModified` handling in the read-index step executor, plus workerpool/pipeline support for safe tuning. Found a High-severity issue: runtime concurrency/resource changes applied during a running subtask do not update the StepExecutor’s stored `GetResource()`, so later subtasks can start with stale (pre-modify) concurrency and potentially oversubscribe resources."
    },
    {
      "pr": 59966,
      "subject": "globalsort: write sorted kv meta to external storage (#59966)",
      "commit": "ed8b869a60175d0c6f39575fd4fdc098acaad841",
      "modules": [
        "pkg/ddl",
        "pkg/lightning/backend/external",
        "pkg/disttask/framework/proto",
        "pkg/disttask/framework/scheduler"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Global-sort add-index subtask meta format becomes backward-incompatible (ExternalPath + external storage), but disttask scheduling has no version/capability gating, so mixed-version clusters can dispatch these subtasks to older executors that can’t read required meta.",
      "impact": "In a rolling-upgrade (or any mixed-version) cluster, global-sort add-index tasks can fail or get stuck when merge-sort/ingest subtasks land on nodes running pre-#59966 code: required subtask inputs (`DataFiles`, `MetaGroups`, `EleIDs`, range keys) are missing from the DB meta and the old executor won’t fetch the external meta, leading to runtime errors/incorrect planning and potentially requiring manual intervention/cleanup.",
      "fix": "Gate the new externalized-meta format on executor capability to avoid mixed-version breakage.\nMinimal options:\n- In `LitBackfillScheduler.GetEligibleInstances`, filter `eligibleNodes` to those whose `infosync.ServerInfo.Version` (or `GitHash`) is >= the first version that includes `BaseExternalMeta` + external-meta decoding, and reject/suspend the task if insufficient nodes.\n- Alternatively, at task submission, verify all managed nodes are at/above the required version before allowing `CloudStorageURI`-enabled (global-sort) backfill.\n- If version filtering is infeasible, keep writing legacy in-DB fields (no omission) until cluster homogeneous (but this may re-trigger the meta-size issue that motivated #59966).",
      "verify": "Repro/verification path:\n1) Run a mixed-version cluster with one TiDB at this commit and one at the first-parent (pre-#59966).\n2) Start ADD INDEX with global sort (CloudStorageURI configured) and ensure subtasks are scheduled across both nodes.\n3) Observe old-version executor failing on merge-sort or ingest subtasks due to empty `DataFiles/MetaGroups/EleIDs` in `subtask.Meta`.\nAfter fix, confirm scheduler excludes old nodes (or refuses task submission) and the task completes without such failures.",
      "evidence": "This commit makes global-sort add-index subtask meta backward-incompatible by externalizing fields into object storage and persisting only an `ExternalPath` pointer:\n- `pkg/lightning/backend/external/util.go`: `BaseExternalMeta.Marshal` returns `marshalInternalFields` when `ExternalPath != \"\"`, omitting any struct field tagged `external:\"true\"`.\n- `pkg/ddl/backfilling_dist_executor.go`: `BackfillSubTaskMeta` tags `RangeJobKeys`, `RangeSplitKeys`, `DataFiles`, `StatFiles`, `MetaGroups`, `EleIDs`, and embedded `external.SortedKVMeta` as `external:\"true\"`; `writeExternalBackfillSubTaskMeta` sets `subtask.ExternalPath` and writes only the external fields via `WriteJSONToExternalStorage`.\n- `pkg/ddl/backfilling_read_index.go` and `pkg/ddl/backfilling_merge_sort.go`: onFinished now calls `writeExternalBackfillSubTaskMeta(...)` and then stores `sm.Marshal()` back into `subtask.Meta`, meaning DB meta no longer contains `DataFiles/MetaGroups/EleIDs/...`.\nOlder TiDB binaries (first-parent of this commit) do not have `ExternalPath` nor any external-storage read path in `decodeBackfillSubTaskMeta`, so they will ignore `ExternalPath` and observe empty `DataFiles/MetaGroups/EleIDs`, breaking merge-sort/ingest execution.\nSecond confirmation (different evidence): disttask scheduling does not filter executors by version, so mixed-version clusters can dispatch these subtasks to older nodes:\n- `pkg/disttask/framework/scheduler/scheduler.go`: `BaseScheduler.switch2NextStep` schedules to `eligibleNodes` derived from `NodeManager` + scope only; no use of `infosync.ServerInfo.Version/GitHash`.\n- `pkg/disttask/framework/scheduler/scheduler.go:GetLiveExecIDs` enumerates all `infosync.ServerInfo` and uses `disttaskutil.GenerateExecID` (IP:PORT) with no version gating.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master`: the framework still does not version-filter executors (`pkg/dxf/framework/scheduler/scheduler.go:GetLiveExecIDs` still returns all nodes), and DDL backfill still relies on externalized meta (`pkg/lightning/backend/external/util.go:BaseExternalMeta.Marshal` keeps omitting `external:\"true\"` fields when `ExternalPath` is set). No upstream change found that would prevent old nodes from receiving these subtasks or make old nodes able to read the external meta.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-59966.json",
      "worthy_scope": true,
      "summary": "Commit externalizes large global-sort backfill subtask meta to object storage (meta.json) and stores only `ExternalPath` in subtask meta JSON; this risks mixed-version compatibility because older executors will ignore `ExternalPath` and see missing `DataFiles/MetaGroups/EleIDs`, while scheduling does not filter executors by version."
    },
    {
      "pr": 60395,
      "subject": "disttask: distribute merge sort subtasks to all available nodes (#60395)",
      "commit": "5a00a10d3839ad2465c090469e55d3dc2392ea42",
      "modules": [
        "pkg/ddl",
        "pkg/lightning/backend/external"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "`external.DivideMergeSortDataFiles` is introduced/used to distribute merge-sort subtasks by node count, but its remainder batching can exceed the documented `MergeSortFileCountStep` cap, making its `maxTargetFilesPerSubtask := max(16, mergeConc)` overlap-budget check unsound. For some `mergeConc>16` + large-file-count inputs, this can produce merge-sort subtasks whose internal merge fanout (and thus downstream overlap/reader pressure) exceeds the intended `MergeSortOverlapThreshold` budget, risking global-sort add-index instability.",
      "impact": "Under certain `(dataFilesCnt, nodeCnt, task.Concurrency)` combinations (notably `task.Concurrency>16`), merge-sort planning can generate oversized file groups that invalidate the overlap/fanout budgeting assumptions. That can lead to excessive downstream merge fanout (more target files than budgeted), increasing the chance that subsequent range-splitting/metadata iteration (which is explicitly designed around `MergeSortOverlapThreshold`) becomes unstable (resource blowups, reader limits, or planner failures), potentially stalling or failing add-index global-sort workflows.",
      "fix": "Make `DivideMergeSortDataFiles`’ sizing math consistent with its documented/assumed invariants:\n- Either enforce the per-group cap for *all* returned groups (including remainder), e.g. ensure `adjustNodeCnt >= ceil(remainder/MergeSortFileCountStep)`; if that conflicts with the overlap-budget constraint, return a clear error (or proactively lower merge concurrency) instead of silently producing oversized groups.\n- Or make the bound concurrency-aware by computing per-batch worst-case fanout as `max(mergeConc, ceil(batchSize/MaxMergingFilesPerThread))` and choosing `adjustNodeCnt`/batch sizes so the aggregate fanout stays under the threshold.\nAlso consider improving the error message (current `\"unexpected zero node count\"` is misleading for “cannot satisfy budget”).",
      "verify": "Add a unit test for `DivideMergeSortDataFiles` covering `mergeConc>16` with a case where `adjustNodeCnt` is reduced (like the example arithmetic), asserting:\n- Every returned group size respects the intended cap (or the new cap definition).\n- The aggregate fanout estimate is actually enforced: `sum(len(splitDataFiles(group, mergeConc))) <= MergeSortOverlapThreshold`.\nAlso add a test for “impossible” inputs to ensure a deterministic, actionable error (and no infinite retry loop behavior at the scheduler level).",
      "evidence": "DDL merge-sort planning now calls `external.DivideMergeSortDataFiles(dataFiles, nodeCnt, task.Concurrency)` when generating merge-sort subtasks (`pkg/ddl/backfilling_dist_scheduler.go:619`).\n\n`DivideMergeSortDataFiles` documents a per-group cap of `MergeSortFileCountStep` but only enforces it for the “full rounds”; the remainder is batched via `mathutil.Divide2Batches(remainder, adjustNodeCnt)` without any `<= MergeSortFileCountStep` cap (`pkg/lightning/backend/external/util.go:533`, `pkg/lightning/backend/external/util.go:547`, `pkg/lightning/backend/external/util.go:564`). Because `adjustNodeCnt` can be reduced by the overlap-budget loop (`pkg/lightning/backend/external/util.go:557`), remainder batch sizes can exceed 4000 even though the function comment claims otherwise.\n\nThis matters because the downstream merge fanout depends on `splitDataFiles`: `shares := max(ceil(len(paths)/MaxMergingFilesPerThread), concurrency)` with `MaxMergingFilesPerThread=250` (`pkg/lightning/backend/external/merge.go:30`, `pkg/lightning/backend/external/merge.go:90`). If a returned group’s size exceeds `concurrency*250`, the number of internal merge shares (i.e. produced “target” files) can exceed `mergeConc`, breaking the `maxTargetFilesPerSubtask := max(16, mergeConc)` estimate used in `DivideMergeSortDataFiles`’ overlap-budget loop (`pkg/lightning/backend/external/util.go:557`).\n\nConcrete arithmetic example (second confirmation): with `mergeConc=20`, `nodeCnt=7`, and `fileCnt≈811999` you get `rounds*nodeCnt=196`, the loop reduces `adjustNodeCnt` to 4, and the remainder (~27999) is divided into ~7000-file batches (>4000 and >20*250). Then `splitDataFiles` yields `ceil(7000/250)=28` shares per such subtask (>20), so total target-file count can exceed the intended `MergeSortOverlapThreshold` budget even though the loop was constructed to keep it within budget.",
      "later_fixed": false,
      "later_fix_evidence": "Checked later history `HEAD..upstream/master` for `pkg/lightning/backend/external/util.go`: the only related follow-up (`06674e2309`) adjusts step/threshold helpers but `upstream/master:pkg/lightning/backend/external/util.go` still batches the remainder via `Divide2Batches` without enforcing a per-group cap, so the inconsistency remains present upstream.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-60395.json",
      "worthy_scope": true,
      "summary": "This commit changes DDL global-sort add-index merge-sort planning to split merge-sort subtasks across eligible nodes via the new `external.DivideMergeSortDataFiles`, and updates a DDL ingest memory failpoint/test. The main High-risk issue is an inconsistency in `DivideMergeSortDataFiles`: remainder groups are not actually capped by `MergeSortFileCountStep` as documented/assumed, which can break its overlap/fanout budgeting and potentially destabilize add-index global-sort planning/execution under certain concurrency + file-count combinations; this is not later-fixed on `upstream/master`."
    },
    {
      "pr": 60403,
      "subject": "ddl: Fix ddl owner doesn't use etcd keyspace prefix (#60403)",
      "commit": "22a8df5a753e91555bb40a6a9608495b750638b6",
      "modules": [
        "pkg/ddl",
        "pkg/keyspace",
        "pkg/store",
        "pkg/util/etcd"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Keyspace (API V2) rolling upgrade split-brain risk: DDL owner election keyspace prefix change makes old and new versions campaign on different etcd paths, allowing multiple concurrent DDL owners.",
      "impact": "In keyspace (API V2) deployments, mixed-version rolling upgrades can create two independent DDL owner elections (legacy unprefixed vs new keyspace-prefixed). This can yield two concurrent “owners” for the same keyspace, risking concurrent DDL job execution and background owner-only tasks (e.g., dead lock cleanup), which can corrupt or stall metadata/state-machine progress.",
      "fix": "Decide/encode an explicit upgrade strategy for keyspace (API V2) deployments to avoid split-brain owners:\n- Option A (safety-first): detect presence of legacy unprefixed owner keys and refuse to start (or disable DDL owner campaign) with a clear error telling operators to stop all old TiDB nodes first.\n- Option B (compat window): during a deprecation window, ensure new nodes still coordinate with legacy election (e.g., take a global compat lock) so old+new cannot both act as owner simultaneously.\nAlso document the operational requirement for keyspace-mode upgrades.",
      "verify": "Run a mixed-version keyspace-name cluster:\n- Start at least one old-version TiDB and one new-version TiDB against the same keyspace.\n- Inspect etcd for both prefixes: legacy `/tidb/ddl/fg/owner/` and namespaced `/keyspaces/tidb/<id>/tidb/ddl/fg/owner/`.\n- Confirm at most one effective owner is active (only one processes DDL jobs / owner-only loops) throughout the upgrade window.",
      "evidence": "This commit changes the DDL owner election etcd client to be keyspace-namespaced: `pkg/ddl/owner_mgr.go:71` calls `etcd.SetEtcdCliByNamespace(cli, keyspace.MakeKeyspaceEtcdNamespace(store.GetCodec()))` before creating the owner manager (`pkg/ddl/owner_mgr.go:74`). The namespace prefix is `\"/keyspaces/tidb/<keyspaceID>\"` for API V2 (`pkg/keyspace/keyspace.go:36`). Keyspace mode (API V2) is enabled when `keyspaceName != \"\"` (`pkg/store/driver/tikv_driver.go:202`). DDL instances use `globalOwnerManager` for real-TiKV owner election when etcd is present (`pkg/ddl/ddl.go:673`). The first-parent version did not set any namespace, so it campaigned under the unprefixed `DDLOwnerKey` (`/tidb/ddl/fg/owner`).",
      "later_fixed": false,
      "later_fix_evidence": "",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-60403.json",
      "worthy_scope": true,
      "summary": "Adds keyspace namespacing to the DDL owner election etcd client; this fixes cross-keyspace contention but can cause mixed-version keyspace-mode upgrades to have split-brain DDL owners due to the changed election path."
    },
    {
      "pr": 60828,
      "subject": "ddl: update row count periodically when running reorg job (#60828)",
      "commit": "e8fb24a20d47ec227ec6f075631927871fcc49b7",
      "modules": [
        "pkg/ddl"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Pause handling can leave reorg work running after a job becomes `JobStatePaused`: because reorg now returns periodic timeouts while preserving `jobCtx.stepCtx`, but the `JobStatePausing/Paused` fast-path returns without canceling that long-lived context, and the scheduler exits on paused (final state), the background reorg goroutine/workers can continue after a successful pause.",
      "impact": "`ADMIN PAUSE DDL JOBS` can reach `JobStatePaused` (a scheduler final state) while the background reorg goroutine/workers keep running under the preserved `stepCtx`, continuing resource consumption and potentially continuing backfill KV work after the job is “paused”. This violates pause semantics and can leave long-lived goroutines/reorg contexts running until owner changes or the job is resumed.",
      "fix": "- When handling `JobStatePausing/Paused`, explicitly cancel/cleanup the long-lived `jobCtx.stepCtx` (and ensure cleanup isn’t skipped due to the reorg-timeout flag). A minimal approach is to cancel `stepCtx` with an `ErrPausedDDLJob` cause before returning from the pausing path, so the background reorg goroutine/workers stop before the scheduler exits on `JobStatePaused`.\n- Consider mirroring the later rollback-oriented fix pattern (explicit step context cleanup when leaving running reorg), but for pause as well, so pausing reliably stops reorg resources regardless of timing.",
      "verify": "- With failpoint-enabled build: set `github.com/pingcap/tidb/pkg/ddl/updateProgressIntervalInMs` to a small value (< poll interval) to force frequent reorg timeouts; start an add-index reorg job; issue `ADMIN PAUSE DDL JOBS <id>`; after `ADMIN SHOW DDL JOBS` reports `paused`, verify reorg/backfill work stops (row count stops increasing, no further backfill logs) and no in-memory reorg context/goroutine leak is observed.\n- Add a unit/integration test that pauses immediately after a timeout return but before the next poll tick, and asserts pausing cancels the job step context and stops reorg work.",
      "evidence": "- `pkg/ddl/reorg.go`: `runReorgJob` now returns `dbterror.ErrWaitReorgTimeout` on `updateProcessTicker.C` after `job.SetRowCount(...)`/warnings merge, and sets `jobCtx.reorgTimeoutOccurred = true`, leaving the background `reorgFn()` goroutine running and keeping the `reorgCtx` in-memory.\n- `pkg/ddl/job_worker.go`: `jobContext.cleanStepCtx()` skips cancelling/clearing `stepCtx` when `reorgTimeoutOccurred` is set, so `jobCtx.stepCtx` becomes long-lived across those periodic timeouts.\n- `pkg/ddl/job_worker.go`: `processJobPausingRequest()` converts `JobStatePausing` -> `JobStatePaused` and returns before starting the polling goroutine (the only place that calls `jobCtx.stepCtxCancel(...)`) and without any explicit `stepCtx` cancellation.\n- `pkg/ddl/job_scheduler.go`: `deliveryJob` refreshes the job after every step “to handle cases like job cancel or pause”, so the step after a pause request will typically see `JobStatePausing/Paused` and take the early return path; and `model.Job.InFinalState()` treats `JobStatePaused` as final, so the scheduler stops running the job after it becomes paused—removing the last chance to cancel the still-alive `stepCtx`/reorg workers.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` (e.g. `bab4993b6f`): the reorg timeout still keeps `stepCtx` alive and `processJobPausingRequest()` still early-returns without cancel/cleanup of `stepCtx` on pausing/paused jobs, so the described pause race/behavior remains.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-60828.json",
      "worthy_scope": true,
      "summary": "This commit changes reorg progress handling to periodically return `ErrWaitReorgTimeout` so row count/progress gets persisted to `mysql.tidb_ddl_job` during reorg, and adjusts `jobContext.stepCtx` lifecycle/polling (including multi-schema change inner-step polling suppression). One High-severity state-machine/concurrency issue remains: pausing can reach `JobStatePaused` without canceling the long-lived reorg step context, so background reorg workers may continue after the job is “paused”."
    },
    {
      "pr": 61865,
      "subject": "ddl: refresh TiFlash PlacementRules periodically (#61865)",
      "commit": "cbafb78f19039a3b4daf1d638108b9c4a0b5db06",
      "modules": [
        "pkg/ddl",
        "pkg/domain/infosync"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Periodic placement-rule “repair” can override user TiFlash replica changes due to stale job args: it submits `ActionSetTiFlashReplica` using snapshot Count/Labels, and when that job executes later it rewrites `TiFlashReplica` metadata even if a prior queued user DDL already changed/disabled TiFlash replica.",
      "impact": "If a user submits `ALTER TABLE ... SET TIFLASH REPLICA ...` (disable or change count/labels) that is ahead in the DDL queue while PD rules are missing, the periodic repair job submitted later can execute afterwards and revert/override the user’s intended TiFlash replica settings, potentially re-enabling TiFlash or restoring old labels/count unexpectedly (metadata/state-machine correctness + operational surprise).",
      "fix": "Avoid encoding Count/Labels from a snapshot into a delayed repair job: add a dedicated repair path/job (e.g. `ActionRepairTiFlashPlacementRules`) that, at execution time, re-reads the latest table meta and only (re)applies PD rules if `TiFlashReplica` is still enabled; do not overwrite `TiFlashReplica` Count/Labels from stale args. At minimum, include an expected (count, labels) precondition and cancel/skip if the table’s current `TiFlashReplica` differs or is nil.",
      "verify": "Repro on a NextGen node: (1) enable TiFlash replica on a table; (2) delete the table/partition TiFlash placement rule in PD to make it “missing”; (3) enqueue a user `SET TIFLASH REPLICA 0` (or change labels/count) but delay its execution (e.g. with other DDLs ahead); (4) let the poller submit the repair job; (5) confirm final `SHOW CREATE TABLE`/infoschema `TiFlashReplica` reflects the user change and is not reverted after the repair job runs.",
      "evidence": "`refreshTiFlashPlacementRules` builds `ActionSetTiFlashReplica` job args from the *current infoschema snapshot* (`replica.TableInfo.TiFlashReplica.Count/LocationLabels`) and submits via `doDDLJob2` (`pkg/ddl/ddl_tiflash_api.go:565`-`pkg/ddl/ddl_tiflash_api.go:637`). `doDDLJob2` enqueues then waits for completion (`pkg/ddl/executor.go:6757`-`pkg/ddl/executor.go:6764`), so the job can run after earlier queued DDLs. The job handler unconditionally rewrites `tblInfo.TiFlashReplica` from args (no guard that current settings still match) (`pkg/ddl/table.go:1083`-`pkg/ddl/table.go:1151`).",
      "later_fixed": false,
      "later_fix_evidence": "",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-61865.json",
      "worthy_scope": true,
      "summary": "Adds periodic TiFlash placement-rule refresh in the DDL owner by scanning TiFlashReplica metadata and submitting `ActionSetTiFlashReplica` jobs when PD rules are missing; introduces infosync `GetPlacementRule` plumbing for TiFlash placement-rule lookup."
    },
    {
      "pr": 62014,
      "subject": "ddl: send result chunks to writers after scan task complete (#62014)",
      "commit": "f2534055f29fdd0f62c7ed3b4ea746e60754a74f",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/ingest",
        "pkg/lightning/backend/local",
        "tests/realtikvtest/addindextest3"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "Potential large memory spike/OOM risk: table scan now buffers all result chunks for a PD-region-sized `TableScanTask` before sending to writers, removing the prior streaming/backpressure behavior.",
      "impact": "On large/wide tables with many regions and higher reorg concurrency, concurrent scan workers can retain multiple full-region scan outputs in Go heap, causing multi-GB memory spikes, long GC pauses, or OOM/crash during ADD INDEX ingest (availability and DDL stability risk).",
      "fix": "Bound per-task buffering: split `TableScanTask` into smaller key ranges than a full PD region (so each task produces a small, predictable number of chunks), or add an explicit memory/row-count cap that forces re-splitting/yielding when exceeded.",
      "verify": "Stress a large multi-region table (wide rows) with elevated `@@tidb_ddl_reorg_worker_cnt`, run ADD INDEX in ingest mode, and monitor TiDB heap/GC (pprof/memstats) to confirm memory stays bounded and the DDL completes without OOM/regressions.",
      "evidence": "`pkg/ddl/backfilling_operators.go:550` now buffers `idxResults` (appends every fetched `*chunk.Chunk` at `pkg/ddl/backfilling_operators.go:577`) and only calls `sender(...)` after `wrapInBeginRollback(...)` completes (`pkg/ddl/backfilling_operators.go:584`). Each `TableScanTask` is a PD-region key range (`pkg/ddl/backfilling_operators.go:272`), produced via `loadTableRanges` using `BatchLoadRegionsWithKeyRange` (`pkg/ddl/backfilling.go:486`, `pkg/ddl/backfilling.go:527`). Operator wiring uses an unbuffered channel (`pkg/disttask/operator/compose.go:30`), so the previous “send-per-chunk” design applied tight backpressure; this change allows scanning to materialize an entire region’s worth of chunks in memory before any writer receives them. Reader concurrency can exceed writer concurrency based on avg row size (`pkg/ddl/backfilling_txn_executor.go:349`).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master` (`bab4993b6fd61de478062151bb615a0e09c0aecb`): `pkg/ddl/backfilling_operators.go` still buffers per-task scan results and only sends them after the scan transaction finishes, preserving the same full-task buffering behavior.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-62014.json",
      "worthy_scope": true,
      "summary": "Moves `TableScanOperator` to buffer per-task scan chunks and only deliver them to writers after the scan transaction finishes; adds/adjusts failpoints and a realtikv test to assert no scan transaction is in-flight during local-engine import."
    },
    {
      "pr": 62120,
      "subject": "ddl/dxf: modify ingest param at runtime for add-index with global sort (#62120)",
      "commit": "c49ac642c7f687b2721658983ae7606b19f8a316",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/ingest",
        "pkg/disttask/framework",
        "pkg/lightning/backend/local",
        "pkg/lightning/backend/external"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Runtime concurrency updates for global-sort add-index don’t reliably carry over to external-engine creation for later subtasks because `engineManager.closeEngine` uses a stale `WorkerConcurrency` copy (separate from the updated `local.Backend`), risking resource oversubscription/stalls after downscaling.",
      "impact": "After a runtime task-concurrency reduction during global-sort add-index, subsequent write&ingest subtasks on the same executor can recreate external engines with a stale higher `workerConcurrency` even though `StepResource` (CPU/mem) is already reduced. This can:\n- Over-allocate memory during `readAllData`/sort (potentially blocking on the memory limiter and stalling the DDL),\n- Oversubscribe CPU via `sorty.MaxGor`,\n- Violate dxf/disttask resource-safety assumptions around downscaling (risking OOM or long stalls).",
      "fix": "Make worker-concurrency updates authoritative and shared for external-engine creation:\n- Add a `func (b *local.Backend) SetWorkerConcurrency(int)` override that updates both `b.BackendConfig.WorkerConcurrency` and `b.engineMgr.WorkerConcurrency` (and have DDL call this), OR\n- Pass an explicit concurrency into `backend.ExternalEngineConfig` and have `engineManager.closeEngine` use that instead of `em.WorkerConcurrency`, OR\n- At minimum, before `CloseEngine` in `cloudImportExecutor.RunSubtask`, set backend/engineMgr worker concurrency from `e.GetResource().CPU.Capacity()` so newly created external engines match the current resource.",
      "verify": "Repro/inspection path (no tests required):\n1) Run a global-sort add-index with multiple write&ingest subtasks on one executor.\n2) While subtask A is running, reduce task concurrency (so `ResourceModified` applies and frees slots).\n3) Observe subtask B start: confirm the newly created external engine’s `workerConcurrency` (from `engineManager.closeEngine`) matches the new concurrency, and that `LoadIngestData` does not attempt batches sized for the old concurrency under the reduced `MemCapacity`.\nA minimal unit-level check would assert that after calling `backend.SetWorkerConcurrency(x)` then `CloseEngine` for an external engine, the created `external.Engine` sees `workerConcurrency==x`.",
      "evidence": "In `pkg/ddl/backfilling_import_cloud.go`, `cloudImportExecutor.ResourceModified` updates only the `*local.Backend` via `e.backend.SetWorkerConcurrency(newConcurrency)` and the currently-running `*external.Engine` via `eng.UpdateResource(...)`, but each `RunSubtask` recreates the external engine by calling `localBackend.CloseEngine(...)` again.\n\nExternal engine creation for each subtask goes through `pkg/lightning/backend/local/engine_mgr.go` `(*engineManager).closeEngine`, which passes `int(em.WorkerConcurrency.Load())` into `external.NewExternalEngine(...)`. However, `pkg/lightning/backend/local/local.go` `NewBackend` copies `BackendConfig` by value into BOTH `Backend.BackendConfig` and `engineMgr.BackendConfig` (`newEngineManager(config, ...)`), so later `e.backend.SetWorkerConcurrency(...)` does not update `em.WorkerConcurrency`.\n\n`pkg/lightning/backend/external/engine.go` uses `workerConcurrency` to (a) set `currBatchSize` in `LoadIngestData` (how many range jobs are loaded/sorted per batch) and (b) set `sorty.MaxGor = workerConcurrency*2` in `loadRangeBatchData`. Range job sizing is explicitly tied to memory-per-core (`pkg/lightning/backend/external/split.go` `CalRangeSize(memPerCore, ...)`). So if task concurrency is reduced mid-step and the next subtask recreates an external engine with the old higher `workerConcurrency`, it can oversubscribe memory/CPU relative to the new `StepResource` and undermine the framework’s “reduce resources before freeing slots to avoid OOM” contract (see `pkg/disttask/framework/taskexecutor/task_executor.go` `tryModifyTaskConcurrency`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `c49ac642..master` history and current `master` content: `pkg/lightning/backend/local/engine_mgr.go` still constructs external engines with `int(em.WorkerConcurrency.Load())`, while `pkg/lightning/backend/local/local.go` `NewBackend` still copies `BackendConfig` by value into both `Backend` and `engineManager` (no shared atomic), and there is no `(*Backend) SetWorkerConcurrency` override to propagate updates.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-62120.json",
      "worthy_scope": true,
      "summary": "Implements disttask hooks to update global-sort add-index ingest params at runtime (write speed + ingest concurrency). A high-risk gap remains: worker-concurrency updates are not propagated to the `engineManager` copy used when recreating external engines for later subtasks, so downscaling can be silently lost and lead to stalls/OOM/CPU oversubscription."
    },
    {
      "pr": 62190,
      "subject": "ddl: add retry for updateSelfVersion (#62190)",
      "commit": "c65420d934ee6e61902bef63c3410beed7b964fa",
      "modules": [
        "pkg/ddl/schemaver",
        "pkg/ddl/util",
        "pkg/infoschema/issyncer",
        "tests/realtikvtest/ddltest"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "High risk of permanent hang / restart-safety bypass when MDL is disabled: UpdateSelfVersion now retries etcd Put “unlimited” using a non-cancelable context (via callers passing context.Background) and a lease captured once. If the etcd session/lease becomes invalid (e.g., prolonged etcd outage > TTL or domain close), the Put can never succeed and Reload can block indefinitely, preventing SyncLoop from handling syncer.Done() to stop validator and restart the syncer.",
      "impact": "In MDL-disabled (classic) mode, persistent etcd/lease failures can block infoschema Reload indefinitely, freezing the schema sync loop. This can prevent processing syncer.Done (no restart) and bypass the intended schemaValidator.Stop safety gate on session loss, risking prolonged stale-schema operation (DDL propagation stalls; DML/query correctness/availability can degrade) and potentially hanging shutdown/restart flows.",
      "fix": "Make UpdateSelfVersion retry bounded/cancelable and session-aware: (1) pass a cancelable ctx from SyncLoop into Reload/UpdateSelfVersion (or wrap the call with a timeout derived from lease, e.g. 2*schemaLease) instead of context.Background; (2) in etcdSyncer.UpdateSelfVersion, avoid retrying forever with a potentially-stale lease ID—break/return when s.loadSession().Done() is closed and/or re-fetch the current session lease between retries (outer retry loop that recreates OpOption each attempt).",
      "verify": "Repro-style verification: in a classic/MDL-disabled run, simulate etcd outage > SessionTTL (or force session close) while a schema reload is happening and confirm SyncLoop can still hit <-syncer.Done() and restart; add a test that combines PutKVToEtcdError (forces retry) with ErrorMockSessionDone (closes session) and asserts Reload/SyncLoop doesn’t hang. Run existing realtikvtest coverage: go test ./tests/realtikvtest/ddltest -run TestUpdateSelfVersionFail.",
      "evidence": "Change introduces unbounded retry on the non-MDL path: pkg/ddl/schemaver/syncer.go:289-293 now calls util.PutKVToEtcd(ctx, ..., putKeyRetryUnlimited, ..., clientv3.WithLease(s.loadSession().Lease())). Caller uses context.Background in Reload: pkg/infoschema/issyncer/syncer.go:365-408 (UpdateSelfVersion at :395) while holding s.m. PutKVToEtcd retries until ctx.Err or success: pkg/ddl/util/util.go:370-396; with context.Background it never stops. Second confirmation: Reload runs inline in the main SyncLoop goroutine (pkg/infoschema/issyncer/syncer.go:257-304), so a stuck Reload blocks handling <-syncer.Done() (and its schemaValidator.Stop()/restart safety path at :278-301). Upstream/master still has the same pattern (Reload calls UpdateSelfVersion(context.Background()), and UpdateSelfVersion uses putKeyRetryUnlimited when MDL is off), so not fixed later.",
      "later_fixed": false,
      "later_fix_evidence": "Checked upstream/master (bab4993b6f...): Reload still calls UpdateSelfVersion(context.Background(), 0, ...) and non-MDL UpdateSelfVersion still uses putKeyRetryUnlimited + WithLease, so the hang/coordination risk remains.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-62190.json",
      "worthy_scope": true,
      "summary": "Switches non-MDL UpdateSelfVersion etcd write from single-attempt to putKeyRetryUnlimited; adds a PutKVToEtcd failpoint hook and a realtikvtest to simulate transient self-version update failures."
    },
    {
      "pr": 62477,
      "subject": "ddl: skip waiting version synced without MDL for multi-schema-change (#62477)",
      "commit": "e2e01c59d6998ffc39d2dcfa188e985def0f5126",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/schemaver",
        "pkg/meta/model"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "Compatibility/correctness risk: in non-MDL mode the restart/owner-change pre-sync is skipped when `job.LastSchemaVersion==0`; for jobs persisted by older TiDB versions (field absent => decoded as 0), this can skip required schema-version synchronization and break the online DDL 2-version invariant.",
      "impact": "With `tidb_enable_metadata_lock=off`, resuming a started DDL job created by an older TiDB binary after owner crash/rolling upgrade can skip the required pre-run schema sync, violating the 2-version invariant and potentially causing online-DDL state-machine correctness issues (e.g. inconsistent DML writes/backfill gaps) if the previous owner died after committing a schema-version bump but before sync completed.",
      "fix": "Preserve old behavior for “unknown” jobs: implement custom JSON unmarshal (or a decode wrapper) to detect missing `last_schema_version` and set a sentinel (e.g. `-1`), then in scheduler wait when `LastSchemaVersion != 0` (unknown or >0) and skip only when explicitly 0; alternatively, restrict the skip to `ActionMultiSchemaChange` to reduce rolling-upgrade blast radius.",
      "verify": "Unit: decode a historical Job JSON blob without `last_schema_version` and ensure owner-change path still performs non-MDL pre-sync; Integration: run a DDL on an older binary with MDL off, crash owner between commit and version sync, restart with this binary, and confirm schema sync happens before the next online-DDL state transition.",
      "evidence": "`pkg/ddl/job_scheduler.go:562` describes maintaining the online-DDL 2-version invariant on restart/owner-change; the non-MDL pre-sync now runs only when `job.LastSchemaVersion > 0` (`pkg/ddl/job_scheduler.go:583`). `Job.Decode` is plain `json.Unmarshal` (`pkg/meta/model/job.go:560`), so older job JSON without `last_schema_version` (`pkg/meta/model/job.go:397`) decodes as 0 and will skip the wait; invariant requirement is also stated in `pkg/ddl/doc.go:23`.",
      "later_fixed": false,
      "later_fix_evidence": "Searched `e2e01c59d6..upstream/master` for follow-up changes and found no modifications to `LastSchemaVersion` usage; `upstream/master` still gates non-MDL pre-sync on `job.LastSchemaVersion > 0`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-62477.json",
      "worthy_scope": true,
      "summary": "This commit adds `Job.LastSchemaVersion` and uses it to skip `waitVersionSyncedWithoutMDL` when MDL is off and the last DDL step didn’t bump schema version; it also adds a regression test for multi-schema-change under MDL toggle. One High risk remains: jobs created by older binaries decode `LastSchemaVersion` as 0, which can incorrectly skip the restart/owner-change pre-sync in non-MDL mode and violate the online-DDL 2-version invariant; no later fix found up to `upstream/master`."
    },
    {
      "pr": 62570,
      "subject": "bootstrap: use reserved ID for system schemas/tables in nextgen (#62570)",
      "commit": "cfe9c12c9afeb97ceacd7d7b041d21b160d3197f",
      "modules": [
        "pkg/meta",
        "pkg/session",
        "pkg/ddl",
        "pkg/infoschema"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Nextgen bootstrap uses reserved IDs for system schemas/tables but only checks existence by ID; upgrading from an already-bootstrapped nextgen cluster with non-reserved system schema IDs can create duplicate `mysql`/`sys` schema names and cause infoschema/metadata nondeterminism.",
      "impact": "Upgrading a nextgen cluster that was bootstrapped before this change (system schemas created via DDL with non-reserved IDs) can create *duplicate* `mysql`/`sys` databases with the same names but different IDs.\nThis can lead to nondeterministic infoschema selection (name overwrite), system table reads/writes against the wrong schema ID, bootstrap/upgrade variables stored in an unexpected `mysql.tidb`, and hard-to-debug metadata inconsistency or startup instability.",
      "fix": "In nextgen, prevent creating a second system schema when an older/non-reserved one already exists:\n- In `(*meta.Mutator).CreateMySQLDatabaseIfNotExists` (`pkg/meta/meta.go:806`), first detect an existing `mysql` DB by *name* via `GetSystemDBID()`. If found and `id != metadef.SystemDatabaseID`, return a clear error (or implement a migration) instead of creating a new reserved-ID schema.\n- Do the same for `sys` in `bootstrapSchemas` (`pkg/session/bootstrap.go:1198`) before `CreateSysDatabaseByIDIfNotExists`, or enhance `CreateSysDatabaseByIDIfNotExists` to also check for name collisions and fail fast on mismatch.\n- If upgrade from old nextgen clusters must be supported, add an explicit migration plan (copy+switch+cleanup) rather than silently allowing duplicate schema names.",
      "verify": "- Repro (upgrade simulation): create a store bootstrapped by pre-change nextgen (where `mysql` exists by name with a non-reserved ID), then run the new `bootstrapSessionImpl` path and confirm it does *not* introduce a second `mysql`/`sys` entry.\n- Observability checks: after bootstrap, ensure `meta.ListDatabases()` contains exactly one `mysql` and one `sys` (by name), and `GetSystemDBID()` returns `metadef.SystemDatabaseID` deterministically.\n- Functional check: verify bootstrap variables in `mysql.tidb` are read from the intended schema/table and infoschema has no duplicate-name overwrite.",
      "evidence": "- Nextgen startup always runs system-schema bootstrap: `pkg/session/session.go:3543-3547`.\n- Bootstrap creates `mysql`/`sys` by fixed reserved IDs, only checking existence by *ID*: `pkg/session/bootstrap.go:1191-1230` calls `m.CreateSysDatabaseByIDIfNotExists`, which uses `m.IsDatabaseExist(id)` (ID-only) in `pkg/meta/meta.go:823-834`; nextgen `CreateMySQLDatabaseIfNotExists` also hardcodes `metadef.SystemDatabaseID` and calls the same helper: `pkg/meta/meta.go:806-820`.\n- Meta DB creation only checks ID uniqueness, not name uniqueness: `pkg/meta/meta.go:675-692`.\n- If duplicate DB names happen, the chosen `mysql` DB ID becomes order-dependent: `GetSystemDBID` scans `ListDatabases()` (`pkg/meta/meta.go:849-860`), which iterates unsorted `HGetAll` results (`pkg/meta/meta.go:1392-1407`).\n- InfoSchema explicitly overwrites on duplicate schema name: `pkg/infoschema/infoschema.go:455-460`.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` at `bab4993b6fd61de478062151bb615a0e09c0aecb`: `pkg/meta/meta.go` still hardcodes `metadef.SystemDatabaseID` for nextgen and `CreateSysDatabaseByIDIfNotExists` remains ID-only; nextgen bootstrap still calls `bootstrapSchemas` early in session bootstrap.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-62570.json",
      "worthy_scope": true,
      "summary": "Commit switches nextgen bootstrap to create `mysql`/`sys` schemas and system tables directly via meta KV using reserved IDs tracked by a new `BootTableVersion`. Main risk: nextgen upgrades from clusters where system schemas already exist under non-reserved IDs can create duplicate schema names and lead to infoschema/metadata nondeterminism."
    },
    {
      "pr": 62759,
      "subject": "ddl,parser: modify schema to store the partial condition (#62759)",
      "commit": "4865e394b321ba7439fd51f7c61f46b4b19ff269",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/infoschema",
        "pkg/executor",
        "pkg/parser/ast",
        "pkg/util/dbterror"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Partial primary key condition is inconsistently handled: CREATE TABLE blocks `PRIMARY KEY(...) WHERE ...`, but ALTER TABLE ADD PRIMARY KEY accepts it and then silently drops the predicate because `IndexOption.Condition` isn’t marshaled and `ConditionString` isn’t populated for PK jobs.",
      "impact": "User can bypass the new CREATE TABLE restriction by using `ALTER TABLE ... ADD PRIMARY KEY(...) WHERE ...`; the `WHERE` clause is accepted but silently dropped during job args marshal/unmarshal. This creates a full primary key (or fails due to full-PK constraints) when the user requested a partial condition, and metadata/`SHOW CREATE TABLE` will not reflect the input predicate.",
      "fix": "In `(*executor).CreatePrimaryKey` (ALTER TABLE ADD PRIMARY KEY path), either:\n- Reject partial primary keys consistently: `if indexOption != nil && indexOption.Condition != nil { return dbterror.ErrUnsupportedAddPartialIndex... }` (matching CREATE TABLE behavior), or\n- If partial PK is intended to be supported, compute `ConditionString` via `CheckAndBuildIndexConditionString` and persist it in `ModifyIndexArgs.IndexArgs[*].ConditionString` (plus worker-side restore), similar to `createIndex`.",
      "verify": "Add a regression case (SQL or integration test): `create table t(a int, b int); alter table t add primary key(b) where a = 1;` should return `ErrUnsupportedAddPartialIndex` (or should persist/show the predicate if choosing to support it). Also confirm `SHOW CREATE TABLE t` behavior matches expectation.",
      "evidence": "CREATE TABLE path blocks partial PK: `pkg/ddl/create_table.go:1333`.\nALTER TABLE ADD PRIMARY KEY path does not: `pkg/ddl/executor.go:4554` builds `ModifyIndexArgs` without checking `indexOption.Condition` and without populating `IndexArg.ConditionString`.\n`IndexOption.Condition` is explicitly not marshaled into DDL job args: `pkg/parser/ast/ddl.go:749` (`json:\"-\"`).\nDDL worker restores predicate only from `arg.ConditionString`: `pkg/ddl/index.go:1095` (so PK jobs will silently lose the predicate).",
      "later_fixed": false,
      "later_fix_evidence": "Not fixed up to `upstream/master` (bab4993b6f...): `CreatePrimaryKey` still does not check `indexOption.Condition` or populate `ConditionString` for PK jobs, so the predicate can still be silently lost.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-62759.json",
      "worthy_scope": true,
      "summary": "This commit adds persistence + display of partial index predicates via `model.IndexInfo.ConditionExprString` and plumbs a marshaled `IndexArg.ConditionString` for add-index jobs (surfaced in `SHOW CREATE TABLE` and `information_schema.tidb_indexes.PREDICATE`). High issue: `ALTER TABLE ... ADD PRIMARY KEY(...) WHERE ...` is still accepted but the predicate is silently lost (no `ConditionString` plumbing / no explicit rejection), allowing a bypass of the CREATE TABLE restriction and causing unexpected primary-key semantics."
    },
    {
      "pr": 62762,
      "subject": "ddl,tables: only write the index when it meets partial index condition (#62762)",
      "commit": "8c2781681a46e3b214c6e50be4439114e935365a",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/copr",
        "pkg/meta",
        "pkg/meta/model",
        "pkg/table",
        "pkg/table/tables",
        "pkg/infoschema/perfschema"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Backward-incompatible metadata/DDL-job JSON key rename for partial index condition: `IndexInfo.ConditionExprString` and `IndexArg.ConditionString` changed tag names without a decode shim, so upgrades or mixed-version clusters can silently lose the partial-index predicate.",
      "impact": "Mixed-version rolling upgrade (or upgrading from builds that wrote the old keys) can silently drop the partial-index predicate from metadata/job args. That can cause nodes to disagree on whether an index is partial vs normal, change `SHOW CREATE TABLE` output, break DDL job resume semantics, and in the worst case produce wrong results/constraint behavior if an older node treats a physically-partial index as a full index.",
      "fix": "Keep the original JSON tags for stable storage format, or add backward-compatible decoding: implement `UnmarshalJSON` (or a versioned wrapper) for `model.IndexInfo` and `model.IndexArg` to accept both old and new keys and marshal to a single canonical key. If upgrade compatibility is intended, also populate `IndexInfo.AffectColumn` for pre-existing partial indexes on load (or compute it lazily) so DML/DDL checks behave consistently.",
      "verify": "1) Create a table with a partial index on a binary at the first parent, persist schema to meta, restart on this commit, and confirm the predicate is preserved and enforced. 2) Reverse direction: create on this commit and restart on the first-parent binary; ensure the old binary either rejects the schema or preserves correctness. 3) Simulate rolling upgrade where an old node plans/executes queries against a partial index created by a new node.",
      "evidence": "IndexInfo JSON tag for the predicate is now `condition_expr_string` in `pkg/meta/model/index.go:216` (was `partial_condition_expr_string` in the first parent). DDL job arg tag for the predicate is now `condition_string` in `pkg/meta/model/job_args.go:1374` (was `partial_condition_str` in the first parent). TableInfo is persisted/loaded via `encoding/json` in `pkg/meta/meta.go:737` and `pkg/meta/meta.go:1063`, so tag name changes directly affect on-disk schema decoding. There is no compatibility shim (no `UnmarshalJSON`, no alias field); `git grep partial_condition_expr_string` and `git grep partial_condition_str` return no matches on this commit and on `upstream/master`.",
      "later_fixed": false,
      "later_fix_evidence": "No fix found up to `upstream/master`: the old key strings don’t exist anywhere in `upstream/master`, implying no decode shim was added.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-62762.json",
      "worthy_scope": true,
      "summary": "Reviewed commit vs first parent for DDL/add-index + metadata paths. A separate DML update inconsistency exists in this commit but is fixed later by `245c8e15f2` (PR #65808) and is therefore dropped; the remaining High issue is a backward-incompatible JSON tag rename for partial-index condition fields without any compatibility decoding."
    },
    {
      "pr": 62979,
      "subject": "table: keep `table.Columns` order by states during modifying column (#62979)",
      "commit": "deb4d6563df7ce39eefe2487203024f009a31d38",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/util"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "`removingObjPrefix` rename (`_Tomestone$_` → `_Tombstone$_`) can break mixed-version / rolling-upgrade compatibility for MODIFY COLUMN tombstone name normalization when legacy-prefixed metadata exists.",
      "impact": "Potential rolling-upgrade / owner-handover compatibility break for in-progress MODIFY COLUMN (especially multi-schema with multiple MODIFY COLUMN subjobs where an index can already be in “removing” name form): the new owner may fail to normalize legacy removing index names back to the correct public name, causing user-visible index names with removing prefixes, name conflicts, or DDL job failure/stuck behavior.",
      "fix": "Add backward-compatible handling for the legacy prefix when stripping/recognizing removing objects: treat both `_Tomestone$_` and `_Tombstone$_` as valid removing prefixes in `getRemovingObjName`/`getRemovingObjOriginName` (and any other prefix checks), while generating new names with the corrected `_Tombstone$_` prefix.",
      "verify": "Simulate a multi-schema MODIFY COLUMN where `oldIdxs[i].Name.O` is already legacy-prefixed (`_Tomestone$_...`) and ensure `markOldObjectRemoving` assigns the new/changing index the unprefixed original name and does not double-prefix the old index. Add a targeted unit test around `markOldObjectRemoving`/removing-name helpers covering both prefixes.",
      "evidence": "In parent commit 6095e22d7377, `pkg/ddl/executor.go:83` defines `removingObjPrefix = \"_Tomestone$_\"`, but this commit changes it to `\"_Tombstone$_\"` (`pkg/ddl/executor.go:83`). During MODIFY COLUMN finalization, `markOldObjectRemoving` derives the *public* index name by stripping the removing prefix from `oldIdxName` (`pkg/ddl/column.go:959-972` via `getRemovingObjOriginName(oldIdxName)`), and then sets the old index name by adding the removing prefix (`getRemovingObjName(oldIdxName)`). Both helpers only recognize the *current* `removingObjPrefix` (`pkg/ddl/column.go:1444-1452`). If metadata contains legacy `_Tomestone$_...` names (e.g., from an in-flight multi-schema MODIFY COLUMN or owner handover across versions), the legacy prefix will not be stripped/recognized, producing wrong public names (still prefixed) and potentially double-prefixed tombstones.",
      "later_fixed": false,
      "later_fix_evidence": "Checked later history up to `upstream/master`: `removingObjPrefix` was refactored into `pkg/meta/model/column.go` but still uses only `_Tombstone$_`; no `_Tomestone$_` compatibility found (e.g. `git grep '_Tomestone\\$_' upstream/master` is empty).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-62979.json",
      "worthy_scope": true,
      "summary": "The commit improves MODIFY COLUMN metadata invariants by ensuring `TableInfo.Columns` remains ordered by schema states (via `moveOldColumnInfo`) and by updating `TableInfo.MoveColumnInfo` to also maintain `ChangeStateInfo.DependencyColumnOffset` across column moves, with added multi-schema test coverage. One High compatibility risk remains: renaming the internal “removing/tombstone” name prefix may break name normalization when legacy-prefixed metadata is encountered during rolling upgrade/owner handover."
    },
    {
      "pr": 63397,
      "subject": "disttask: support metering on next-gen (#63397)",
      "commit": "b3011b689de0a646e8d4683b37c414c0499d258a",
      "modules": [
        "pkg/ddl",
        "pkg/domain",
        "pkg/disttask/framework/metering",
        "pkg/config"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Potential nil-pointer panic on metering initialization: `InitDistTaskLoop` assumes `metering.NewMeter` never returns `nil` on success, but `NewMeter` can return `(nil, nil)` for configs with empty bucket (reachable via valid-looking URIs), and the code still starts the flush loop on `m`.",
      "impact": "A non-empty but unsupported/malformed `metering-storage-uri` (e.g. `localfs:///...` or `s3:///prefix`) can trigger a nil-pointer panic in the metering flush goroutine, crashing the TiDB process and preventing DXF/next-gen DDL disttasks from running.",
      "fix": "In `(*Domain).InitDistTaskLoop`, handle `m == nil` before `SetMetering`/starting the flush loop (warn + disable metering, or return a configuration error). Optionally: make `metering.NewMeter` return a non-nil error for empty/unsupported configs, and/or validate `Config.MeteringStorageURI` in `(*Config).Valid()`.",
      "verify": "Run next-gen TiDB with SYSTEM keyspace DXF enabled and set `metering-storage-uri` to `localfs:///tmp/metering` (or `s3:///prefix`), then start TiDB and confirm it does not panic; expect either a clear startup error or a warning and metering disabled.",
      "evidence": "`pkg/domain/domain.go:1095-1106` parses `MeteringStorageURI`, calls `metering.NewMeter(mCfg)` and unconditionally runs `m.StartFlushLoop(do.ctx)` in a background goroutine.\n`pkg/disttask/framework/metering/metering.go:137-141` returns `(nil, nil)` when `cfg.Type` or `cfg.Bucket` is empty.\n`github.com/pingcap/metering_sdk/config.NewFromURI` (the function being called) supports `localfs://`/`file://` URIs which have no bucket, and also accepts cloud URIs with empty host (bucket) without error, making `m == nil` reachable.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` at `bab4993b6f...`: `pkg/domain/domain.go` still calls `m.StartFlushLoop(do.ctx)` without guarding `m != nil`; `pkg/dxf/framework/metering/metering.go` still returns `(nil, nil)` when `cfg.Bucket` is empty, so the crash condition still appears possible.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-63397.json",
      "worthy_scope": true,
      "summary": "DDL add-index metering hooks are additive and the `writeChunk` KV-byte accounting fix looks correct, but DXF metering bootstrap can crash TiDB if `metering-storage-uri` parses to a config with an empty bucket because `InitDistTaskLoop` starts the flush loop on a potentially-nil `*Meter`."
    },
    {
      "pr": 63901,
      "subject": "ddl: find old column by name for old version modify column job (#63901)",
      "commit": "fa42e905f4f0fd19d47828518a459ce991fe6b2b",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "`getModifyColumnInfo` can misidentify the target column when `OldColumnID` is missing: it now looks up `_Tombstone$_<oldName>` before `<oldName>`, so tables that already contain a `_Tombstone$_<oldName>` column can cause `MODIFY COLUMN <oldName>` to operate on the wrong column.",
      "impact": "If a table contains a user column named `_Tombstone$_<col>` (or a leftover internal tombstone column unrelated to the current job), `ALTER TABLE ... MODIFY COLUMN <col>` can pick the wrong `oldCol` on the first execution (when `OldColumnID` is still 0). This can lead to modifying the wrong column, DDL failure, or persisting inconsistent table metadata (e.g. duplicate column names) depending on the modify-column path.",
      "fix": "Disambiguate the “old column by name” fallback:\n- Prefer original-name lookup for early phases (e.g. `job.SchemaState != model.StatePublic`), and only prefer removing-name lookup when the job is expected to have already renamed the old column (e.g. `job.SchemaState == model.StatePublic`).\n- Additionally/alternatively, when the removing-name lookup hits, require a non-`StatePublic` column (internal removing column) before accepting it; otherwise fall back to the original-name column.",
      "verify": "Add an integration test that creates `t(a INT, _Tombstone$_a INT)` and runs both:\n- a no-reorg `MODIFY COLUMN a` (e.g. metadata-only change) and assert `a` changes while `_Tombstone$_a` is unchanged;\n- a reorg-required `MODIFY COLUMN a` and assert the job doesn’t mis-target `_Tombstone$_a` or produce invalid metadata.",
      "evidence": "`pkg/ddl/modify_column.go:234` now prefers `model.FindColumnInfo(..., getRemovingObjName(args.OldColumnName.L))` when `args.OldColumnID == 0`, only falling back to the original name lookup.\n`pkg/ddl/modify_column.go:1124` shows new modify-column jobs don’t initialize `OldColumnID`, so the first execution always enters this name-based path.\n`pkg/parser/misc.go:24` includes `$` in `isIdentChar`, so identifiers like `_Tombstone$_a` are syntactically valid (and there’s no repo-wide guard/validation for this prefix; `_Tombstone$_` only appears as `pkg/ddl/executor.go:85`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` at `bab4993b6fd61de478062151bb615a0e09c0aecb`: `getModifyColumnInfo` still prefers removing-name lookup first when `OldColumnID` is missing (same risk remains).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-63901.json",
      "worthy_scope": true,
      "summary": "Commit adds a compatibility fallback for old modify-column jobs missing `OldColumnID` by searching the removing-name (`_Tombstone$_<col>`) first, plus an integration test and minor refactors (reorg-timeout helper; defaulting/logging when certain session vars are absent; a test-only job-args mutator). One High-risk edge case remains: the removing-name-first lookup can mis-target user schemas that already contain `_Tombstone$_<col>` columns."
    },
    {
      "pr": 63970,
      "subject": "modify column: support ingest/DXF mode to recreate indexes (#63970)",
      "commit": "4011c9f6c56da1f2bc515d72c0ae95a6d542be40",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/ingest",
        "pkg/meta/model"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Rolling-upgrade incompatibility: in-flight `MODIFY COLUMN` reorg jobs from pre-#63970 can get stuck because `AnalyzeState` moved into `job.ReorgMeta` and a new persisted `Stage` gate was introduced, but there is no migration/default handling for missing `reorg_meta.stage/analyze_state`.",
      "impact": "Rolling upgrade / owner switch compatibility regression: an in-flight `ActionModifyColumn` job created by the first-parent code and already in `StateWriteReorganization` can become permanently stuck after upgrading to this commit (no reorg, no analyze, no state advancement). This can leave the table in an intermediate schema state (hidden changing column/indexes present) and require manual intervention (cancel/retry), with potential prolonged performance/behavior impact while stuck.",
      "fix": "- Add a compatibility shim so in-flight jobs from pre-#63970 can continue:\n  - In `pkg/ddl/modify_column.go` (and any other modify-column reorg entrypoint), treat `job.ReorgMeta.Stage==model.ReorgStageNone` as a safe default (likely `ReorgStageModifyColumnUpdateColumn`), or infer stage from `mysql.tidb_ddl_reorg.ele_type` (column vs index element) when `Stage==0`.\n  - Consider decoding legacy top-level `analyze_state` into `job.ReorgMeta.AnalyzeState` (custom decode or a deprecated alias field) so ongoing jobs don’t lose analyze progression during rolling upgrade.\n- Ensure the shim is idempotent and only triggers when `Stage==0` / legacy fields present.",
      "verify": "- Repro (rolling upgrade): on the first parent (`2c25d745`), start a `MODIFY COLUMN` that requires reorg (e.g., type change with indexes) and wait until `ADMIN SHOW DDL JOBS` shows `schema_state=write reorganization`. Upgrade owner to this commit and observe whether the job continues advancing rowcount/state.\n- Static check: confirm `job.ReorgMeta.Stage` can be 0 for pre-#63970 jobs (no JSON field), and that `StateWriteReorganization` path makes progress when stage/analyze are missing (after adding shim).",
      "evidence": "- `pkg/ddl/modify_column.go` (`doModifyColumnTypeWithData`, `StateWriteReorganization`): control flow is now `switch job.ReorgMeta.AnalyzeState { case None: switch job.ReorgMeta.Stage { UpdateColumn/RecreateIndex/Completed } }` with **no default / no `ReorgStageNone` handling**. If `Stage==0`, the switch does nothing and the function returns `nil`, leaving the job stuck in `StateWriteReorganization`.\n- `pkg/meta/model/reorg.go`: this PR introduces `DDLReorgMeta.Stage` (with `ReorgStageNone=0`) and `DDLReorgMeta.AnalyzeState`.\n- `pkg/meta/model/job.go`: this PR removes the top-level `Job.AnalyzeState` field (previously `json:\"analyze_state\"`), and `Job.Decode` is still a plain `json.Unmarshal` (no migration).\n- First-parent behavior (pre-PR) confirms incompatibility: `2c25d745:pkg/ddl/modify_column.go` drives the reorg/analyze progression via `job.AnalyzeState` only (no stage persisted), so an in-flight modify-column job from the first parent can exist with `SchemaState=WriteReorganization` but without `reorg_meta.stage/analyze_state` fields; on this commit it deserializes as `Stage=0/AnalyzeState=0` and then no-ops forever.",
      "later_fixed": false,
      "later_fix_evidence": "Searched up to `upstream-master`: no handling of `ReorgStageNone` / `Stage==0` found (only the constant definition in `pkg/meta/model/reorg.go`), and `pkg/meta/model/job.go` `Decode` remains a plain `json.Unmarshal` without migrating legacy `analyze_state` into `reorg_meta`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-63970.json",
      "worthy_scope": true,
      "summary": "Adds staged modify-column reorg to allow index recreation via ingest/DXF (`ReorgMeta.Stage` + `ReorgMeta.AnalyzeState`, temp-index rollback IDs, reuse create-index backfill). Main risk found is rolling-upgrade compatibility: pre-existing modify-column jobs in `write reorganization` can deserialize with missing stage/analyze and then no-op indefinitely."
    },
    {
      "pr": 64096,
      "subject": "ddl: add timeout mechanism for add index analyze (#64096)",
      "commit": "6dc2c5b63fe0db8b815e929fc41afdcaae45daf1",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/executor",
        "tests/realtikvtest"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`AnalyzeStateTimeout` (value 4) is persisted into DDL job metadata but first-parent code has no handler/default for this new value. During mixed-version rolling upgrades, if ownership falls back to an older TiDB while an add-index/modify-column job is between “set AnalyzeStateTimeout” and “publish/finalize”, the older owner can’t advance the state machine and the DDL job can get stuck.",
      "impact": "Mixed-version rolling upgrade risk: if a newer owner writes `AnalyzeStateTimeout` (4) to an in-flight add-index/modify-column job, and ownership later moves to an older TiDB binary (pre-#64096), that older owner will not advance the job state machine. The DDL job can become stuck in `StateWriteReorganization`, potentially blocking subsequent DDLs and causing prolonged schema-change unavailability.",
      "fix": "Avoid introducing new persisted `AnalyzeState` values that older binaries can’t handle during rolling upgrade. Options:\n- Encode timeout as a new JSON field (unknown fields are ignored by older versions) while keeping `AnalyzeState` in the existing set (e.g., `Skipped`), and drive `SHOW DDL JOBS` labeling from the new field.\n- Or, if keeping `AnalyzeStateTimeout`, ensure timeout transitions don’t require another DDL round (finish/publish within the same round) so there’s no window where an in-queue job has `analyze_state=4` but still needs processing.\n- Additionally, add a `default` branch in the analyze-state switches to treat unknown states as terminal (helps forward-compat within newer binaries).",
      "verify": "Reproduce with a mixed-version scenario:\n1) On new binary, enable `tidb_enable_ddl_analyze=1` and force timeout (e.g., via failpoint/sleep as in `pkg/ddl/index_modify_test.go`). Let the job persist `analyze_state=4` while still in `StateWriteReorganization`.\n2) Switch DDL ownership to an older binary (pre-#64096).\n3) Observe the job no longer progresses (no matching `AnalyzeState` case), remaining in the queue indefinitely until manually intervened.",
      "evidence": "- New persisted enum value: `pkg/meta/model/reorg.go` adds `AnalyzeStateTimeout = 4`.\n- New state is written mid-job: `pkg/ddl/index.go` and `pkg/ddl/modify_column.go` set `job.ReorgMeta.AnalyzeState = model.AnalyzeStateTimeout` when `analyzeTableAfterCreateIndex()` reports `timedOut`, then only publish schema in a later pass when `AnalyzeStateTimeout` is handled as a terminal state.\n- First-parent code doesn’t recognize value 4 and has no default: `git show 6dc2c5b63f^:pkg/ddl/index.go` and `git show 6dc2c5b63f^:pkg/ddl/modify_column.go` inner `switch job.ReorgMeta.AnalyzeState` only covers `None/Running/Done/Skipped` (no `default`). If `AnalyzeState==4`, the switch body is skipped and the job doesn’t advance.\n- This state is stored in DDL job metadata: `pkg/meta/model/reorg.go` defines `DDLReorgMeta.AnalyzeState int8 \\`json:\"analyze_state\"\\`` and `pkg/meta/model/job.go` proxies/copies `ReorgMeta.AnalyzeState` into persisted job JSON, so an older TiDB owner will read `analyze_state=4` from `mysql.tidb_ddl_job`.",
      "later_fixed": false,
      "later_fix_evidence": "",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-64096.json",
      "worthy_scope": true,
      "summary": "Adds a cumulative-timeout path for the embedded post-reorg `ANALYZE TABLE` in add-index/modify-column DDL, including a new persisted `AnalyzeStateTimeout` state and logic to avoid re-running analyze across owner changes. Main High-risk concern is mixed-version compatibility: older owners can’t handle the new persisted analyze state value and may strand in-flight DDL jobs."
    },
    {
      "pr": 64111,
      "subject": "ddl: add unit test for lossy column change (#64111)",
      "commit": "427b89168597c6c50adc69c6641338e7e69577d0",
      "modules": [
        "pkg/ddl",
        "pkg/table/tables"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Potential nil-pointer panic in `worker.onModifyColumn`: deferred `failpoint.InjectCall` dereferences `args` even when `GetModifyColumnArgs` returns an error (args can be `nil`).",
      "impact": "If a modify-column job’s args cannot be decoded (corrupted job, version incompatibility, etc.), this turns a handled “cancel job and return error” path into a panic. The scheduler recovers panics and re-pends unfinished jobs, so this can devolve into repeated panics and a stuck DDL job instead of a clean cancellation.",
      "fix": "Move the `defer` below the `err` check, or guard it:\n- After `if err != nil { ... }`, add `defer failpoint.InjectCall(\"getModifyColumnType\", args.ModifyColumnType)`.\n- Or in the deferred func: `if args != nil { failpoint.InjectCall(...) }`.",
      "verify": "Add/adjust a unit test that constructs a `model.Job` for modify-column with invalid/undecodable `RawArgs` and asserts `onModifyColumn` cancels/returns error without panicking (e.g., wrap call with `require.NotPanics`). Also sanity-check that the `getModifyColumnType` InjectCall still fires for the intended test cases when args decode succeeds.",
      "evidence": "`pkg/ddl/modify_column.go:251-258` registers `defer func(){ failpoint.InjectCall(\"getModifyColumnType\", args.ModifyColumnType) }()` before checking `err` from `model.GetModifyColumnArgs(job)`.\n`pkg/meta/model/job_args.go:62-67` returns zero `T` on decode error; for `T=*ModifyColumnArgs` (see `pkg/meta/model/job_args.go:1774-1776`) this can be `nil`, so `args.ModifyColumnType` can panic.\nParent version (`427b891^`) invoked `failpoint.InjectCall` only after successful args decode and type computation (`pkg/ddl/modify_column.go` in parent around `failpoint.InjectCall(\"getModifyColumnType\", args.ModifyColumnType)`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master:pkg/ddl/modify_column.go` and the same `defer` before the `err` check is still present around `onModifyColumn` (i.e., not fixed later).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-64111.json",
      "worthy_scope": true,
      "summary": "Reviewed commit diff vs first parent (DDL/modify-column + tables). Found one High issue: a new deferred failpoint hook in `onModifyColumn` can nil-deref on args decode failure, converting a clean cancel path into a panic; this is still present on `upstream/master`."
    },
    {
      "pr": 64171,
      "subject": "ddl: add checkpoint operator for DXF local add index (#64171)",
      "commit": "f2abf53fab601b3a466c5ac686643f0508a3d687",
      "modules": [
        "pkg/ddl",
        "pkg/ddl/ingest",
        "pkg/disttask/framework",
        "pkg/disttask/framework/storage"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Regression in checkpoint initialization: when a checkpoint object exists but has `ts=0` (the default initial reorg_meta state), `CheckpointManager` now returns early and never allocates/persists an initial ingest TS. This undermines the intended failover/idempotency behavior around the first import and delays persisting `instance_addr`.",
      "impact": "For normal (non-DXF) local ingest add-index, the reorg table always contains a checkpoint object from initialization, so `CheckpointManager` can keep `ts=0` for a long time (until first flush/import). The first import then picks a TS inside Lightning (because `ts==0`) but that TS is not persisted in DDL checkpoint storage.\nIf the job/subtask fails over/crashes around the first import boundary (before the checkpoint write that would otherwise persist TS), a retry can choose a different TS for the same imported KV range, breaking the intended idempotency/failover stability and increasing risk of duplicate MVCC versions / inconsistent import semantics under retries.\nAlso prolongs the `instance_addr==\"\"` “initial state” window, weakening the intended guard that local sort dir reuse is tied to the producing instance.",
      "fix": "In `CheckpointManager.resumeOrInitCheckpoint`, after applying a loaded checkpoint, if `s.ts == 0` (or `cp.TS == 0`) allocate a PD TS and persist it via `updateCheckpointImpl()` before returning. This restores the pre-existing invariant that a non-zero, persisted “next ingest TS” exists before the first import and also persists `instance_addr` early.\nAlternative: make `NormalCheckpointStorage.LoadCheckpoint` treat checkpoints with `TS==0` as “not initialized” (return nil) so the existing TS-allocation branch runs.",
      "verify": "Add a unit test that inserts `mysql.tidb_ddl_reorg.reorg_meta` using the same shape as `initDDLReorgHandle` (checkpoint object present but `ts==0`), then constructs `NewCheckpointManager` and asserts:\n- `GetImportTS()` becomes non-zero immediately\n- storage row is updated with non-zero `ts` and non-empty `instance_addr`.\nManual/behavioral: simulate a crash after the first `SetTSBeforeImportEngine` but before checkpoint update; on resume, verify the same TS is reused (no re-import TS drift).",
      "evidence": "`pkg/ddl/ingest/checkpoint.go:492` loads cp and returns early on `cp != nil` at `pkg/ddl/ingest/checkpoint.go:498`, so the TS allocation/persist path at `pkg/ddl/ingest/checkpoint.go:527` is skipped whenever a checkpoint object exists.\nInitial reorg meta inserts a checkpoint object without setting `ts`/`instance_addr` (defaults `ts=0`, `instance_addr=\"\"`) in `pkg/ddl/job_scheduler.go:779`.\nImport TS is consumed for every import via `pkg/ddl/ingest/backend.go:281` -> `SetTSBeforeImportEngine`, which auto-allocates a TS when given 0 (`pkg/lightning/backend/local/local.go:1644`, `pkg/lightning/backend/local/local.go:1650`), defeating the “persist chosen TS before import/failover” intent.\nNot fixed later: upstream/master still has the same early-return logic (`pkg/ddl/ingest/checkpoint.go:504` on upstream/master f0813a369666ac6314e76f5e573e982fe50c8a09).",
      "later_fixed": false,
      "later_fix_evidence": "Checked later history up to upstream/master f0813a369666ac6314e76f5e573e982fe50c8a09; `pkg/ddl/ingest/checkpoint.go:504` still returns early on non-nil checkpoint without allocating/persisting TS when `ts==0`. `pkg/ddl/job_scheduler.go:803` still initializes the checkpoint without TS.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-64171.json",
      "worthy_scope": true,
      "summary": "Adds a checkpoint storage abstraction so DXF local add-index can persist checkpoints in the disttask subtask table, and wires the read-index local pipeline to use it. Main concern: in the normal (reorg-table) path, checkpoint initialization now returns early when a checkpoint object exists, so an initial `ts=0` checkpoint never gets a persisted non-zero ingest TS, weakening first-import failover/idempotency semantics."
    },
    {
      "pr": 64337,
      "subject": "ddl, model: fix unexpected missing analyze for multi schema change (#64337)",
      "commit": "16a5fff9fec25f6ac2b4951138348bfc8f16fff7",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/executor"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Potential nil dereference introduced in the new multi-schema-change embedded-analyze path: `onMultiSchemaChange` now always calls `doAnalyzeWithoutReorg`, which assumes `job.ReorgMeta` is non-nil, but upgrade-compat initialization only runs on reorg/background workers; MSC jobs can be dispatched to the general worker (`reorg=0`).",
      "impact": "Compatibility/state-machine risk: an in-flight `ActionMultiSchemaChange` job decoded from an older TiDB version without `reorg_meta` (so `job.ReorgMeta == nil`) can panic when it reaches the new analyze gate. This is most acute when the job is dispatched to the general worker (`reorg=0`), because the existing upgrade-compat initialization does not run there. Result: DDL owner step panic / job stuck / upgrade instability.",
      "fix": "Add a nil-guard before using `job.ReorgMeta` in the new MSC analyze path, e.g.:\n- Initialize in `doAnalyzeWithoutReorg`: `if job.ReorgMeta == nil { job.ReorgMeta = &model.DDLReorgMeta{} }`.\n- Or initialize in `onMultiSchemaChange` before calling `doAnalyzeWithoutReorg`.\n- Or broaden the existing upgrade-compat init in `runOneJobStep` to also cover `ActionMultiSchemaChange` even on `generalWorker`.",
      "verify": "Minimal repro (unit-level): construct a `model.Job{Type: ActionMultiSchemaChange, MultiSchemaInfo: ..., ReorgMeta: nil}` and drive it to the code path where revertible sub-jobs are done; confirm `onMultiSchemaChange` panics at `doAnalyzeWithoutReorg`.\nUpgrade-level: restore a job record produced by a TiDB build without `reorg_meta` into `mysql.tidb_ddl_job` with `reorg=0`, run the upgraded owner, and observe whether it hits the MSC analyze gate without crashing after applying the nil-guard.",
      "evidence": "`pkg/ddl/multi_schema_change.go:84` calls `w.doAnalyzeWithoutReorg(job, tblInfo)` unconditionally once revertible sub-jobs reach the last revertible point.\n`pkg/ddl/index.go:1407` `doAnalyzeWithoutReorg` immediately dereferences `job.ReorgMeta` (`switch job.ReorgMeta.AnalyzeState`) with no nil-guard.\n`pkg/ddl/job_worker.go:939` only initializes `job.ReorgMeta` on upgrade when `w.tp` is `addIdxWorker` or `backgroundWorker`.\n`pkg/ddl/job_scheduler.go:419` dispatches jobs with `reorg=0` to the general worker pool.\n`pkg/meta/model/job.go:772` `Job.MayNeedReorg()` can return `false` for `ActionMultiSchemaChange` (so `reorg=0` is possible).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` at `f0813a369666ac6314e76f5e573e982fe50c8a09`: `pkg/ddl/multi_schema_change.go` still calls `doAnalyzeWithoutReorg` without a nil-guard; `pkg/ddl/index.go` still dereferences `job.ReorgMeta` in `doAnalyzeWithoutReorg`; `pkg/ddl/job_worker.go` still only initializes `ReorgMeta` for `addIdxWorker`/`backgroundWorker`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-64337.json",
      "worthy_scope": true,
      "summary": "Moves DDL analyze for multi-schema-change off sub-jobs (removing non-persisted `NeedAnalyze`) and into the parent MSC job using persisted `ReorgMeta.AnalyzeState`; refactors modify-column temporary naming helpers into `pkg/meta/model` and replaces `CtxVars` with `NeedReorg` for modify-column reorg signaling."
    },
    {
      "pr": 64356,
      "subject": "ddl: add auto ID rebase create table submitted by BR (#64356)",
      "commit": "d059a7de1b1a1c4f06c0fd39bd832b6048f6177f",
      "modules": [
        "pkg/ddl",
        "pkg/meta",
        "pkg/meta/autoid",
        "pkg/infoschema"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`autoIDAccessor.CopyTo` now treats `curr==0` as “nothing to copy”, but 0 can be an explicitly stored value (not just “missing”). This can break sequence RESTART semantics across schema transfers: after `ALTER SEQUENCE ... RESTART WITH 1` (base becomes 0) and a subsequent rename back to a schema with a stale non-zero sequence value, the stale value can survive because the explicit 0 is not copied.",
      "impact": "Sequences can silently lose a `RESTART WITH 1` (base=0) across a rename-back to a schema that still has a stale non-zero `sequenceKey` value: `CopyTo` skips copying the explicit 0, `Transfer` still switches allocator to the destination schema, and the destination’s stale value can be used. This violates DDL semantics and can break applications relying on sequence restart behavior; diagnosis is difficult because the rename completes successfully.",
      "fix": "Change `autoIDAccessor.CopyTo` to skip only when the *source field is absent*, not when the parsed value equals 0. Use `m.txn.HGet(srcDBKey, srcField)` to distinguish `nil` (missing/deleted) from `[]byte(\"0\")` (explicit), and `HSet` the raw bytes to the destination when non-nil. This preserves the BR cross-version safety (avoid overwriting target when source was deleted) while keeping explicit-zero semantics (notably sequence restart).",
      "verify": "SQL repro (schema names arbitrary):\n1) `create database a; create database b;`\n2) `create sequence a.seq start with 1 increment by 1; select nextval(a.seq);` (make prior value non-zero)\n3) `rename table a.seq to b.seq;`\n4) `alter sequence b.seq restart with 1;` (persists base=0)\n5) `rename table b.seq to a.seq;`\n6) `select nextval(a.seq);` should reflect the restart (start again), not continue from the stale pre-restart value.",
      "evidence": "`pkg/meta/meta_autoid.go:85` `autoIDAccessor.CopyTo` now does `curr := a.Get()` and returns early when `curr == 0` (`pkg/meta/meta_autoid.go:99`).\n`a.Get()` uses `HGetInt64`, which returns `0` both when the field is missing and when it is explicitly stored as \"0\" (`pkg/structure/hash.go:85`).\nRename/apply-diff path calls `alloc.Transfer(...)` (`pkg/infoschema/builder.go:836`), which invokes `CopyTo` and then unconditionally switches allocator to the destination `(dbID, tableID)` on success (`pkg/meta/autoid/autoid.go:328`).\nFor sequences, `Transfer` uses the `SequenceValue()` accessor (`pkg/meta/autoid/autoid.go:1183`). `ALTER SEQUENCE ... RESTART WITH 1` persists `sequenceBase = restartValue - 1 = 0` (`pkg/ddl/sequence.go:292`, stored via `HSet` in `pkg/meta/meta.go:943`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `git log d059a7d..upstream/master -- pkg/meta/meta_autoid.go` (no follow-up changes touching this logic).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-64356.json",
      "worthy_scope": true,
      "summary": "Adds BR-mode post-create auto ID rebasing in the DDL executor (for CreateTableWithInfo/BatchCreateTableWithInfo), adds failpoint-based tests to assert rebase call counts, and changes `AutoIDAccessor.CopyTo` to skip copying zero values to avoid overwriting downstream auto IDs during cross-version BR restore. The zero-skip in `CopyTo` appears to introduce a high-severity regression for explicitly-stored 0 values (notably sequence restart) during schema-transfer/rename paths."
    },
    {
      "pr": 64584,
      "subject": "add-index/import-into: meter row and kv size data and disable some options for import-into in nextgen (#64584)",
      "commit": "6eeb23e31753a026b1d241f2de7db0623518e35b",
      "modules": [
        "pkg/ddl",
        "pkg/disttask/framework/handle",
        "pkg/disttask/framework/metering",
        "pkg/disttask/importinto",
        "pkg/executor/importer"
      ],
      "severity": "High",
      "confidence": "Medium",
      "what": "IMPORT INTO cleanup can become effectively non-retriable if metering send fails: `CleanUp` returns error after deleting sorted data, but still redacts `Plan.CloudStorageURI` (masking credential query params). On the next cleanup retry, the redacted CloudStorageURI may no longer authenticate to the object store, preventing cleanup from reaching metering again and leaving finished tasks stuck in active task tables.",
      "impact": "If `Plan.CloudStorageURI` includes credential query parameters, a transient metering write failure can strand a succeeded IMPORT INTO task in the cleanup retry loop: task never transfers to history, cleanup may become permanently unable to access the object store (due to redaction), and row/kv-size metering may never be emitted. This can lead to persistent rows in `mysql.tidb_global_task`/`mysql.tidb_background_subtask` and repeated cleanup overhead.",
      "fix": "Make cleanup retriable when metering fails: avoid redacting `Plan.CloudStorageURI` until after both file cleanup and metering succeed (don’t `defer` the CloudStorageURI redaction), or ensure retries can reconstruct the real CloudStorageURI from config/system var instead of task meta. If metering must not block GC, treat metering send as best-effort (log + proceed to `TransferTasks2History`) and add a separate retry mechanism for metering.",
      "verify": "Run an IMPORT INTO where `tidb_cloud_storage_uri` (or the generated `Plan.CloudStorageURI`) contains `access-key`/`secret-access-key` params; force `handle.SendRowAndSizeMeterData`/metering writer to return an error (e.g., misconfigure metering storage or inject a metering write failure). Observe: first cleanup deletes sorted data, then fails on metering; task meta is redacted; subsequent cleanup attempts fail to access cloud storage using the redacted URI and the task stays in active task tables.",
      "evidence": "`pkg/disttask/importinto/clean_up.go:59` defers `redactSensitiveInfo(task, taskMeta)` even when `CleanUp` returns an error later; after deleting sorted data, cleanup sends metering and returns the metering error (`pkg/disttask/importinto/clean_up.go:86`, `pkg/disttask/importinto/clean_up.go:91`). Redaction masks S3 credentials via `ast.RedactURL` (`pkg/disttask/importinto/scheduler.go:697`; masking behavior at `pkg/parser/ast/misc.go:3789`, especially `pkg/parser/ast/misc.go:3806`). Cleanup retries require recreating the sort store from `Plan.CloudStorageURI` (`pkg/executor/importer/import.go:1189`), but the scheduler cleanup loop keeps tasks in active tables when cleanup returns error (`pkg/disttask/framework/scheduler/scheduler_manager.go:430`). Metering errors propagate from `SendRowAndSizeMeterData` (`pkg/disttask/framework/handle/handle.go:402`).",
      "later_fixed": false,
      "later_fix_evidence": "",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-64584.json",
      "worthy_scope": true,
      "summary": "Adds nextgen metering for add-index (from succeeded read-index subtask summaries) and IMPORT INTO (from post-process checksum meta) by writing row_count and KV byte sizes during scheduler cleanup; introduces `handle.SendRowAndSizeMeterData` + metering writer overwrite semantics; and disallows `checksum_table`/`record_errors` options for IMPORT INTO in nextgen."
    },
    {
      "pr": 65049,
      "subject": "ddl: sync affinity group for pd and support `show affinity` (#65049)",
      "commit": "a14f0c8f22f6ba190039dd175717b58849992f1a",
      "modules": [
        "pkg/ddl",
        "pkg/domain/affinity",
        "pkg/domain/infosync",
        "pkg/infoschema/context",
        "pkg/executor",
        "pkg/planner/core"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`SHOW AFFINITY` bypasses database/table privilege visibility checks and can expose affinity-enabled objects (and PD topology/state) to unprivileged users.",
      "impact": "Information disclosure: any user can enumerate DB/table/partition names with affinity across the cluster and observe PD affinity state (leader/voter store IDs), violating expected SHOW visibility semantics and leaking topology/metadata.",
      "fix": "Add privilege filtering in `fetchShowAffinity`: for each candidate (db, table), skip DBs failing `checker.DBIsVisible(activeRoles, db)` and skip tables failing `checker.RequestVerification(activeRoles, db, table, \"\", mysql.AllPrivMask&(^mysql.CreateTMPTablePriv))`. Optionally require a stricter global privilege (e.g. `PROCESS`/`SUPER`) before returning PD topology fields (leader/voter store IDs).",
      "verify": "Add tests creating multiple DBs/users/roles: ensure a user without DB/table privileges (or DB not visible) sees no rows for those objects via `SHOW AFFINITY`; if restricting topology fields, verify those columns are NULL/empty unless required privilege is granted.",
      "evidence": "`pkg/executor/show_affinity.go:31` builds results by iterating `domain.GetDomain(e.Ctx()).InfoSchema().ListTablesWithSpecialAttribute(infoschemacontext.AffinityAttribute)` (`pkg/executor/show_affinity.go:45`) and appending rows without any visibility/privilege checks. Unlike other SHOW implementations that filter by `DBIsVisible` + `RequestVerification` (e.g. `pkg/executor/show.go:443`, `pkg/executor/show.go:570`), `SHOW AFFINITY` has no calls to `privilege.GetPrivilegeManager`. Planner also adds no `visitInfo` for `ast.ShowAffinity` (`pkg/planner/core/planbuilder.go:3630`). Later history: no commits touch `pkg/executor/show_affinity.go` up to `upstream/master` (`git log a14f0c8..upstream/master -- pkg/executor/show_affinity.go` is empty).",
      "later_fixed": false,
      "later_fix_evidence": "No later changes up to `upstream/master`: `git log a14f0c8..upstream/master -- pkg/executor/show_affinity.go` returns empty.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-65049.json",
      "worthy_scope": true,
      "summary": "This commit implements PD affinity-group lifecycle sync during DDL (create/alter/truncate/drop/database drop) and adds `SHOW AFFINITY`. One remaining High issue: the new `SHOW AFFINITY` path lists affinity tables across all schemas and exposes PD affinity state without any DB/table visibility or privilege filtering."
    },
    {
      "pr": 65133,
      "subject": "ddl: support region split policy (#65133)",
      "commit": "35d50f2b1277c3eb6280e84d923a46fb2a627a4d",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Persistent region split policy accepts arbitrarily large `REGIONS` values (no `SplitRegionMaxNum` cap), enabling unbounded split-key generation and potential DoS/stability issues.",
      "impact": "A user with DDL privileges can bypass the existing `SplitRegionMaxNum` safety limit by using the new persistent split policy syntax (CREATE/ALTER TABLE ... SPLIT ... REGIONS N). With a large enough `(upper-lower)` range, this can drive unbounded key generation (`O(N)` allocations/CPU) and huge split requests, potentially causing severe memory pressure/OOM, long stalls, or cluster instability (and it may happen asynchronously when `tidb_scatter_region` is off).",
      "fix": "Enforce an upper bound for `splitOpt.SplitOpt.Num` in `normalizeSplitPolicy` using `config.GetGlobalConfig().SplitRegionMaxNum` (same as `SPLIT REGION` statements). Consider a defensive runtime guard in `applySplitPoliciesForTable` as well (reject or clamp) to protect against corrupted metadata.",
      "verify": "Add/extend integration tests to assert `CREATE TABLE ... /*T![region_split]*/ SPLIT ... REGIONS <SplitRegionMaxNum+1>` and `ALTER TABLE ... SPLIT ... REGIONS <SplitRegionMaxNum+1>` fail with a clear error, and that values at/below the limit still succeed (both table-level and index-level policies). Also validate that large-but-valid ranges do not trigger excessive key generation after the fix.",
      "evidence": "`pkg/ddl/split_region.go:365` only rejects `REGIONS < 1` in `normalizeSplitPolicy` and does not enforce the existing `SplitRegionMaxNum` cap; the resulting `policy.Regions` is later used directly to generate split keys via `regionsplit.GetSplitTableKeys(..., int(policy.Regions), ...)` and `regionsplit.GetSplitIndexKeys(..., int(policy.Regions), ...)` (`pkg/ddl/split_region.go:259`, `pkg/ddl/split_region.go:309`). Key generation ultimately loops `num-1` times and allocates per iteration in `util.GetValuesList` (`pkg/util/split.go:24`, `pkg/util/split.go:30`). By contrast, the existing `SPLIT TABLE/INDEX ... REGIONS` statement path enforces `SplitRegionMaxNum` in the planner (`pkg/planner/core/planbuilder.go:5178`, `pkg/planner/core/planbuilder.go:5276`).",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` at `f0813a369666ac6314e76f5e573e982fe50c8a09`: `pkg/ddl/split_region.go` still contains no `SplitRegionMaxNum` enforcement for `normalizeSplitPolicy` (`git show upstream/master:pkg/ddl/split_region.go | rg SplitRegionMaxNum` returns nothing). No intervening commits touched the relevant files (`git log 35d50f2b..upstream/master -- pkg/ddl/split_region.go` is empty).",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-65133.json",
      "worthy_scope": true,
      "summary": "Reviewed diff vs first parent for ddl/meta changes introducing persistent region split policies (`TableInfo.TableSplitPolicy`, `IndexInfo.RegionSplitPolicy`) and a new DDL action (`ActionAlterTableSetRegionSplitPolicy`) that applies policies during pre-split/scatter. Found one High-severity issue: the new normalization path lacks the existing `SplitRegionMaxNum` upper bound, allowing extremely large `REGIONS` values to trigger unbounded key generation and potentially destabilize TiDB/cluster."
    },
    {
      "pr": 65380,
      "subject": "table: Non-clustered table non-unique global index needs partid in key, v1 (#65380)",
      "commit": "2f9776e8e7b252a1b14237e9a071bb1fa4ceac99",
      "modules": [
        "pkg/ddl",
        "pkg/meta/model",
        "pkg/tablecodec",
        "pkg/table/tables",
        "pkg/executor",
        "tests/integrationtest"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "Mixed-version/downgrade incompatibility: `GlobalIndexVersionV1` changes global non-distinct index key format by inserting `PartitionIDFlag`+partitionID into the key, but there is no compatibility gating to prevent creating these v1 indexes when older TiDB binaries may still be serving traffic. Older binaries would ignore the new metadata field and also cannot decode the new key suffix flag (126), risking query/index correctness during rolling upgrades or after downgrade.",
      "impact": "Rolling upgrade / mixed-version cluster risk: if a v1 global index is created while any TiDB nodes still run the pre-v1 code, those nodes can’t decode non-distinct global index keys (flag 126 in key suffix) and will mis-handle index access (query errors or wrong results) and/or index maintenance (writes using legacy key format won’t match v1 keys). Downgrade risk: older versions will not understand v1 keys but will still see the index as `Global` and attempt to use it.",
      "fix": "Add an explicit mixed-version compatibility gate before producing `GlobalIndexVersionV1` indexes. Options: (1) refuse CREATE/ADD GLOBAL INDEX that would require v1 unless all TiDB instances report a version that supports v1 (similar pattern exists in `pkg/ddl/ddl.go:1019` for job version gating); or (2) force legacy v0 format with a warning (but then the original collision bug remains). If downgrade is supported, also add downgrade protection/documentation (v1 indexes should block downgrade or require rebuild).",
      "verify": "Design-level: add a unit/integration test that simulates `infosync.GetAllServerInfo` returning at least one “old” TiDB version and assert DDL rejects (or forces legacy) when creating a global index that would need v1. Runtime-level: in a real rolling-upgrade test (old+new TiDB), run the PR’s reproduction (EXCHANGE PARTITION creating duplicate `_tidb_rowid`, then create global non-unique index) and ensure old nodes either (a) never observe v1 indexes (DDL refused), or (b) can still query correctly (if backward-compat decoding is implemented).",
      "evidence": "This commit introduces a new metadata field `IndexInfo.GlobalIndexVersion` (new in `pkg/meta/model/index.go:256`), sets it to `GlobalIndexVersionV1` for certain new global indexes (`pkg/ddl/index.go:354`), and changes non-distinct index key encoding to prefix the handle suffix with `PartitionIDFlag`+partitionID when `GlobalIndexVersion>=V1` (`pkg/tablecodec/tablecodec.go:1259`). In the first-parent code, `decodeHandleInIndexKey` did not recognize `PartitionIDFlag` and immediately calls `codec.DecodeOne` on the key suffix; `codec.DecodeOne` errors on unknown flags (`pkg/util/codec/codec.go:1312`), so a v1 key starting with 126 would fail decoding. Because older binaries would also ignore the new JSON field, they would treat the index as legacy while encountering v1 keys on disk, causing read/write incompatibility in mixed-version scenarios.",
      "later_fixed": false,
      "later_fix_evidence": "Checked `upstream/master` at `f0813a3696`: `pkg/ddl/index.go` still sets `GlobalIndexVersionV1` without any cluster-version gate, and `pkg/tablecodec/tablecodec.go` still encodes `PartitionIDFlag` into keys for v1; no follow-up compatibility guard was found via `git log 2f9776e8..upstream/master -- pkg/ddl/index.go pkg/tablecodec/tablecodec.go pkg/meta/model/index.go`.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-65380.json",
      "worthy_scope": true,
      "summary": "Adds `GlobalIndexVersion` to global index metadata and introduces v1 global index key encoding that includes partition ID in the key for non-clustered non-distinct global indexes (and unique-with-NULL cases) to prevent key collisions after EXCHANGE PARTITION; updates DDL backfill/DML key generation accordingly and adds tests. Main risk: lacks mixed-version/downgrade compatibility gating for the new on-disk key format."
    },
    {
      "pr": 66701,
      "subject": "importinto: introduce a param to control conflict resolution (#66701)",
      "commit": "97edb43f43e9489b92ea5f0e39ca6f07ed91cf91",
      "modules": [
        "pkg/dxf/importinto",
        "pkg/executor/importer"
      ],
      "severity": "High",
      "confidence": "High",
      "what": "`Plan.GetOnDupKeyMode()` defaults missing (legacy) task metadata to `error`, which changes conflict handling for IMPORT INTO DXF tasks created before this option existed (legacy capture → now error) when those tasks are resumed after upgrade.",
      "impact": "**Compatibility/metadata correctness:** IMPORT INTO disttask metadata generated before this PR won’t contain `Plan.OnDupKey`. After upgrade/resume, those tasks will implicitly switch from legacy “capture conflicts” to “error on conflict”, causing unexpected job failure on duplicate keys (and potentially leaving partially-processed import state requiring manual recovery).",
      "fix": "Preserve legacy semantics for pre-option task metadata:\n- Change `(*Plan).GetOnDupKeyMode()` default for empty to `OnDupKeyModeCapture` (legacy behavior) OR introduce an explicit task-meta/version field and map “missing OnDupKey” -> capture only for legacy tasks.\n- Keep new default for newly-created plans by continuing to set `p.OnDupKey = OnDupKeyModeError` in `initDefaultOptions` (already done).",
      "verify": "- Add a regression test that unmarshals legacy `TaskMeta` JSON without `on_dup_key` and asserts the executor path uses capture-mode `onDup` (or at least `Plan.GetOnDupKeyMode()` returns capture for empty).\n- (Optional) Simulate an in-flight task upgrade: create a task meta blob from pre-change code, run merge/ingest step selection, and assert it doesn’t fail solely due to missing `OnDupKey`.",
      "evidence": "- Legacy behavior (first parent): `git show 97edb43^:pkg/dxf/importinto/task_executor.go` shows `getOnDupForKVGroup()` returned `engineapi.OnDuplicateKeyRecord` for `external.DataKVGroup` and `getOnDupForIndex()` returned `OnDuplicateKeyRecord` for unique indexes unconditionally (capture-style conflict handling).\n- New behavior: DDL task execution now derives `onDup` from task metadata via `m.taskMeta.Plan.GetOnDupKeyMode()` in `pkg/dxf/importinto/task_executor.go:443-447`, and `Plan.GetOnDupKeyMode()` treats empty as error in `pkg/executor/importer/import.go:337-346` (explicitly noting old task metadata has empty).",
      "later_fixed": false,
      "later_fix_evidence": "Checked later history up to `upstream/master` (at `f0813a3696`): `git log 97edb43..upstream/master -- pkg/executor/importer/import.go` shows no follow-up changing `GetOnDupKeyMode`, and `upstream/master:pkg/executor/importer/import.go` still returns `OnDupKeyModeError` when empty.",
      "result_file": "/Users/bba/.codex-context/tidb--906fbbe4--0f8c9a77/artifacts/pr-review-2y/results/pr-66701.json",
      "worthy_scope": true,
      "summary": "Found one High-severity compatibility issue: legacy IMPORT INTO DXF tasks (metadata created before `on_duplicate_key`) will default to `error` on resume due to `GetOnDupKeyMode()` treating empty as error, changing prior capture semantics and potentially breaking in-flight jobs after upgrade. No later fix observed up to `upstream/master`."
    }
  ]
}