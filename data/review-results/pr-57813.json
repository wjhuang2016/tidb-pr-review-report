{"commit":"575310677da18511b9b98c72b48443b8471ade9b","findings":[{"confidence":"High","evidence":"- `pkg/ddl/backfilling.go:330` calls `d.isReorgRunnable(d.ctx, false)` inside the long-running per-batch loop of `backfillWorker.handleBackfillTask`.\n- `pkg/ddl/reorg.go:554` + `pkg/ddl/reorg.go:560` show `isReorgRunnable` only treats a job as non-runnable when the *passed-in* `ctx` is done (`return context.Cause(ctx)`); `d.ctx` is the ddl/owner lifetime context, not per-job.\n- Job pause/cancel is signaled via the *job step* context: `pkg/ddl/job_worker.go:827` creates `jobCtx.stepCtx` with `context.WithCancelCause`, and `pkg/ddl/job_worker.go:856` / `pkg/ddl/job_worker.go:862` cancel it with `ErrCancelledDDLJob` / `ErrPausedDDLJob...`.\n- Backfill txn work itself is not context-cancellable: e.g. `pkg/ddl/index.go:2284` starts the txn using `context.Background()` and `pkg/ddl/index.go:2320` uses `txn.LockKeys(context.Background(), ...)`, so the only timely stop point is the pre-batch runnable check.\n- Shutdown path can block waiting for workers: `pkg/ddl/backfilling_scheduler.go:341` cancels worker ctx in `closeBackfillWorkers`, then `pkg/ddl/backfilling_scheduler.go:343` waits for `b.wg.Wait()`; but `pkg/ddl/backfilling.go:403` only observes `w.ctx.Done()` between tasks, so if `handleBackfillTask` never sees job-step cancellation, close can be delayed until the current region-range finishes.\n- Note: worker ctx (`w.ctx`) cannot be blindly used as the job-runnable ctx because it is also canceled during dynamic worker-count decrease (`pkg/ddl/backfilling_scheduler.go:329`), which would otherwise surface as `context.Canceled` and fail the job.","fix":"Pass a *job-step/scheduler* context (canceled on job pause/cancel) into `handleBackfillTask`’s runnable check instead of `d.ctx`.\n- Minimal: store the parent ctx passed to `newBackfillWorker(b.ctx, ...)` in the worker struct (e.g. `jobCtx context.Context`), and call `d.isReorgRunnable(w.jobCtx, false)`.\n- Keep `w.ctx` solely for worker lifecycle/scale-down cancellation so shrinking worker count doesn’t turn into a job-level error (`context.Canceled`).","impact":"DDL add-index (txn backfill path) may ignore or significantly delay `ADMIN CANCEL DDL JOBS` / pause requests, continuing backfill work until the current region-range completes. This can prolong cluster load and can delay DDL job step exit (and `scheduler.close(true)`/worker shutdown) under cancellation scenarios.","later_fix_evidence":"Not fixed up to `remotes/upstream/master`: `git show remotes/upstream/master:pkg/ddl/backfilling.go` still contains `err := d.isReorgRunnable(d.ctx, false)` (line 360 in that file snapshot), and add-index txn backfill still uses `context.Background()` for kv txn setup (e.g. `remotes/upstream/master:pkg/ddl/index.go` around the `addIndexTxnWorker.BackfillData` implementation).","later_fixed":false,"severity":"High","verify":"- Run a large add-index job using txn backfill (non-DXF/dist), then issue `ADMIN CANCEL DDL JOBS <job_id>` / `ADMIN PAUSE DDL JOBS <job_id>` while backfilling; confirm the job transitions promptly and backfill workers stop without waiting for an entire region-range.\n- Change `job.ReorgMeta` concurrency downward at runtime (triggering `adjustWorkerSize`); confirm shrinking workers doesn’t fail the job (no propagated `context.Canceled`).","what":"`backfillWorker.handleBackfillTask` checks reorg runnability using `d.ctx` (ddl/owner lifetime context) rather than the per-job step context that is canceled on job pause/cancel. Since backfill txn operations use `context.Background()`, this prevents timely interruption of long-running txn backfill loops on `ADMIN CANCEL/PAUSE`, and can delay scheduler/worker shutdown that waits on backfill workers."}],"modules":["pkg/ddl"],"pr":57813,"status":"has_high_or_blocker","subject":"ddl: check context done in isReorgRunnable function (#57813)","summary":"Found a High-severity cancellation/pause responsiveness issue in txn backfill: `handleBackfillTask` passes `d.ctx` into `isReorgRunnable`, so job-step cancel/pause signals are ignored during long-running per-region backfill loops; this can delay `ADMIN CANCEL/PAUSE` and block worker shutdown waiting for backfill completion."}