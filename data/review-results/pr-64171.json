{"commit":"f2abf53fab601b3a466c5ac686643f0508a3d687","findings":[{"confidence":"High","evidence":"`pkg/ddl/ingest/checkpoint.go:492` loads cp and returns early on `cp != nil` at `pkg/ddl/ingest/checkpoint.go:498`, so the TS allocation/persist path at `pkg/ddl/ingest/checkpoint.go:527` is skipped whenever a checkpoint object exists.\nInitial reorg meta inserts a checkpoint object without setting `ts`/`instance_addr` (defaults `ts=0`, `instance_addr=\"\"`) in `pkg/ddl/job_scheduler.go:779`.\nImport TS is consumed for every import via `pkg/ddl/ingest/backend.go:281` -> `SetTSBeforeImportEngine`, which auto-allocates a TS when given 0 (`pkg/lightning/backend/local/local.go:1644`, `pkg/lightning/backend/local/local.go:1650`), defeating the “persist chosen TS before import/failover” intent.\nNot fixed later: upstream/master still has the same early-return logic (`pkg/ddl/ingest/checkpoint.go:504` on upstream/master f0813a369666ac6314e76f5e573e982fe50c8a09).","fix":"In `CheckpointManager.resumeOrInitCheckpoint`, after applying a loaded checkpoint, if `s.ts == 0` (or `cp.TS == 0`) allocate a PD TS and persist it via `updateCheckpointImpl()` before returning. This restores the pre-existing invariant that a non-zero, persisted “next ingest TS” exists before the first import and also persists `instance_addr` early.\nAlternative: make `NormalCheckpointStorage.LoadCheckpoint` treat checkpoints with `TS==0` as “not initialized” (return nil) so the existing TS-allocation branch runs.","impact":"For normal (non-DXF) local ingest add-index, the reorg table always contains a checkpoint object from initialization, so `CheckpointManager` can keep `ts=0` for a long time (until first flush/import). The first import then picks a TS inside Lightning (because `ts==0`) but that TS is not persisted in DDL checkpoint storage.\nIf the job/subtask fails over/crashes around the first import boundary (before the checkpoint write that would otherwise persist TS), a retry can choose a different TS for the same imported KV range, breaking the intended idempotency/failover stability and increasing risk of duplicate MVCC versions / inconsistent import semantics under retries.\nAlso prolongs the `instance_addr==\"\"` “initial state” window, weakening the intended guard that local sort dir reuse is tied to the producing instance.","later_fix_evidence":"Checked later history up to upstream/master f0813a369666ac6314e76f5e573e982fe50c8a09; `pkg/ddl/ingest/checkpoint.go:504` still returns early on non-nil checkpoint without allocating/persisting TS when `ts==0`. `pkg/ddl/job_scheduler.go:803` still initializes the checkpoint without TS.","later_fixed":false,"severity":"High","verify":"Add a unit test that inserts `mysql.tidb_ddl_reorg.reorg_meta` using the same shape as `initDDLReorgHandle` (checkpoint object present but `ts==0`), then constructs `NewCheckpointManager` and asserts:\n- `GetImportTS()` becomes non-zero immediately\n- storage row is updated with non-zero `ts` and non-empty `instance_addr`.\nManual/behavioral: simulate a crash after the first `SetTSBeforeImportEngine` but before checkpoint update; on resume, verify the same TS is reused (no re-import TS drift).","what":"Regression in checkpoint initialization: when a checkpoint object exists but has `ts=0` (the default initial reorg_meta state), `CheckpointManager` now returns early and never allocates/persists an initial ingest TS. This undermines the intended failover/idempotency behavior around the first import and delays persisting `instance_addr`."}],"modules":["pkg/ddl","pkg/ddl/ingest","pkg/disttask/framework","pkg/disttask/framework/storage"],"pr":64171,"status":"has_high_or_blocker","subject":"ddl: add checkpoint operator for DXF local add index (#64171)","summary":"Adds a checkpoint storage abstraction so DXF local add-index can persist checkpoints in the disttask subtask table, and wires the read-index local pipeline to use it. Main concern: in the normal (reorg-table) path, checkpoint initialization now returns early when a checkpoint object exists, so an initial `ts=0` checkpoint never gets a persisted non-zero ingest TS, weakening first-import failover/idempotency semantics."}