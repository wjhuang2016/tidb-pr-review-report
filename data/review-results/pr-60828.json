{"commit":"e8fb24a20d47ec227ec6f075631927871fcc49b7","findings":[{"confidence":"High","evidence":"- `pkg/ddl/reorg.go`: `runReorgJob` now returns `dbterror.ErrWaitReorgTimeout` on `updateProcessTicker.C` after `job.SetRowCount(...)`/warnings merge, and sets `jobCtx.reorgTimeoutOccurred = true`, leaving the background `reorgFn()` goroutine running and keeping the `reorgCtx` in-memory.\n- `pkg/ddl/job_worker.go`: `jobContext.cleanStepCtx()` skips cancelling/clearing `stepCtx` when `reorgTimeoutOccurred` is set, so `jobCtx.stepCtx` becomes long-lived across those periodic timeouts.\n- `pkg/ddl/job_worker.go`: `processJobPausingRequest()` converts `JobStatePausing` -> `JobStatePaused` and returns before starting the polling goroutine (the only place that calls `jobCtx.stepCtxCancel(...)`) and without any explicit `stepCtx` cancellation.\n- `pkg/ddl/job_scheduler.go`: `deliveryJob` refreshes the job after every step “to handle cases like job cancel or pause”, so the step after a pause request will typically see `JobStatePausing/Paused` and take the early return path; and `model.Job.InFinalState()` treats `JobStatePaused` as final, so the scheduler stops running the job after it becomes paused—removing the last chance to cancel the still-alive `stepCtx`/reorg workers." ,"fix":"- When handling `JobStatePausing/Paused`, explicitly cancel/cleanup the long-lived `jobCtx.stepCtx` (and ensure cleanup isn’t skipped due to the reorg-timeout flag). A minimal approach is to cancel `stepCtx` with an `ErrPausedDDLJob` cause before returning from the pausing path, so the background reorg goroutine/workers stop before the scheduler exits on `JobStatePaused`.\n- Consider mirroring the later rollback-oriented fix pattern (explicit step context cleanup when leaving running reorg), but for pause as well, so pausing reliably stops reorg resources regardless of timing." ,"impact":"`ADMIN PAUSE DDL JOBS` can reach `JobStatePaused` (a scheduler final state) while the background reorg goroutine/workers keep running under the preserved `stepCtx`, continuing resource consumption and potentially continuing backfill KV work after the job is “paused”. This violates pause semantics and can leave long-lived goroutines/reorg contexts running until owner changes or the job is resumed.","later_fix_evidence":"Checked `upstream/master` (e.g. `bab4993b6f`): the reorg timeout still keeps `stepCtx` alive and `processJobPausingRequest()` still early-returns without cancel/cleanup of `stepCtx` on pausing/paused jobs, so the described pause race/behavior remains.","later_fixed":false,"severity":"High","verify":"- With failpoint-enabled build: set `github.com/pingcap/tidb/pkg/ddl/updateProgressIntervalInMs` to a small value (< poll interval) to force frequent reorg timeouts; start an add-index reorg job; issue `ADMIN PAUSE DDL JOBS <id>`; after `ADMIN SHOW DDL JOBS` reports `paused`, verify reorg/backfill work stops (row count stops increasing, no further backfill logs) and no in-memory reorg context/goroutine leak is observed.\n- Add a unit/integration test that pauses immediately after a timeout return but before the next poll tick, and asserts pausing cancels the job step context and stops reorg work." ,"what":"Pause handling can leave reorg work running after a job becomes `JobStatePaused`: because reorg now returns periodic timeouts while preserving `jobCtx.stepCtx`, but the `JobStatePausing/Paused` fast-path returns without canceling that long-lived context, and the scheduler exits on paused (final state), the background reorg goroutine/workers can continue after a successful pause."}],"modules":["pkg/ddl"],"pr":60828,"status":"has_high_or_blocker","subject":"ddl: update row count periodically when running reorg job (#60828)","summary":"This commit changes reorg progress handling to periodically return `ErrWaitReorgTimeout` so row count/progress gets persisted to `mysql.tidb_ddl_job` during reorg, and adjusts `jobContext.stepCtx` lifecycle/polling (including multi-schema change inner-step polling suppression). One High-severity state-machine/concurrency issue remains: pausing can reach `JobStatePaused` without canceling the long-lived reorg step context, so background reorg workers may continue after the job is “paused”."}