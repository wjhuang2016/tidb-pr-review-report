{"commit":"6dc2c5b63fe0db8b815e929fc41afdcaae45daf1","findings":[{"confidence":"High","evidence":"- New persisted enum value: `pkg/meta/model/reorg.go` adds `AnalyzeStateTimeout = 4`.\n- New state is written mid-job: `pkg/ddl/index.go` and `pkg/ddl/modify_column.go` set `job.ReorgMeta.AnalyzeState = model.AnalyzeStateTimeout` when `analyzeTableAfterCreateIndex()` reports `timedOut`, then only publish schema in a later pass when `AnalyzeStateTimeout` is handled as a terminal state.\n- First-parent code doesn’t recognize value 4 and has no default: `git show 6dc2c5b63f^:pkg/ddl/index.go` and `git show 6dc2c5b63f^:pkg/ddl/modify_column.go` inner `switch job.ReorgMeta.AnalyzeState` only covers `None/Running/Done/Skipped` (no `default`). If `AnalyzeState==4`, the switch body is skipped and the job doesn’t advance.\n- This state is stored in DDL job metadata: `pkg/meta/model/reorg.go` defines `DDLReorgMeta.AnalyzeState int8 \\`json:\"analyze_state\"\\`` and `pkg/meta/model/job.go` proxies/copies `ReorgMeta.AnalyzeState` into persisted job JSON, so an older TiDB owner will read `analyze_state=4` from `mysql.tidb_ddl_job`.","fix":"Avoid introducing new persisted `AnalyzeState` values that older binaries can’t handle during rolling upgrade. Options:\n- Encode timeout as a new JSON field (unknown fields are ignored by older versions) while keeping `AnalyzeState` in the existing set (e.g., `Skipped`), and drive `SHOW DDL JOBS` labeling from the new field.\n- Or, if keeping `AnalyzeStateTimeout`, ensure timeout transitions don’t require another DDL round (finish/publish within the same round) so there’s no window where an in-queue job has `analyze_state=4` but still needs processing.\n- Additionally, add a `default` branch in the analyze-state switches to treat unknown states as terminal (helps forward-compat within newer binaries).","impact":"Mixed-version rolling upgrade risk: if a newer owner writes `AnalyzeStateTimeout` (4) to an in-flight add-index/modify-column job, and ownership later moves to an older TiDB binary (pre-#64096), that older owner will not advance the job state machine. The DDL job can become stuck in `StateWriteReorganization`, potentially blocking subsequent DDLs and causing prolonged schema-change unavailability.","later_fix_evidence":"","later_fixed":false,"severity":"High","verify":"Reproduce with a mixed-version scenario:\n1) On new binary, enable `tidb_enable_ddl_analyze=1` and force timeout (e.g., via failpoint/sleep as in `pkg/ddl/index_modify_test.go`). Let the job persist `analyze_state=4` while still in `StateWriteReorganization`.\n2) Switch DDL ownership to an older binary (pre-#64096).\n3) Observe the job no longer progresses (no matching `AnalyzeState` case), remaining in the queue indefinitely until manually intervened.","what":"`AnalyzeStateTimeout` (value 4) is persisted into DDL job metadata but first-parent code has no handler/default for this new value. During mixed-version rolling upgrades, if ownership falls back to an older TiDB while an add-index/modify-column job is between “set AnalyzeStateTimeout” and “publish/finalize”, the older owner can’t advance the state machine and the DDL job can get stuck."}],"modules":["pkg/ddl","pkg/meta/model","pkg/executor","tests/realtikvtest"],"pr":64096,"status":"has_high_or_blocker","subject":"ddl: add timeout mechanism for add index analyze (#64096)","summary":"Adds a cumulative-timeout path for the embedded post-reorg `ANALYZE TABLE` in add-index/modify-column DDL, including a new persisted `AnalyzeStateTimeout` state and logic to avoid re-running analyze across owner changes. Main High-risk concern is mixed-version compatibility: older owners can’t handle the new persisted analyze state value and may strand in-flight DDL jobs."}