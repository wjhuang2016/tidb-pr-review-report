{"commit":"4011c9f6c56da1f2bc515d72c0ae95a6d542be40","findings":[{"confidence":"High","evidence":"- `pkg/ddl/modify_column.go` (`doModifyColumnTypeWithData`, `StateWriteReorganization`): control flow is now `switch job.ReorgMeta.AnalyzeState { case None: switch job.ReorgMeta.Stage { UpdateColumn/RecreateIndex/Completed } }` with **no default / no `ReorgStageNone` handling**. If `Stage==0`, the switch does nothing and the function returns `nil`, leaving the job stuck in `StateWriteReorganization`.\n- `pkg/meta/model/reorg.go`: this PR introduces `DDLReorgMeta.Stage` (with `ReorgStageNone=0`) and `DDLReorgMeta.AnalyzeState`.\n- `pkg/meta/model/job.go`: this PR removes the top-level `Job.AnalyzeState` field (previously `json:\"analyze_state\"`), and `Job.Decode` is still a plain `json.Unmarshal` (no migration).\n- First-parent behavior (pre-PR) confirms incompatibility: `2c25d745:pkg/ddl/modify_column.go` drives the reorg/analyze progression via `job.AnalyzeState` only (no stage persisted), so an in-flight modify-column job from the first parent can exist with `SchemaState=WriteReorganization` but without `reorg_meta.stage/analyze_state` fields; on this commit it deserializes as `Stage=0/AnalyzeState=0` and then no-ops forever.","fix":"- Add a compatibility shim so in-flight jobs from pre-#63970 can continue:\n  - In `pkg/ddl/modify_column.go` (and any other modify-column reorg entrypoint), treat `job.ReorgMeta.Stage==model.ReorgStageNone` as a safe default (likely `ReorgStageModifyColumnUpdateColumn`), or infer stage from `mysql.tidb_ddl_reorg.ele_type` (column vs index element) when `Stage==0`.\n  - Consider decoding legacy top-level `analyze_state` into `job.ReorgMeta.AnalyzeState` (custom decode or a deprecated alias field) so ongoing jobs don’t lose analyze progression during rolling upgrade.\n- Ensure the shim is idempotent and only triggers when `Stage==0` / legacy fields present.","impact":"Rolling upgrade / owner switch compatibility regression: an in-flight `ActionModifyColumn` job created by the first-parent code and already in `StateWriteReorganization` can become permanently stuck after upgrading to this commit (no reorg, no analyze, no state advancement). This can leave the table in an intermediate schema state (hidden changing column/indexes present) and require manual intervention (cancel/retry), with potential prolonged performance/behavior impact while stuck.","later_fix_evidence":"Searched up to `upstream-master`: no handling of `ReorgStageNone` / `Stage==0` found (only the constant definition in `pkg/meta/model/reorg.go`), and `pkg/meta/model/job.go` `Decode` remains a plain `json.Unmarshal` without migrating legacy `analyze_state` into `reorg_meta`.","later_fixed":false,"severity":"High","verify":"- Repro (rolling upgrade): on the first parent (`2c25d745`), start a `MODIFY COLUMN` that requires reorg (e.g., type change with indexes) and wait until `ADMIN SHOW DDL JOBS` shows `schema_state=write reorganization`. Upgrade owner to this commit and observe whether the job continues advancing rowcount/state.\n- Static check: confirm `job.ReorgMeta.Stage` can be 0 for pre-#63970 jobs (no JSON field), and that `StateWriteReorganization` path makes progress when stage/analyze are missing (after adding shim).","what":"Rolling-upgrade incompatibility: in-flight `MODIFY COLUMN` reorg jobs from pre-#63970 can get stuck because `AnalyzeState` moved into `job.ReorgMeta` and a new persisted `Stage` gate was introduced, but there is no migration/default handling for missing `reorg_meta.stage/analyze_state`."}],"modules":["pkg/ddl","pkg/ddl/ingest","pkg/meta/model"],"pr":63970,"status":"has_high_or_blocker","subject":"modify column: support ingest/DXF mode to recreate indexes (#63970)","summary":"Adds staged modify-column reorg to allow index recreation via ingest/DXF (`ReorgMeta.Stage` + `ReorgMeta.AnalyzeState`, temp-index rollback IDs, reuse create-index backfill). Main risk found is rolling-upgrade compatibility: pre-existing modify-column jobs in `write reorganization` can deserialize with missing stage/analyze and then no-op indefinitely."}