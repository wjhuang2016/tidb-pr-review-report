{"commit":"14e99eab84426fede2d56ee2076ec25c5d729e73","findings":[{"confidence":"High","evidence":"- `pkg/util/codec/float.go:23-30`: `encodeFloatToCmpUint64` uses `if f >= 0` (vs sign-bit check), so `-0.0` is treated as non-negative and encoded identically to `+0.0`, and `DecodeFloat` round-trips `-0.0` to `+0.0`.\n- `pkg/expression/expr_to_pb.go:139-144`: coprocessor constants of Float32/Float64 are encoded via `codec.EncodeFloat`, so any constant-folded `-0.0` loses its sign when pushed down.\n- This commit deletes the integration test for `#41878` that expected `{0}` from `SELECT c0 ... WHERE CAST(ATAN2(..., -('')) AS TIME)`.\n- Upstream issue `pingcap/tidb#41878` documents the wrong-result repro (actual empty set, expected `{0}`), matching the reverted behavior.","fix":"If TiDB needs to satisfy `pingcap/tidb#41878` while avoiding the pushdown/no-pushdown inconsistency seen after `#57343` (ref `#57500`), split the concerns:\n- Keep `-0.0` canonicalization only where equality/grouping keys are serialized/hashes are computed (so `-0` and `+0` remain equal for GROUP BY / hash keys / index lookups).\n- Preserve IEEE sign for value/constant transport needed by numeric functions (e.g., a dedicated float encoder for coprocessor constants / row values that uses sign-bit detection like in `9b2656951d`, without changing key/range encoding).\nThis likely requires a new encode API or explicit `if v == 0 { v = 0 }` normalization at key-serialization call sites, rather than in the shared float codec primitive.","impact":"- Reintroduces the wrong-result bug in `pingcap/tidb#41878`: predicates depending on negative-zero-sensitive math (e.g., `ATAN2`) can evaluate differently because `-0.0` collapses to `+0.0` during float encoding used for coprocessor execution.\n- Can lead to MySQL incompatibility and plan-dependent results (coprocessor pushdown vs root evaluation) for edge cases involving `-0.0`.","later_fix_evidence":"No subsequent commits touching `pkg/util/codec/float.go` or `pkg/expression/expr_to_pb.go` were found in `14e99eab84..upstream-master` (`git log` is empty for both paths), and `pingcap/tidb#41878` is currently `OPEN`.","later_fixed":false,"severity":"High","verify":"- Run the exact repro from `pingcap/tidb#41878` and confirm it returns `{0}` with normal settings (pushdown enabled).\n- Re-run the coprocessor pushdown consistency case from `pingcap/tidb#57500` (the provided randgen query) and confirm WithPushDown/NoPushDown outputs are identical.\n- Add a targeted check for `ATAN2(0, -0.0)` behavior under pushdown vs no-pushdown to ensure determinism.","what":"Reverting the negative-zero-preserving float codec reintroduces an incorrect query result (documented in `pingcap/tidb#41878`) by collapsing `-0.0` to `+0.0` in `codec.EncodeFloat`, which is used for coprocessor constant encoding."}],"modules":["pkg/util/codec","pkg/expression","tests/integrationtest/expression"],"pr":57525,"status":"has_high_or_blocker","subject":"Revert \"util: fix codec for negative zero (#57343)\" (#57525)","summary":"This commit reverts the `#57343` float codec change, restoring `f >= 0` handling in `encodeFloatToCmpUint64` and deleting the regression tests. That brings back the negative-zero collapse that causes the wrong-result bug in `pingcap/tidb#41878` (ATAN2 + CAST predicate), especially via coprocessor constant encoding/pushdown."}