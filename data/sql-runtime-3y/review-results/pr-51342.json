{"commit":"218f3af41e43bb71e2c7b3b532e77add8ee6d55e","findings":[],"modules":["pkg/executor/aggregate/agg_hash_executor.go"],"pr":51342,"status":"no_high_or_blocker","subject":"executor: fix data race in hash agg ut (#51342)","summary":"Reviewed diff vs parent (only `pkg/executor/aggregate/agg_hash_executor.go`). Change converts `HashAggExec.executed` from `bool` to `atomic.Bool` and updates all reads/writes (`Load`/`Store`) across `Close`, `initForUnparallelExec`, `initForParallelExec`, `parallelExec`, `unparallelExec`, and `resetSpillMode`. Checked surrounding control flow to ensure `executed` still correctly gates one-time emission of `DefaultVal` and prevents duplicate reads after `finalOutputCh` closure; no correctness/SQL-semantics/data-consistency/concurrency Blocker/High issues found within executor scope."}