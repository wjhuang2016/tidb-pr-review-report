{"commit":"fbf158686df0d6c276a5233746bf4ce8d59e9b72","findings":[{"confidence":"High","evidence":"`pkg/planner/optimize.go:235-241` records restores even when setting fails: it calls `sessVars.SetSystemVarWithOldValAsRet(name, val)`, appends the warning on error, and still executes `sessVars.StmtCtx.AddSetVarHintRestore(name, oldV)`.\n`pkg/sessionctx/variable/session.go:2809-2817` returns `oldV == \"\"` on validation errors (`sv.Validate` fails before old value is fetched).\n`pkg/util/hint/hint.go:371-390` does not validate `SET_VAR` values (only var name); invalid values reach `SetSystemVarWithOldValAsRet`.\nThis PR changes `pkg/sessionctx/stmtctx/stmtctx.go:1216-1224` to “first-wins” (do not overwrite an existing restore entry).\nRestore runs in `pkg/executor/select.go:885-891`: it calls `vars.SetSystemVar(name, val)`; on error it only logs and leaves the current (hint-set) value.\nRepro sequence: a query `SET_VAR(x=bad)` (warning, no change) stores restore `x:\"\"`; then a binding `SET_VAR(x=good)` successfully sets x but cannot update the restore entry (first-wins), so the next statement’s restore tries `SET x=\"\"` (fails) and x leaks as `good`. Binding use with query hints is exercised in `pkg/planner/core/hint_test.go:55-61` (binding still used when query has `SET_VAR`).","fix":"In `pkg/planner/optimize.go`, only call `AddSetVarHintRestore` when the variable was actually set successfully (e.g., `if err == nil { AddSetVarHintRestore(...) }`). This avoids populating the restore map with an invalid/empty old value on validation failure and avoids blocking later correct restore capture (e.g., from bindings).\nAlternative (less preferred): change `SetSystemVarWithOldValAsRet`/`SetSystemVarWithOldStateAsRet` to return the old state even on validation failure, but this is tricky for “stateful” vars (e.g., `timestamp`) and still doesn’t justify recording restores when the set didn’t happen.","impact":"Session-scoped variables changed by a later successful `SET_VAR` (notably from plan bindings) can leak across statements if an earlier `SET_VAR` for the same var failed validation (query hint ignored with warning). This breaks session semantics and can persistently alter behavior for subsequent statements (timeouts, DML mode, etc.) while only emitting warnings/logs.","later_fix_evidence":"Not fixed as of `upstream/master`: `AddSetVarHintRestore` remains first-wins and `Optimize` still records restore entries even when setting returns an error, so the same “invalid first, valid later” leak pattern remains possible.","later_fixed":false,"severity":"High","verify":"Add a regression test (planner/core hint tests) that:\n1) Sets a session var to a known value.\n2) Creates a binding that applies `SET_VAR(var=valid)`.\n3) Executes the query with `/*+ SET_VAR(var=invalid) */` so the first application fails validation (warning), but the binding still applies.\n4) Verifies `@@last_plan_from_binding=1` and that `@@var` is restored to the original value after the statement (no leak).","what":"With the new “first-wins” restore map, an earlier `SET_VAR` application that fails validation can permanently occupy the restore entry with an invalid/empty old value, preventing later correct old-value capture for the same sysvar (e.g., when a binding’s `SET_VAR` succeeds). On the next statement, restore can fail and leave the session variable stuck at the hint-applied value, leaking across statements."}],"modules":["planner","sessionctx"],"pr":61280,"status":"has_high_or_blocker","subject":"planner: Make SET_VAR query hints restore the original session variable values. (#61280)","summary":"The change correctly preserves the *original* session var value when the same sysvar is affected by multiple `SET_VAR` sources (query + binding), fixing the primary overwrite bug and adding regression coverage. However, because restore entries are still recorded even when `SET_VAR` setting fails validation, the new first-wins behavior can cause session variables set later (e.g., by bindings) to leak across statements when an earlier `SET_VAR` for the same var was invalid."}