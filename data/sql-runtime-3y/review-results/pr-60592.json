{"commit":"bf9fbde9e9f642c2b4dcfe8e081fbc8afc69f38e","findings":[{"confidence":"High","evidence":"- Default enables v2 path: `pkg/sessionctx/vardef/tidb_vars.go:1642` (`DefTiDBOptIndexJoinBuild = true`) + `pkg/sessionctx/variable/sysvar.go:2546` (sets `SessionVars.EnhanceIndexJoinBuildV2`).\n- Planner switches to v2 enumeration when enabled: `pkg/planner/core/exhaust_physical_plans.go:2772`.\n- v2 enumeration always applies `NO_INDEX_*` filtering even when force INL_* hints exist: `pkg/planner/core/exhaust_physical_plans.go:2058` returns `handleFilterIndexJoinHints(p, candidates)` after only emitting a conflict warning; `pkg/planner/core/exhaust_physical_plans.go:2155` does not special-case force hints.\n- v1 path explicitly gives force hints higher priority (skips filter hints when forced candidates exist): `pkg/planner/core/exhaust_physical_plans.go:2137` (`handleForceIndexJoinHints` + early return).\n- Not fixed later: `remotes/upstream/master` (3b531d4e3d) still has `tryToEnumerateIndexJoin` ending with `return handleFilterIndexJoinHints(p, candidates)` (around `pkg/planner/core/exhaust_physical_plans.go:1125`).","fix":"In v2 enumeration, honor the stated priority “force INL_* > NO_INDEX_*”:\n- Easiest: in `tryToEnumerateIndexJoin`, if any force INL_* hint is present for this join, skip `handleFilterIndexJoinHints` entirely.\n- More precise: adjust `handleFilterIndexJoinHints` to *not* filter out the join method(s) that are explicitly forced (e.g. keep `PhysicalIndexJoin` candidates when INL_JOIN present even if NO_INDEX_JOIN present).\n- Keep the existing conflict warning, or update it to reflect the actual precedence if precedence is intentionally different.","impact":"Queries with overlapping/ambiguous join hints can hit the “force + filter” conflict scenario unintentionally. With v2 default ON, forced `INL_JOIN/INL_HASH_JOIN/INL_MERGE_JOIN` can be removed by `NO_INDEX_*` filtering, leading to unexpected join algorithm selection and potentially large performance regressions; the emitted warning text (“NO_INDEX_JOIN may be ignored”) also becomes misleading under the v2 path.","later_fix_evidence":"","later_fixed":false,"severity":"High","verify":"- Add a planner integration test that runs `EXPLAIN` for a join with both `INL_JOIN(...)` and `NO_INDEX_JOIN(...)` applying to the same join node, under `@@tidb_opt_index_join_build_v2=ON`, asserting the chosen physical plan still follows the force hint (and emits the conflict warning).\n- Add a corresponding case with `@@tidb_opt_index_join_build_v2=OFF` to ensure behavior matches the v1 path (force hint wins when a valid forced plan exists).","what":"Under the new default (`@@tidb_opt_index_join_build_v2=ON`), index-join enumeration uses `tryToEnumerateIndexJoin`, which still filters candidates with `NO_INDEX_*` hints even when conflicting forced `INL_*` hints exist. This can suppress forced index-join plans in conflict scenarios, contradicting the documented priority and the v1 behavior where force hints override filter hints."}],"modules":["executor","planner","sessionctx"],"pr":60592,"status":"has_high_or_blocker","subject":"planner: add variable control for index join build v2 and default them on (#60592)","summary":"This commit introduces `@@tidb_opt_index_join_build_v2` (default ON) to switch IndexJoin/IndexHashJoin to the v2 build/enumeration path and adjusts hint/warning handling in task enumeration. A High compatibility issue remains: in the v2 path, `NO_INDEX_*` filtering can still remove forced `INL_*` candidates in conflict scenarios, diverging from the stated hint-priority and prior v1 behavior; the same logic still exists on upstream/master."}