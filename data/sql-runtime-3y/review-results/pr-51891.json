{"commit":"469cf047fa09b1fcbb09afd15049e7dfc2a50988","findings":[],"modules":["expression","planner"],"pr":51891,"status":"no_high_or_blocker","subject":"expression: add `PushDownContext` for push down operations (#51891)","summary":"Reviewed this commit diff vs parent. Main change is a refactor: introduce `expression.PushDownContext` in `pkg/expression/infer_pushdown.go` and thread it through expression pushdown checks (`PushDownExprs`/`CanExprsPushDown`) and planner pushdown decision points via `core.GetPushDownCtx` (`pkg/planner/core/util.go`), plus update agg PB conversion (`aggregation.AggFuncToPBExpr`) call sites in plan-to-PB. Verified updated callers consistently derive eval ctx/session vars/client from the same `PlanContext`/`SCtx()`, and changes are mechanical (signature/parameter plumbing) without altering pushdown rules/SQL semantics. No plausible Blocker/High issues found in correctness, session/stmtctx handling, or pushdown protocol/runtime behavior."}