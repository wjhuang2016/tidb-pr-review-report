{"commit":"1245c28b3b1577694fd842587217d8d56dd77777","findings":[{"confidence":"Medium","evidence":"- Spill error is recorded asynchronously *after* `spillToDisk` returns: `pkg/executor/sortexec/sort_spill.go:53-60`.\n- `spillToDisk` marks the partition as spilled (`defer s.setSpillTriggered()`) regardless of whether `spillToDiskImpl()` returns an error: `pkg/executor/sortexec/sort_partition.go:250-258`.\n- If `spillToDiskImpl()` hits an error during `inDisk.Add`, it returns early and does not free memory / repair iterator state: `pkg/executor/sortexec/sort_partition.go:210-228`.\n- `DataInDiskByChunks.Add()` only updates `offsetOfEachChunk` and `totalRowNum` after a successful write; on error the chunk (and its rows) are not represented in metadata, so later reads can silently see fewer rows: `pkg/util/chunk/chunk_in_disk.go:84-113`, `pkg/util/chunk/chunk_in_disk.go:309-317`.\n- After spill is “triggered”, row iteration relies on `NumChunks()`/`GetChunk()`; reaching `restoredChkID >= NumChunks()` is treated as end-of-data (not an error): `pkg/executor/sortexec/sort_partition.go:124-137`, `pkg/executor/sortexec/sort_partition.go:261-285`.\n- SortExec does not consistently re-check all partitions for errors during output; external merge checks only the *last* partition: `pkg/executor/sortexec/sort.go:177-183`. This increases the chance that a spill error in an earlier partition is never surfaced if the one-time `checkError()` happens before the async `setError()` wins the `syncLock`.","fix":"- Make spill failure unmissable and synchronous w.r.t. state transitions:\n  - Record `spillError` inside `spillToDisk` while holding `syncLock` (or before broadcasting/setting `spillTriggered`), so any subsequent `checkError()` cannot race past it.\n  - Only set `spillTriggered` when `spillToDiskImpl()` succeeds; if it fails, keep the partition in a non-spilled state (or force query failure) to avoid switching the read path to disk with incomplete metadata.\n  - Optionally: have `getNextSortedRow()` return `spillError` early (under `syncLock`) to prevent silent truncation.\n- Consider checking `checkError()` for every partition (or for the partition being popped) during external merge, not just the last partition.","impact":"If disk spill fails (e.g., out of temporary space / I/O error) after writing some chunks, the spilled metadata (`NumChunks`/`NumRows`) can reflect only the successfully written chunks. Because the partition is still marked `spillTriggered`, iteration can stop early and return a truncated result set without necessarily raising an error. In multi-partition merge, the current error-check strategy (only checking the last partition) further increases the chance of missing spill errors from earlier partitions.","later_fix_evidence":"Not fixed on `upstream/master` as of this review:\n- `upstream/master:pkg/executor/sortexec/sort_spill.go` still sets `partition.setError(err)` asynchronously after `spillToDisk()` returns (see lines 56-63 in `git show upstream/master:pkg/executor/sortexec/sort_spill.go | nl -ba`).\n- `upstream/master:pkg/executor/sortexec/sort.go` still only checks the last partition error in `externalSorting` (see lines 497-503 in `git show upstream/master:pkg/executor/sortexec/sort.go | nl -ba`).","later_fixed":false,"severity":"High","verify":"- Add a unit test that forces `DataInDiskByChunks.Add()` to return an error (e.g., via a failpoint / injected error) after some successful chunk writes, then assert the sort executor returns an error (and does not produce a shorter-but-successful result).\n- Add a test for multi-partition external merge where a non-last partition’s spill fails and ensure the error is surfaced reliably.\n- Manual reasoning check: ensure `spillTriggered` is only reachable when disk state is complete, or ensure any incomplete spill makes the executor fail fast." ,"what":"Spill-to-disk failures can be missed due to async error recording + unconditional `spillTriggered` transition, allowing external sort to treat partially-spilled partitions as complete and potentially return truncated/incorrect results without error."}],"modules":["pkg/executor/sortexec","pkg/util/chunk"],"pr":48872,"status":"has_high_or_blocker","subject":"executor: refine sort spill (#48872)","summary":"Commit refactors SortExec spill from `SortedRowContainer` to per-partition `sortPartition` + async OOM-triggered disk spill and k-way merge. A High-severity correctness edge exists: if disk spill fails, the error can race with one-time checks and the partition can still switch to disk-backed iteration with incomplete metadata, potentially truncating results."}