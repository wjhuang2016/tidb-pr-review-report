{"commit":"9906339471dc968624b3a046d77e98a5a0d97285","findings":[{"confidence":"High","evidence":"`pkg/util/hint/hint_processor.go:91` builds the hint list for `*ast.InsertStmt` as `x.TableHints` (hints after INSERT/REPLACE) followed by statement-level hints extracted from `x.Select` (hints after SELECT). `pkg/util/hint/hint.go:286` updates `hintOffs[\"memory_quota\"] = i` for every occurrence and `pkg/util/hint/hint.go:340` applies `hints[hintOffs[\"memory_quota\"]]` (“only the last definition takes effect”), so the SELECT-side MEMORY_QUOTA will win when both exist. But `pkg/util/hint/hint_processor.go:137` emits `ErrWarnConflictingHint` using the SELECT-side hint (`duplicatedHint`), i.e. it claims that specific SELECT hint is ignored.\nRuntime effect path: `pkg/planner/optimize.go:167` parses these hints into `sessVars.StmtCtx.StmtHints`, and `pkg/executor/adapter.go:531` applies `StmtCtx.MemQuotaQuery` to the session `MemTracker` limit.","fix":"Make duplicate handling consistent:\n- If INSERT/REPLACE-level MEMORY_QUOTA should have precedence, do not append the SELECT-side stmt hint when the same stmt hint already exists in `x.TableHints` (or explicitly drop SELECT-side `memory_quota` when INSERT-side exists), so the “ignored” warning becomes true and the effective quota is stable.\n- If “last textual occurrence wins” is intended, remove/replace `checkInsertStmtHintDuplicated` (or change it to warn that the earlier INSERT-side hint is ignored/overwritten), and avoid emitting contradictory warnings for the same conflict.\nAlso add a regression test covering differing values (e.g. INSERT 1MB vs SELECT 3MB) to lock the precedence semantics.","impact":"For `INSERT/REPLACE ... SELECT ...` containing MEMORY_QUOTA in both positions, the effective statement memory limit can become the SELECT-side value even though a warning claims the SELECT hint is ignored. This can cause unexpected statement cancellation/oom-action behavior (or unexpectedly higher memory use) and misleading diagnostics; it’s also a compatibility risk for users who relied on INSERT-side precedence or on warning correctness.","later_fix_evidence":"Not fixed later: `upstream/master` at `b75374faa619e24e380292fdbdf0f6fe5bf11943` still has the same `InsertStmt` hint concatenation order and the same `checkInsertStmtHintDuplicated` warning behavior in `pkg/util/hint/hint_processor.go`.","later_fixed":false,"severity":"High","verify":"Add/extend a unit test similar to `pkg/session/test/session_test.go:384`:\n- Run `insert /*+ MEMORY_QUOTA(1 MB) */ into t select /*+ MEMORY_QUOTA(3 MB) */ * from t;` and assert which limit is actually applied (via mem tracker limit or by forcing memory usage between 1MB and 3MB), and that `SHOW WARNINGS` matches the chosen precedence.\n- Repeat with bindings injecting the SELECT-side hint to ensure explicit query hints don’t get unexpectedly overridden." ,"what":"Conflicting MEMORY_QUOTA hints on `INSERT/REPLACE` (one after INSERT/REPLACE and one inside the SELECT subquery) can now be applied inconsistently with warnings: the code warns that the SELECT-side hint is ignored as conflicting/duplicated, but the extracted hint list plus ParseStmtHints “last one wins” logic makes the SELECT-side hint take effect."}],"modules":["pkg/util/hint","pkg/session/test","pkg/bindinfo"],"pr":54083,"status":"has_high_or_blocker","subject":"planner: fix the issue that statement-level hints in sub-queries of Insert/Replace can not take effect (#54083)","summary":"High: `ExtractTableHintsFromStmtNode` now appends SELECT-subquery statement hints for INSERT/REPLACE, but duplicate MEMORY_QUOTA handling becomes inconsistent—`checkInsertStmtHintDuplicated` warns the SELECT hint is ignored while `ParseStmtHints` resolves duplicates by last occurrence (SELECT appended last), so the effective memory quota can contradict warnings."}