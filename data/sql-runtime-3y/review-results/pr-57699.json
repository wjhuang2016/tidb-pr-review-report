{"commit":"0374ae26b922684abe29e7178c4167b018a1afb6","findings":[{"confidence":"High","evidence":"`pkg/executor/infoschema_reader.go:3907` exposes all entries from `domain.GetDomain(sctx).GetInstancePlanCache().All()` and returns `SQLText` / `ParseUser` / `ParseValues` etc with no `PROCESS` privilege check or per-user filtering.\n`pkg/infoschema/tables.go:1725` defines sensitive columns `SQL_TEXT` and `PARSE_VALUES` (LONG_BLOB).\nContrast: `information_schema.processlist` is filtered by `PROCESS` privilege / login user in `pkg/executor/infoschema_reader.go:1784`.","fix":"Gate `information_schema.tidb_plan_cache` behind `PROCESS` (or a suitable dynamic privilege like `RESTRICTED_TABLES_ADMIN`), or filter rows to current `user@host` unless `PROCESS` is granted.\nConsider redacting or omitting `PARSE_VALUES`/`SQL_TEXT` for non-privileged users (and under SEM), aligning with existing visibility rules for statement text in `processlist`-like tables.","impact":"Privilege/visibility violation: any user who can query `information_schema.tidb_plan_cache` can read other users’ cached SQL text, bindings, and compile-time parameter values (potential secrets/PII) from the instance plan cache. This can leak metadata and sensitive values across users/tenants.","later_fix_evidence":"Not fixed as of `upstream/master` (`3b531d4e3d99cb988bebccb8f63a96afff0ec6cd`): `upstream/master:pkg/executor/infoschema_reader.go` still builds plan-cache rows without any `PROCESS` privilege gating/filtering while returning `SQLText` and `ParseValues`.","later_fixed":false,"severity":"Blocker","verify":"1) Create two users `u1`, `u2` with minimal privileges.\n2) As `u1`, execute a prepared statement containing a sensitive parameter so it enters instance plan cache.\n3) As `u2` (without `PROCESS`), run `SELECT SQL_TEXT, PARSE_VALUES, PARSE_USER FROM information_schema.tidb_plan_cache`.\nExpected after fix: access denied (requires `PROCESS`/dynamic privilege) or only `u2`’s own entries are visible; no cross-user SQL/param leakage." ,"what":"`information_schema.tidb_plan_cache` exposes cached SQL text and parameter values for all users without privilege/visibility controls (no `PROCESS` gating or per-user filtering), enabling cross-user information disclosure."}],"modules":["executor","planner","sessionctx"],"pr":57699,"status":"has_high_or_blocker","subject":"planner: provide a system view information_schema.tidb_plan_cache to allow users to see info in the cache (#57699)","summary":"Adds `information_schema.tidb_plan_cache` backed by the instance plan cache, extends `PlanCacheValue` with metadata/runtime fields, and updates executor to accumulate per-plan runtime counters on statement finish."}