{"commit":"a7400e539ea88fd6413d121f27e071e8ce6de8e6","findings":[{"confidence":"High","evidence":"`pkg/sessionctx/variable/session.go`: `SessionVars.GetReplicaRead()` now returns `kv.ReplicaReadLeader` whenever `!StmtCtx.IsReadOnly`.\n`pkg/sessiontxn/isolation/base.go`: `getSnapshotByTS()` sets `kv.ReplicaRead` only on the *returned* `kv.Snapshot` via `snapshot.SetOption(kv.ReplicaRead, sessVars.GetReplicaRead())`. It does not proactively reset the active txn’s bound snapshot when the statement doesn’t call `GetSnapshotWithStmt*TS()`.\n`pkg/table/tables/tables.go`: `TableCommon.addRecord()` performs duplicate-key check via `txn.Get(ctx, key)` (point read) for normal tables.\n`pkg/store/driver/txn/txn_driver.go`: `SetOption(kv.ReplicaRead, ...)` and `SetOption(kv.SnapshotTS, ...)` mutate `txn.KVTxn.GetSnapshot()` (bound snapshot).\nclient-go confirms the bound snapshot is persistent (`txnkv/transaction/txn.go`: `GetSnapshot() { return txn.snapshot }`) and `SetSnapshotTS` doesn’t reset replica-read (`txnkv/txnsnapshot/snapshot.go`: only changes version/cache).","fix":"Ensure the active transaction’s bound snapshot replica-read is reset per statement (not only on snapshots returned by `getSnapshotByTS`).\nA minimal fix is to set `kv.ReplicaRead` on the active txn at statement start (or before any table mutation reads) based on the current statement’s read-only classification, e.g. in `baseTxnContextProvider.OnStmtStart` (compute read-only from `node`/AST, then apply) or in a central per-statement hook in `TxnManager`.\nKey requirement: DML statements must explicitly restore `ReplicaReadLeader` even if a prior read-only statement set follower on the txn snapshot.","impact":"Replica-read can leak across statements within the same explicit transaction:\n1) a read-only point-get (e.g. `SELECT ... WHERE pk=...`) can set the txn-bound snapshot’s `ReplicaRead` to follower;\n2) a later non-read-only statement that uses `txn.Get()` internally (notably `INSERT ... VALUES` duplicate-key checks) can still issue follower reads because the txn snapshot keeps the previous replica-read setting.\nThis violates the PR’s intended semantics (“replica read only affects read-only statements”) and can reintroduce follower reads in write statements.","later_fix_evidence":"Searched later history up to `upstream/master` (3b531d4e3d99cb988bebccb8f63a96afff0ec6cd) with `git log HEAD..upstream/master -S 'SetOption(kv.ReplicaRead'` and `-S 'GetReplicaReadUnadjusted'` on the relevant files; no follow-up change found that would reset txn snapshot replica-read per statement.","later_fixed":false,"severity":"High","verify":"Add an integration test (similar to `tests/realtikvtest/txntest/replica_read_test.go`) that runs in one session/txn:\n- `set session tidb_replica_read='follower'`;\n- `begin pessimistic`;\n- run a point-get `SELECT ... WHERE id=1` (expect `replica_read=true` on Get/BatchGet);\n- then run `INSERT INTO t VALUES (...)` (or another DML that triggers `txn.Get` duplicate checks) and assert the KV Get/BatchGet requests have `replica_read=false`.\nThis specifically catches cross-statement leakage via the txn-bound snapshot.","what":"Txn-bound snapshot `ReplicaRead` state can persist across statements, allowing follower reads during later non-read-only statements that use `txn.Get()` (e.g. `INSERT ... VALUES` duplicate-key checks) after a prior read-only point-get in the same explicit transaction."}],"modules":["pkg/sessionctx/variable","pkg/sessiontxn/isolation"],"pr":62852,"status":"has_high_or_blocker","subject":"txn: follower read only affect read-only statements (#62852)","summary":"Implements statement-level gating of replica read via `SessionVars.GetReplicaRead()` and applies it in `getSnapshotByTS()`, plus tests; however, txn-bound snapshot `ReplicaRead` can leak across statements for code paths that use `txn.Get()` without refreshing snapshot options per statement."}