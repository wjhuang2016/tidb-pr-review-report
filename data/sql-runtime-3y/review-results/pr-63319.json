{"commit":"9e69f9a5b33f11074ddf0a523880df4493d5b7c9","findings":[{"confidence":"High","evidence":"`pkg/distsql/select_result.go`:\n- `(*selectResult).fetchRespWithIntermediateResults` only releases prior response bytes when `r.selectResp != nil` (lines ~398-400: `if r.selectResp != nil { r.memConsume(-atomic.LoadInt64(&r.selectRespSize)) }`).\n- `(*selectResultIter).Next` drops the current response by setting `iter.result.selectResp = nil` after draining all channels (line ~950), but does not call `memConsume(-...)` nor reset `selectRespSize`.\n- `(*selectResult).close` only subtracts the *current* `selectRespSize` once (lines ~734-737: `respSize := atomic.SwapInt64(&r.selectRespSize, 0); r.memConsume(-respSize)`), so any previously-accounted response sizes that weren’t subtracted earlier remain leaked in the tracker.","fix":"When the iterator finishes a response and is about to drop it, release the tracked response bytes too. Minimal option in `(*selectResultIter).Next` right before `iter.result.selectResp = nil`:\n- `respSize := atomic.SwapInt64(&iter.result.selectRespSize, 0)`\n- `if respSize > 0 { iter.result.memConsume(-respSize) }`\n- then `iter.result.selectResp = nil`\nAlternative: don’t set `selectResp` to nil; instead keep it non-nil and add explicit “drained” state so the next `fetchRespWithIntermediateResults` can perform the existing `memConsume(-old)` path.","impact":"If/when `IntoIter()` is used on real cop responses (which commonly stream many `ResultSubset`s across regions), the DistSQL `MemTracker` will monotonically grow and never fully return to zero. This can:\n- Trigger statement/session memory quota enforcement incorrectly (query killed/aborted),\n- Skew memory accounting across statements if parent trackers aren’t force-reset,\n- Accumulate proportionally to number of cop responses (not live memory), causing severe false positives under large scans.","later_fix_evidence":"Checked later history up to `upstream/master` (`git show upstream/master:pkg/distsql/select_result.go`): `(*selectResultIter).Next` still ends with `iter.result.selectResp = nil` without any corresponding `memConsume(-...)`/`selectRespSize` reset, and `fetchRespWithIntermediateResults` still only subtracts old size when `r.selectResp != nil`. No later fix observed.","later_fixed":false,"severity":"High","verify":"Add/extend a unit test that:\n- Creates a `selectResult` with a non-nil `kvReq.MemTracker` (or a dedicated `memory.Tracker`),\n- Uses a mock `kv.Response` returning multiple non-nil `ResultSubset`s,\n- Consumes all rows via `IntoIter().Next()` until drained, then calls `iter.Close()`,\n- Asserts the tracker returns to its initial consumption (and does not grow linearly with number of subsets).","what":"`SelectResultIter` drops `selectResp` between cop responses without releasing the per-response memory accounting (`selectRespSize`), causing a memTracker leak/overcount across streamed responses."}],"modules":["executor"],"pr":63319,"status":"has_high_or_blocker","subject":"distsql: Add `SelectResultIter` to read rows from cop-task in the iterator way (#63319)","summary":"The new `SelectResultIter` API correctly iterates main + intermediate outputs, but it introduces a High-severity memory accounting leak: draining a response sets `selectResp=nil` without decrementing `selectRespSize` from the DistSQL `MemTracker`, so tracked memory grows with every streamed cop response and can incorrectly trip memory limits."}