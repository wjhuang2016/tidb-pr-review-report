{"commit":"4674b125fc76a0c0b109786d83977e7eeaa9b779","findings":[{"confidence":"High","evidence":"- `pkg/expression/builtin_cast.go:332` wraps CAST’s string->string argument via `HandleBinaryLiteral(..., explicitCast=true)`, which may build `from_binary` with `cannotConvertStringAsWarning=true`.\n- `pkg/expression/builtin_convert_charset.go:162` introduces `builtinInternalFromBinarySig.cannotConvertStringAsWarning`; `evalString` uses it to convert decode failures into warnings and return `NULL` in strict mode or truncated prefix in non-strict (`pkg/expression/builtin_convert_charset.go:175`).\n- Pushdown decode path does not carry this flag: `pkg/expression/distsql_builtin.go:1076` hard-codes `builtinInternalFromBinarySig{base, false}` with a TODO.\n- TiKV pushdown eligibility includes both `from_binary` and `cast`: `pkg/expression/infer_pushdown.go:193` (`InternalFuncFromBinary`) and `pkg/expression/infer_pushdown.go:217` (`ast.Cast`).\n=> So `CAST(varbinary_expr AS CHAR/…charset…)` can be pushed down but evaluated with `cannotConvertStringAsWarning=false`, diverging from TiDB’s intended warning/NULL behavior when invalid bytes appear.","fix":"- Encode `cannotConvertStringAsWarning` into pushdown (e.g., via scalar function metadata or a distinct PB signature) and implement matching behavior in the pushdown evaluator.\n- If that’s not feasible short-term, disable TiKV pushdown for `from_binary` when it’s created for explicit CAST (or blacklist that specific `cast.*` signature combination) to avoid plan-dependent behavior.","impact":"Plan-dependent SQL semantics: explicit `CAST(binary/varbinary AS CHAR CHARACTER SET …)` on rows containing invalid sequences may return warning+NULL/truncation on TiDB but error out when pushed down to TiKV (or vice versa). This can cause query failures/regressions that depend on optimization/pushdown decisions, affecting compatibility and correctness in distributed scans.","later_fix_evidence":"Checked `upstream/master` (at `b75374faa619e24e380292fdbdf0f6fe5bf11943`): `pkg/expression/distsql_builtin.go` still constructs `builtinInternalFromBinarySig{base, false}` for `ScalarFuncSig_FromBinary` and the TODO about setting `cannotConvertStringAsWarning` remains; no propagation mechanism is present in `pkg/expression/builtin_convert_charset.go`.","later_fixed":false,"severity":"High","verify":"- Add an integration test that forces TiKV pushdown (table scan) for `SELECT CAST(v AS CHAR) FROM t` where `v` is `VARBINARY` containing invalid utf8 bytes.\n- Run under default SQL mode (strict) and `sql_mode=''`, asserting both result values and warnings are identical between pushdown-enabled and pushdown-disabled plans (e.g., via pushdown failpoints/blacklist toggles).","what":"`from_binary`’s new “warn instead of error” mode for explicit CAST is not preserved across expression pushdown, so TiDB vs TiKV can diverge on invalid-byte conversions."}],"modules":["pkg/expression"],"pr":51191,"status":"has_high_or_blocker","subject":"expression: fix charset conversion warning and error behavior (#51191)","summary":"Adds warning/NULL behavior for invalid binary->charset conversions during explicit CAST via a new `cannotConvertStringAsWarning` flag, but the flag is not propagated through pushdown/PB decoding, creating plan-dependent TiDB vs TiKV semantic divergence."}