{"commit":"42edc2239ba83643063538ebdf095255044c186c","findings":[{"confidence":"Medium","evidence":"- Regression in `pkg/planner/core/plan_to_pb.go:276`: this commit now blindly appends all `p.UsedColumnarIndexes[*].QueryInfo` into `tsExec.UsedColumnarIndexes` with no `TopK` guard.\n- First-parent version had an explicit safety guard: `42edc^:pkg/planner/core/plan_to_pb.go:277` checked `p.AnnIndexExtra.PushDownQueryInfo.TopK != math.MaxUint32` before emitting the vector `ColumnarIndexInfo`.\n- That guard was introduced by `cbf34b01b508958c049a92a3eaa1565167eb5078` (message: \"planner: do not set AnnQuery when TopK = MaxUint32 (#57307)\") referencing bug `#54245`, indicating `TopK==MaxUint32` is a known-bad/sentinel case.\n- `TopK` is derived from `LogicalTopN.Count/Offset` (both `uint64`, see `pkg/planner/core/operator/logicalop/logical_top_n.go:38-40`) via `resultProp.VectorProp.TopK = uint32(lt.Count + lt.Offset)` (`pkg/planner/core/exhaust_physical_plans.go:2874`), so `TopK` can equal `math.MaxUint32` for certain inputs (or wrap to it). The vector query is then built with that `TopK` (`pkg/planner/core/columnar_index_utils.go:38-47`, wired from `pkg/planner/core/find_best_task.go:2698-2709`).","fix":"Re-introduce the `TopK != math.MaxUint32` guard for vector ANN pushdown when serializing to TiFlash protobuf. Minimal options:\n- In `PhysicalTableScan.ToPB` / partition variant: skip appending `idx.QueryInfo` when `idx.QueryInfo.IndexType==TypeVector` and `idx.QueryInfo.GetAnnQueryInfo().TopK == math.MaxUint32`.\n- Or earlier: avoid creating/appending the vector `ColumnarIndexExtra` when `prop.VectorProp.TopK == math.MaxUint32` (keeps `EXPLAIN`/cost consistent with actual pushdown).","impact":"Potentially reintroduces the previously-fixed edge case where TiFlash receives an ANN/vector query with `TopK == MaxUint32` (sentinel/known-bad per #57307/#54245). Depending on TiFlash handling, this can cause incorrect runtime behavior (wrong/missing results from ANN pushdown) or stability/performance issues (e.g., pathological TopK).","later_fix_evidence":"Checked `upstream/master` and did not find a reintroduced `TopK == MaxUint32` suppression in the equivalent serialization path (`pkg/planner/core/operator/physicalop/physical_table_scan.go` appends `QueryInfo` without filtering; `git grep MaxUint32 upstream/master -- pkg/planner/core` only hits unrelated preprocess code).","later_fixed":false,"severity":"High","verify":"- Create a TiFlash table with a vector index, run a query that produces `TopK == 4294967295` (e.g., `ORDER BY vec_distance(...) LIMIT 4294967295` or any scenario that yields `TopK==math.MaxUint32`), and confirm the generated TiFlash `TableScan.UsedColumnarIndexes` does **not** include a `TypeVector` ANN query.\n- Add a planner unit/case test that asserts the emitted protobuf omits vector `ColumnarIndexInfo` when `TopK == math.MaxUint32` (regression test for #57307 semantics).","what":"`plan_to_pb` drops the historical `TopK != math.MaxUint32` guard and may push down an ANN/vector query with `TopK==MaxUint32`, regressing the safety behavior added in #57307 (ref #54245)."}],"modules":["planner"],"pr":60522,"status":"has_high_or_blocker","subject":"planner: generalize vector index to columnar index (#60522)","summary":"Found a likely High-severity regression: TiFlash ANN/vector pushdown no longer suppresses the known `TopK == math.MaxUint32` sentinel case (previously guarded by #57307), which may reintroduce the associated runtime/protocol bug."}