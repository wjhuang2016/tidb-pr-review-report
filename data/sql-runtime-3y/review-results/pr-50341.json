{"commit":"12d273e00f8373312c73bf9f397d490864336c4c","findings":[{"confidence":"High","evidence":"`IMPORT INTO ... FROM SELECT` fan-outs encoding across `ti.ThreadCnt` workers, each creating its own KV-encoding session with `Timestamp` coming from an empty `checkpoints.ChunkCheckpoint{}`.\n- Query path uses `chunkCheckpoint := checkpoints.ChunkCheckpoint{}` (Timestamp defaults to 0): `pkg/executor/importer/table_import.go:699`.\n- KV encoder uses `encode.SessionOptions{Timestamp: chunk.Timestamp}`: `pkg/executor/importer/table_import.go:355`.\n- When the session sysvar `timestamp` is `DefTimestamp (\"0\")`, TiDB resolves it to a per-session cached `time.Now()` (statement cache): `pkg/sessionctx/variable/sysvar.go:93`.\n- Time-dependent evaluation (e.g. `CURRENT_TIMESTAMP`, `NOW()`) is supposed to be stable “during one sql statement”: `pkg/expression/helper.go:159` (`getStmtTimestamp`).\nSecond confirmation (contrast with file import): file-import pins a single timestamp across all chunks (`timestamp := time.Now().Unix()` and stored into `ChunkCheckpoint.Timestamp`): `pkg/executor/importer/table_import.go:450`.\nLater-history check: still present on `upstream/master@b75374faa6` (query path still doesn’t attach a shared timestamp to query chunks / checkpoints; encoder still uses `chunk.Timestamp`): `pkg/executor/import_into.go:305` (upstream/master numbering from `git show`), `pkg/executor/importer/table_import.go:364`, `pkg/executor/importer/table_import.go:722`.","fix":"Pin a single statement timestamp for the whole `IMPORT INTO ... FROM SELECT` execution and feed it into all KV encoders.\n- Minimal: in `ImportSelectedRows`, compute one `ts := time.Now().Unix()` and set `chunkCheckpoint.Timestamp = ts` before calling `ProcessChunk` (or set a shared timestamp field on `TableImporter/LoadDataController` and have `getKVEncoder` fall back to it when `chunk.Timestamp==0`).\n- Better semantic match: derive the timestamp from the user statement’s “now” (the same one used by the SQL layer) and propagate that exact value into all encoding sessions.\nThis ensures `NOW()/CURRENT_TIMESTAMP` used by defaults, generated columns, or `SET` clause evaluates consistently across worker goroutines when `WITH thread > 1`.","impact":"Correctness/SQL-semantics drift for time-dependent expressions under parallel import-from-select.\n- If a target column is omitted and has default `CURRENT_TIMESTAMP`/`CURRENT_DATE` (or a generated column / `SET` clause expression uses `NOW()`/`CURRENT_TIMESTAMP`), different worker encoders can observe different per-session “statement time”, so rows in the same statement can get different timestamps.\n- This violates TiDB’s own stated invariant that `current_time` is constant within one SQL statement (`getStmtTimestamp`) and can lead to subtle data inconsistencies (especially noticeable when `WITH thread > 1`).","later_fix_evidence":"Checked `upstream/master@b75374faa619e24e380292fdbdf0f6fe5bf11943`: import-from-select still sends query chunks without an attached shared timestamp and the KV encoder still uses `chunk.Timestamp` from an empty `ChunkCheckpoint` (see `git show` line numbers: `pkg/executor/import_into.go:305`, `pkg/executor/importer/table_import.go:364`, `pkg/executor/importer/table_import.go:722`).","later_fixed":false,"severity":"High","verify":"Manual repro:\n1) `create table src(id int); insert into src values (1),(2),(3),(4);`\n2) `create table dst(id int, ts timestamp default current_timestamp);`\n3) `import into dst(id) from select id from src with thread = 8;`\n4) `select count(distinct ts) from dst;` Expect `1` if statement-time is pinned; >1 indicates per-worker divergence.\nAlso test `SET` clause: `import into dst(id) set ts = now() from select id from src with thread = 8;` and assert `count(distinct ts)=1`.","what":"`IMPORT INTO ... FROM SELECT` does not pin a single statement timestamp across parallel encoding workers, so `NOW()/CURRENT_TIMESTAMP`-based defaults / generated columns / `SET` clause expressions can evaluate to different times within the same statement when `WITH thread > 1`."}],"modules":["pkg/executor/import_into.go","pkg/executor/importer/import.go","pkg/executor/importer/table_import.go","pkg/executor/importer/engine_process.go","pkg/executor/importer/chunk_process.go"],"pr":50341,"status":"has_high_or_blocker","subject":"importinto: executor part of import from select (#50341)","summary":"Executor-side `IMPORT INTO ... FROM SELECT` is implemented by streaming SELECT output into the local-import backend with parallel encoding. One High-severity semantic issue remains: statement-time (`NOW()/CURRENT_TIMESTAMP`) is not pinned across worker encoders when `WITH thread > 1`, so time-dependent defaults/generated columns/SET-clause expressions can diverge across rows; this is still present on `upstream/master`."}