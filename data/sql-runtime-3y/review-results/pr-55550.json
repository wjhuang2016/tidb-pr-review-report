{"commit":"48989e726f34aeb215059c1d1454915ed754e1f8","findings":[{"confidence":"High","evidence":"`pkg/executor/aggfuncs/func_first_row.go`: `firstRow4VectorFloat32` is added (around `type firstRow4VectorFloat32 struct`) but does not implement `SerializePartialResult` / `DeserializePartialResult` (unlike `firstRow4JSON`, which does).\n`pkg/executor/aggfuncs/func_max_min.go`: `maxMin4VectorFloat32` is added (around `type maxMin4VectorFloat32 struct`) but does not implement `SerializePartialResult` / `DeserializePartialResult` (unlike `maxMin4JSON`, which does).\n`pkg/executor/aggfuncs/aggfuncs.go`: `baseAggFunc.SerializePartialResult` is a no-op and `baseAggFunc.DeserializePartialResult` returns `(nil, 0)`.\n`pkg/executor/aggregate/agg_hash_partial_worker.go`: spill path calls `aggFunc.SerializePartialResult(partialResults[i], tmpChk, ...)` for every agg func when spilling.\n`pkg/executor/aggregate/agg_spill.go`: restore path calls `aggFunc.DeserializePartialResult(chunk)` and then indexes `partialResultsRestored[aggPos][rowPos]`; a base no-op deserialize yields a nil slice and will panic on indexing (or otherwise corrupt spill chunks due to missing appended rows).","fix":"Implement spill serialization for vector partial results, mirroring the JSON patterns:\n- Add `SerializePartialResult` and `DeserializePartialResult` + `deserializeForSpill` for `firstRow4VectorFloat32` in `pkg/executor/aggfuncs/func_first_row.go`.\n- Add `SerializePartialResult` and `DeserializePartialResult` + `deserializeForSpill` for `maxMin4VectorFloat32` in `pkg/executor/aggfuncs/func_max_min.go`.\n- Extend `pkg/executor/aggfuncs` spill helpers (`SerializeHelper` / `deserializeHelper`) with vector-specific (de)serialization routines that encode/decode: null flags + (for first_row) gotFirstRow + vector bytes (e.g., `VectorFloat32.ZeroCopySerialize()` / `ZeroCopyDeserializeVectorFloat32`).\n- Ensure serialized bytes are appended via `chk.AppendBytes(e.ordinal, resBuf)` so spilled chunks have consistent row counts across all aggregate columns.","impact":"Queries that use `MAX(vector)` / `MIN(vector)` or `FIRST_ROW(vector)` (including implicit `first_row` used by some GROUP BY projections) can fail with a panic or incorrect results when hash aggregation enters spill-to-disk mode (OOM/spill enabled). This is a correctness + runtime-stability risk that manifests under memory pressure / large aggregations.","later_fix_evidence":"Checked `upstream/master` (3b531d4e3d99cb988bebccb8f63a96afff0ec6cd): `pkg/executor/aggfuncs/func_first_row.go` still has `firstRow4VectorFloat32` without `SerializePartialResult`/`DeserializePartialResult`, and `pkg/executor/aggfuncs/func_max_min.go` still has `maxMin4VectorFloat32` without them.","later_fixed":false,"severity":"High","verify":"With spill enabled (e.g. `EnableTmpStorageOnOOM` + parallel hash agg spill session vars) and a small memory quota to force spilling, run large GROUP BYs using vector columns:\n- `SELECT MAX(c) FROM t GROUP BY k;` where `c` is `VECTOR(...)`\n- `SELECT c FROM t GROUP BY k;` (to exercise implicit `first_row`)\nVerify the query completes without panic and matches results when spill is disabled / memory quota is large." ,"what":"VectorFloat32 implementations of `MAX/MIN` (`maxMin4VectorFloat32`) and `FIRST_ROW` (`firstRow4VectorFloat32`) do not implement spill serialization/deserialization. In hash-aggregation spill mode this will produce malformed spill chunks and/or panic during restore (nil partial result slices), causing query failure or wrong results."}],"modules":["executor","expression","kv","parser","planner","table"],"pr":55550,"status":"has_high_or_blocker","subject":"*: Vector Data types and Functions (#55550)","summary":"Adds VectorFloat32 type support and vec_* functions across parser/planner/expression/executor plus pushdown wiring. High-risk gap: vector `MAX/MIN` and `FIRST_ROW` aggregate implementations lack spill (de)serialization, which can break hash-agg spill-to-disk (panic/wrong results) and is not fixed on upstream/master."}