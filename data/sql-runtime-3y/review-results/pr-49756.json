{"commit":"07e9ece9107536fad2d5df7664809350f0ce0a4a","findings":[{"confidence":"High","evidence":"- pkg/expression/aggregation/base_func.go:143: `typeInfer4ApproxPercentile` now uses `a.Args[1].ConstItem(false)` and does *all* percent validation (`percent <= 0 || percent > 100`) only during TypeInfer (planner-time).\n- pkg/expression/util.go:1166: `ParamMarkerExpression` sets `Constant.ParamMarker` when `StmtCtx.UseCache` is true (prepared plan cache enabled), meaning the cached plan’s percent expression can evaluate to *different* values across executions while still being `ConstItem(false)`.\n- pkg/planner/core/plan_cache.go:166: `StmtCtx.UseCache` is set before plan generation; pkg/planner/core/plan_cache.go:352 shows cache-hit handling only calls `RebuildPlan4CachedPlan` which only runs `rebuildRange` (no re-TypeInfer / no re-validation of aggregate args).\n- pkg/executor/aggfuncs/builder.go:147: `buildApproxPercentile` re-evaluates `aggFuncDesc.Args[1]` each execution but does not re-check range; it relies on the comment “Checked while building descriptor”.\n- pkg/executor/aggfuncs/func_percentile.go:41: `percentile()` computes `k := ceil(len * percent/100)` and only clamps `k > len`; for `percent <= 0`, `k <= 0` flows into pkg/util/selection/selection.go:26 (`Select(..., k)`), which passes `k-1` to `introselect`; pkg/util/selection/selection.go:84 (`randomPivot`) can panic when recursion reaches `right < left` (mod by `right-left+1 == 0`).","fix":"Pick one:\n1) Re-validate `percent` on every execution (executor side): add a range guard in `buildApproxPercentile` (or before calling `selection.Select`) so `percent` outside `[1,100]` returns a proper SQL error instead of reaching `selection.Select`.\n2) If runtime validation is not feasible, make the statement uncacheable for prepared plan cache when the percent argument is not strict-constant across contexts (i.e., depends on params), so TypeInfer runs each execution and the existing range check remains effective.","impact":"- Correctness/compatibility: a cached prepared statement using `APPROX_PERCENTILE(col, ?)` can bypass the percent range check on cache hits when `?` changes later.\n- Stability: `percent <= 0` can drive `k <= 0` into `selection.Select`, leading to a possible panic (process crash / DoS) instead of returning a SQL error.\n- Semantics: `percent > 100` would silently clamp to 100th percentile on cache hit, contradicting the planner-time “out of range” error.","later_fix_evidence":"Checked later history up to upstream/master (b75374faa6): `buildApproxPercentile` still does not validate the percent range, and `selection.Select` still assumes `k >= 1`; no later fix found for this specific plan-cache bypass.","later_fixed":false,"severity":"Blocker","verify":"Manual SQL repro (prepared plan cache enabled):\n1) `set tidb_enable_prepared_plan_cache=ON;`\n2) `prepare s from 'select approx_percentile(a, ?) from t';`\n3) `set @p=50; execute s using @p;` (populate cache)\n4) `set @p=0; execute s using @p;` (should return “out of range” error; must not panic)\nAlso check `select @@last_plan_from_cache;` to confirm step (4) is a cache hit (or verify plan-cache skip behavior if you choose fix #2).","what":"`APPROX_PERCENTILE` percent constness was loosened to `ConstItem(false)` in TypeInfer, allowing parameter-dependent percent values under prepared plan cache. Because percent range validation only happens during TypeInfer (not on cached-plan reuse), later executions with different `?` can bypass the range check and feed invalid `percent` into executor percentile selection, potentially causing a panic (`percent <= 0`) or wrong semantics (`percent > 100`)."}],"modules":["pkg/expression","pkg/expression/aggregation"],"pr":49756,"status":"has_high_or_blocker","subject":"expression: pass const bool to all calls for expression.ConstItem (#49756)","summary":"Reviewed commit 07e9ece… (first-parent diff) in `pkg/expression/*`.\n\nMost ConstItem callsite changes look consistent with intended semantics (strict-const vs const-only-in-context) and are covered by updated tests, but `APPROX_PERCENTILE` now accepts parameter-dependent percent values under prepared plan cache while only validating the percent range at TypeInfer time. Cached-plan reuse can bypass that validation and reach executor code paths that can panic for `percent <= 0`."}