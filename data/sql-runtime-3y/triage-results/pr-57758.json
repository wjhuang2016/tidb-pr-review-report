{"confidence":"high","decision":"skip","pr":57758,"reason":"PR is centered on TTL default configuration behavior, mainly in DDL/TTL worker and table metadata. The only target-module touch is `pkg/executor/show.go`, which looks incidental for exposing the new default in `SHOW` output, plus matching result updates. This does not look clearly centered on executor/table runtime semantics, planner, expression, session, statistics, kv/store, parser, or privilege behavior.","target_modules":["executor","table"]}