{"confidence":"high","decision":"deep_review","pr":55717,"reason":"This is centered on table-creation defaults, not incidental touches. The metadata shows core logic in `pkg/ddl/create_table.go` plus `pkg/sessionctx/variable/*` sysvar/session-variable changes that control default `shard_row_id_bits` and `pre_split_regions` for all tables. That makes it directly a `table` and `sessionctx` behavior change; the executor file is only a test.","target_modules":["table","sessionctx"]}