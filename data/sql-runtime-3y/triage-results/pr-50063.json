{"confidence":"high","decision":"deep_review","pr":50063,"reason":"The PR is centered on SQL plan binding handle logic (`pkg/bindinfo/*`) and touches the executor bind path (`pkg/executor/bind.go`). Even though only one changed file is in the heuristic target set, global/session bindings directly affect planner behavior and can also affect session-scoped semantics, so this is plausibly core rather than incidental.","target_modules":["planner","executor","session"]}