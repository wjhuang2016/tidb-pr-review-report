{"confidence":"high","decision":"skip","pr":65411,"reason":"The PR is centered on DDL safeguards for system table IDs, with the substantive logic apparently in `pkg/ddl/executor.go`. The only target-area code touch is `pkg/executor/ddl.go`, which looks like DDL plumbing rather than a planner/executor/runtime semantic change. Other target-related files are tests under session/statistics/privilege, so the focus is not clearly on the requested core modules/behaviors.","target_modules":["pkg/executor/ddl.go"]}