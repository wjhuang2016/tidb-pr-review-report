{"confidence":"high","decision":"deep_review","pr":56429,"reason":"The PR is entirely in parser files and changes the default vector index type to HNSW, which is a SQL parsing/default-semantics change rather than an incidental touch. Because it directly affects how statements are interpreted when index type is omitted, it is clearly centered on parser behavior.","target_modules":["parser"]}