{"confidence":"high","decision":"skip","pr":51436,"reason":"The PR metadata reads like an infoschema-wide API refactor (`AllSchemas` -> `AllSchemaNames`), not a change centered on planner/executor/expression/session semantics. Although it touches `pkg/executor`, `pkg/session`, and `pkg/statistics`, those look like downstream call-site updates around schema enumeration rather than core behavioral ownership; the broader file list is dominated by `pkg/infoschema` and unrelated consumers (`ddl`, `br`, `ttl`, server handler, tests).","target_modules":["executor","session","statistics"]}