{"confidence":"high","decision":"deep_review","pr":60520,"reason":"PR is entirely in planner code (`pkg/planner/core` and `pkg/planner/property`) and fixes a `PhysicalExchangeSender::ResolveIndicesItself` bug, which is core physical-plan/index-resolution correctness. The only non-code change is a planner test, so this is clearly centered on planner semantics rather than incidental touches.","target_modules":["planner"]}