{"confidence":"high","decision":"deep_review","pr":55793,"reason":"The PR is centered on core `expression` and `planner` code, not incidental touches. It introduces a new `hashEquals` interface on `expression.Expression` and updates multiple concrete expression types plus a planner-side scalar subquery expression, which plausibly affects planner/expression semantics and equality/hash behavior with broad optimizer impact.","target_modules":["expression","planner"]}