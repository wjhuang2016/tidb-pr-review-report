{"confidence":"high","decision":"deep_review","pr":66162,"reason":"The PR is explicitly about extending `PLAN REPLAYER DUMP EXPLAIN` to accept multiple SQL statements, and the changed files are concentrated in parser, planner, and executor code paths rather than incidental tests or infra. That directly touches SQL grammar/AST (`pkg/parser/**`), plan-building semantics (`pkg/planner/core/**`), and executor wiring (`pkg/executor/builder.go`), which are high-blast-radius areas for statement parsing and runtime behavior.","target_modules":["planner","executor","parser"]}