{"confidence":"medium","decision":"skip","pr":60787,"reason":"The metadata points to a tooling-driven refactor, not a core behavior change: the subject is `executor: gopls modernize`, the edits span many executor files and tests in a sweep-like pattern, and `build/nogo_config.json` is also touched. That suggests broad mechanical modernization inside executor-owned code rather than a PR centered on executor/runtime semantics, planner logic, statistics, KV/store, parser, table, or privilege behavior.","target_modules":["executor"]}