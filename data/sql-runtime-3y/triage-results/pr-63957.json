{"confidence":"high","decision":"deep_review","pr":63957,"reason":"The PR is explicitly scoped to `executor` and its main touched target paths are HashJoin v2 runtime code under `pkg/executor/join/*`. Even though many supporting files are in `types`/`util/codec`/`collate`, the subject and target paths indicate those are likely enabling changes for executor key preallocation rather than unrelated infra. HashJoin is a core executor path with meaningful runtime/correctness blast radius, so this is review-worthy.","target_modules":["executor"]}