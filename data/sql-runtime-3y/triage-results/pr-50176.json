{"confidence":"high","decision":"skip","pr":50176,"reason":"The subject is explicitly test-focused, and the touched session path is `pkg/session/mock_bootstrap.go`, which reads like mock/bootstrap test support rather than core session/runtime behavior. With only test files plus a mock helper changed, this looks like test-fixture maintenance, not a PR centered on the target modules’ production semantics.","target_modules":["session"]}