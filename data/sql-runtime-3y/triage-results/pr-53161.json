{"confidence":"high","decision":"deep_review","pr":53161,"reason":"The PR is entirely within planner-owned code paths and explicitly changes planner utility logic (`hashcode` encoding and `HasMaxOneRow`) rather than only tests, docs, or infra. Even though it looks partly like a refactor, these helpers are tied to planner behavior/correctness, so it is clearly centered on planner concerns.","target_modules":["planner"]}