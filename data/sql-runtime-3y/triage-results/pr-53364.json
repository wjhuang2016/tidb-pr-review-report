{"confidence":"high","decision":"deep_review","pr":53364,"reason":"The PR is clearly centered on planner behavior: it changes `pkg/planner/core/point_get_plan.go` to alter fast-point-get planning for `SELECT ... INTO OUTFILE`, which is a user-visible optimizer/plan-selection semantic path. The executor file is only a test, so the core concern remains planner semantics rather than incidental touches.","target_modules":["planner"]}