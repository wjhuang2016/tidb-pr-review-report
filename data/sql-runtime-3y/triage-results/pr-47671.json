{"confidence":"high","decision":"deep_review","pr":47671,"reason":"The PR is explicitly about planner behavior (`planner: implementing multi index merge from CNF/DNF condition`) and its non-test code changes are concentrated in `pkg/planner/core/*`, which directly affects optimizer/path selection semantics. The other touched files are supporting tests/metadata, so this is clearly centered on planner logic rather than incidental planner impact.","target_modules":["planner"]}