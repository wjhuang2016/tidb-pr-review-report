{"confidence":"high","decision":"deep_review","pr":60698,"reason":"The PR is centered on `sessionctx` and `store` code paths, with changes to session variables (`sysvar.go`, `error.go`) and transaction driver behavior (`txn_driver.go`). Disabling fair locking in next-gen plausibly changes session/transaction runtime semantics rather than being an incidental test or infra touch.","target_modules":["sessionctx","store"]}