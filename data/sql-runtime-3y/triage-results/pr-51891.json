{"confidence":"high","decision":"deep_review","pr":51891,"reason":"The PR is clearly centered on core `expression` and `planner` behavior: every listed code path is in those modules, and the subject introduces a new `PushDownContext` used by pushdown, plan-to-PB, predicate pushdown, and physical planning paths. That is directly tied to optimizer and expression semantics rather than incidental touches.","target_modules":["expression","planner"]}