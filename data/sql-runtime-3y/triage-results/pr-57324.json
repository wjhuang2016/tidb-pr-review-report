{"confidence":"medium","decision":"deep_review","pr":57324,"reason":"The change is fully concentrated in parser-owned core types (`pkg/parser/model/model.go` and `pkg/parser/types/field_type.go`), and adding `hash64`/`equals` behavior for `CIStr` is plausibly correctness-sensitive because `CIStr` is a widely used identifier type. Even though the title says planner, the metadata suggests a parser-core type change rather than incidental parser touch.","target_modules":["parser"]}