{"confidence":"high","decision":"skip","pr":60474,"reason":"The PR is primarily a TTL subsystem change (`use AdvancedSessionPool in TTL`) with most edits under `pkg/ttl/**`. The lone focus-area touch in `pkg/session/syssession/session.go` looks like supporting infrastructure for TTL rather than a PR centered on session semantics or other high-blast-radius core behaviors.","target_modules":["session"]}