{"confidence":"high","decision":"skip","pr":65885,"reason":"The only target-module code path is `pkg/executor/plan_replayer.go`, but the change is about plan replayer load handling for semicolons, which is diagnostic/tooling behavior rather than core planner/executor/runtime, parser SQL semantics, or session/kv/statistics correctness. The rest of the diff is Bazel metadata and a handler test, so this looks like an executor-adjacent support fix, not a PR centered on high-blast-radius target-module behavior.","target_modules":["executor"]}