{"confidence":"high","decision":"deep_review","pr":63104,"reason":"The PR is centered on statistics initialization behavior: it adds a config-controlled path in `pkg/domain/domain.go` and validates it with tests under `pkg/statistics/handle/...`. Even though the touched runtime code is in domain/config, the stated effect is to skip init stats, which directly affects the statistics subsystem and can plausibly change planner cost/cardinality behavior.","target_modules":["statistics"]}