{"confidence":"high","decision":"deep_review","pr":52542,"reason":"The PR is explicitly about planner behavior (`UnionAll` range bump handling), and the only substantive target code path is `pkg/planner/core/logical_plan_builder.go`, which is core planner/optimizer logic. The other changes are supporting tests plus an incidental `pkg/types/field_type.go` touch, so this is clearly centered on planner semantics rather than infra or peripheral code.","target_modules":["planner"]}