{"confidence":"medium","decision":"deep_review","pr":54028,"reason":"Although labeled as a DDL fix, the only non-test code change is in planner preprocessing and the behavior concerns SQL type semantics for decimal column definitions. That makes the planner-side DDL/type handling look like the primary implementation surface rather than an incidental touch.","target_modules":["planner","table"]}