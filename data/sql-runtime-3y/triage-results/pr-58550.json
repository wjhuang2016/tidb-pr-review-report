{"confidence":"high","decision":"skip","pr":58550,"reason":"The PR is centered on `domain`/context API optimization, not on planner/executor/expression/session semantics. The only target-module code paths are `pkg/session/session.go` and `pkg/planner/core/mock.go`, which look like incidental call-site adaptations to the new `GetDomain` plumbing; executor changes are test-only. From metadata alone, this reads as infra/API reshaping with downstream touch points, not a core planner/executor/session behavior change.","target_modules":["session","planner"]}