{"confidence":"high","decision":"deep_review","pr":64253,"reason":"Although the title says 'add test', the metadata shows multiple production changes across core `pkg/statistics/**` paths: bootstrap/loading, storage reads, syncload, and column/index/table/histogram logic. That is clearly centered on statistics behavior rather than incidental test churn, and it can affect stats initialization and downstream cardinality/cost behavior.","target_modules":["statistics"]}