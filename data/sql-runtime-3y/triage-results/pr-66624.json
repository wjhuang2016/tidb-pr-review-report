{"confidence":"high","decision":"deep_review","pr":66624,"reason":"The PR is explicitly planner-focused and changes the default of a sessionctx system variable (`tidb_ignore_inlist_plan_digest`) with accompanying planner normalization tests. Even though the only production code path shown is `pkg/sessionctx/vardef/tidb_vars.go`, the stated behavior change is about planner plan-digest handling by default, so it is centered on target planner/sessionctx behavior rather than incidental infra or test-only work.","target_modules":["planner","sessionctx"]}