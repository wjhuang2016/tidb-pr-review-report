{"confidence":"high","decision":"skip","pr":55758,"reason":"The metadata points to a DDL-internal event refactor, not a statistics-logic change. Although two touched files are under `pkg/statistics`, their paths (`handle/ddl` and `util/ddl_event`) look like downstream adaptation to a new schema-change event type, with no clear signal of cost-model/cardinality/runtime semantics impact. This fits incidental target-module touch rather than a PR centered on the requested core areas.","target_modules":["statistics"]}