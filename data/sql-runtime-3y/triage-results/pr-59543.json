{"confidence":"high","decision":"deep_review","pr":59543,"reason":"The PR’s only non-test code change is in `pkg/executor/importer/import.go`, and the subject indicates a user-visible `IMPORT INTO` behavior change: automatic file type recognition from filenames. That is executor runtime semantics rather than incidental infrastructure, so it is genuinely centered on a target module.","target_modules":["executor"]}