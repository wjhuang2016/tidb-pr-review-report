{"confidence":"high","decision":"deep_review","pr":66387,"reason":"The PR is explicitly about prepared-parameter compatibility in `GROUP BY`/`ORDER BY`, which is planner/executor SQL semantics with user-visible behavior. The primary code change is in `pkg/planner/core/logical_plan_builder.go`, and the executor-side change is a regression test, so this is clearly centered on target modules rather than incidental touches.","target_modules":["planner","executor"]}