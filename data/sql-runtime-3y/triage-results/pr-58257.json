{"confidence":"high","decision":"deep_review","pr":58257,"reason":"The PR is centered on core `executor` and `planner` code paths, not incidental touches. It changes `infoschema_reader` and `indexusage` logic plus the planner-side infoschema extractor, which directly affects user-visible `INFORMATION_SCHEMA`/index-usage behavior and executor-side correctness for reporting primary-key index usage.","target_modules":["executor","planner"]}