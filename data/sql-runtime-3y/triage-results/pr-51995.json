{"confidence":"high","decision":"deep_review","pr":51995,"reason":"The PR is entirely within `pkg/executor/infoschema_reader.go` and the subject indicates a refinement to the `infoschemaReader` column-fetch path. That is an executor-side, user-visible `information_schema` behavior change rather than an incidental touch, so it is plausibly in scope for executor/runtime correctness review.","target_modules":["executor"]}