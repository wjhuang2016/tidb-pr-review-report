{"confidence":"high","decision":"deep_review","pr":52311,"reason":"The PR is clearly centered on core expression semantics and context plumbing, not incidental touches. The subject is an `expression` API split (`BuildContext` vs `EvalContext`), and the changed paths heavily span `pkg/expression/**`, `pkg/planner/**`, and `pkg/executor/**` with very high target ratio/count, which strongly suggests planner rewrite/build behavior and executor/runtime evaluation behavior are being refactored together. `sessionctx` is also directly touched via expression context implementation, with some supporting propagation into store/table paths.","target_modules":["expression","planner","executor","sessionctx"]}