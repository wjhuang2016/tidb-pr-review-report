{"confidence":"high","decision":"deep_review","pr":53279,"reason":"The PR is explicitly centered on planner behavior: it moves noop-function checks within `SELECT` handling from `logical_plan_builder` to `preprocess`, both under `pkg/planner/core`. That is a core SQL-semantics/planning-path change, not an incidental touch. The integration test update in `expression/noop_functions` appears to validate planner-visible behavior rather than indicating the change is merely test-only or infra-related.","target_modules":["planner"]}