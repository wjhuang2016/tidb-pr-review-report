{"confidence":"high","decision":"deep_review","pr":56703,"reason":"The PR is centered on parser SQL semantics: it changes only `pkg/parser/parser.y`/generated `parser.go`, plus parser and integration tests, to reject/support syntax around `ALTER TABLE ... DROP FOREIGN KEY IF EXISTS`. That is a direct parser-behavior change, not an incidental touch.","target_modules":["parser"]}