{"confidence":"high","decision":"skip","pr":55738,"reason":"The PR is parser-scoped by path, but the subject and file mix point to a toolchain/runtime upgrade (`upgrade golang version to 1.21`) rather than parser SQL semantics. `go.mod` and `BUILD.bazel` changes are infra signals, and the code touches in `charset.go`, `goyacc/main.go`, and `field_type.go` look more like compatibility fallout from the Go upgrade than a parser-behavior change from metadata alone. That makes parser a touched module, but not clearly the behavioral center in the high-signal sense.","target_modules":["parser"]}