{"confidence":"medium","decision":"skip","pr":60273,"reason":"The metadata points to a narrow `SHOW PLAN FOR <SQL>` / `stmt_stats` plan-digest lookup change, not a core planner/optimizer, executor, expression, session, statistics, kv/store, parser, table, or privilege behavior change. Only one target-path file is in scope (`pkg/planner/optimize.go`), while most touched files are bindinfo/tests, so the planner touch looks incidental to statement-introspection plumbing rather than a central planner semantic change.","target_modules":["planner"]}