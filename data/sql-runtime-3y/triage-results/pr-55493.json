{"confidence":"high","decision":"deep_review","pr":55493,"reason":"The PR is clearly centered on core expression/planner behavior: every listed code path is under `pkg/expression` or `pkg/planner/core`, and the subject changes `expression.BuildSimpleExpr` to support `ParamMarker`, which can affect expression-building and planning semantics (including rewrites and point-get planning). The non-code touch (`BUILD.bazel`) is incidental; this is not mainly docs/tests/infra.","target_modules":["expression","planner"]}