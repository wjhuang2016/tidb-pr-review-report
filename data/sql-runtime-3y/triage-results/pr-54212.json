{"confidence":"high","decision":"deep_review","pr":54212,"reason":"The PR is entirely within `pkg/planner/core/logical_plan_builder.go` and explicitly targets `buildDelete` in the planner. Even though the subject says dead code removal, the change is clearly centered on planner logic rather than incidental touches, so it falls within the requested high-signal planner surface.","target_modules":["planner"]}