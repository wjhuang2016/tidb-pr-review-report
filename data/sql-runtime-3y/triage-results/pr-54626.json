{"confidence":"high","decision":"deep_review","pr":54626,"reason":"The PR is primarily in core executor paths (`pkg/executor/**`) and explicitly refactors `BaseExecutor` while removing session context from `IndexLookUpExecutor` and `IndexReaderExecutor`. That is directly tied to executor runtime correctness and plausibly affects session/sessionctx-dependent behavior, so it is clearly within the target review scope rather than incidental.","target_modules":["executor","sessionctx"]}