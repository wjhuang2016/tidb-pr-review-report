{"confidence":"high","decision":"deep_review","pr":61807,"reason":"The only production code change is in `pkg/planner/core/rule_partition_processor.go`, and the subject explicitly targets partition-pruning behavior for `NULL` handling in `IN` predicates on range partitions. That is core planner/optimizer semantic correctness, not an incidental touch; the remaining files are planner integration test updates that reinforce this scope.","target_modules":["planner"]}