{"confidence":"medium","decision":"deep_review","pr":48471,"reason":"The PR is centered on propagating `connection_id`/`connection_alias` through the TiFlash request path, and the changed files are predominantly in executor, kv, planner, and store code that build and dispatch coprocessor/MPP requests. Even if the intent is metadata propagation rather than SQL semantics, it is still plausibly core runtime behavior in the distributed execution stack rather than incidental test/infra churn, so this merits deep review.","target_modules":["executor","kv","planner","store"]}