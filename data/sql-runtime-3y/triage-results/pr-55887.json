{"confidence":"high","decision":"deep_review","pr":55887,"reason":"The PR is centered on `pkg/executor/join` with all target code paths in hash join v2 internals. Hash join logic is core executor runtime behavior, so even a 'code refine' here can affect correctness and execution semantics. The non-code touches are only supporting test/BUILD changes, not evidence that executor is incidental.","target_modules":["executor"]}