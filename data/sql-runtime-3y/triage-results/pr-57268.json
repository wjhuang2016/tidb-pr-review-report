{"confidence":"high","decision":"deep_review","pr":57268,"reason":"The PR is entirely concentrated in `pkg/parser` and adds grammar support for `admin alter ddl jobs`, which is a parser SQL-semantics change rather than an incidental touch. The changed files are parser grammar/AST/test files only, so it is clearly centered on the target `parser` concern.","target_modules":["parser"]}