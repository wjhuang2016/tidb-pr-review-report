{"confidence":"high","decision":"deep_review","pr":62533,"reason":"Almost all touched code is in `pkg/planner/core` with a couple of `pkg/executor` integration points, and the PR is explicitly about moving physical projection logic. That is core optimizer/physical-plan machinery, not incidental infra or test-only churn. The file list also spans cost, explain, plan serialization, task building, and executor builder paths, so the blast radius is clearly in planner semantics with some executor coupling.","target_modules":["planner","executor"]}