{"confidence":"high","decision":"deep_review","pr":51176,"reason":"The PR is centered on a real code change in `pkg/session/bootstrap.go`, and the subject is removing a `mysql` system table during bootstrap/upgrade. That is direct `session` bootstrap behavior with possible system-table lifecycle impact; the `executor` files look like supporting tests rather than the main change.","target_modules":["session","table"]}