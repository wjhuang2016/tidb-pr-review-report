{"confidence":"medium","decision":"deep_review","pr":48959,"reason":"The PR is explicitly planner-scoped and centers on the SQL binding subsystem, which feeds optimizer behavior. Even though most edits are in `pkg/bindinfo`, it directly touches `pkg/planner/optimize.go` and `pkg/executor/bind.go`, so the planner/executor linkage does not look incidental. Metadata says refactor, but binding logic is close enough to optimizer semantics that conservative triage should review it deeply.","target_modules":["planner","executor"]}