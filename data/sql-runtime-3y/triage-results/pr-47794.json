{"confidence":"high","decision":"deep_review","pr":47794,"reason":"The PR is clearly centered on statement-context and date-handling semantics, not incidental touches. The subject explicitly changes `stmtctx` behavior, and the high-signal code paths are concentrated in `pkg/sessionctx/stmtctx`, multiple `pkg/expression/*` builtin implementations, `pkg/executor/*`, and `pkg/statistics/*`. Replacing ZeroDate/InvalidDate handling flags can affect runtime SQL correctness and expression evaluation semantics, so this is plausibly core to the requested modules.","target_modules":["sessionctx","expression","executor","statistics"]}