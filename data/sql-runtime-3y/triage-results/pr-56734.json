{"confidence":"high","decision":"skip","pr":56734,"reason":"The PR is centered on `ddl_notifier` and test/bootstrap support, not on planner/executor/expression/session core behavior. The only focused-module code touch is `pkg/session/session.go`, while the executor/session changes are otherwise tests (`pkg/executor/*_test.go`, `pkg/session/bootstrap_test.go`). From the subject and file list, the session touch looks incidental to DDL notifier system-table setup / string escaping rather than a clear change to session/runtime semantics.","target_modules":["session"]}