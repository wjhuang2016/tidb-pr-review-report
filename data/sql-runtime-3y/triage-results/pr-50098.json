{"confidence":"high","decision":"deep_review","pr":50098,"reason":"The PR is explicitly planner-focused and changes core planner/executor code paths (`pkg/planner/core/*`, `pkg/executor/*`) rather than only tests or peripheral infrastructure. SQL binding affects optimizer plan selection and execution path construction, which is plausibly high-blast-radius planner/executor behavior. The bindinfo changes appear to support that same planner feature rather than being incidental.","target_modules":["planner","executor"]}