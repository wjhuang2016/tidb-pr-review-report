{"confidence":"high","decision":"deep_review","pr":56545,"reason":"The PR is centered on `pkg/session/bootstrap.go` and its subject explicitly describes session bootstrap/upgrade behavior (`old cluster infoschema v2 should not be used`). That is a core `session` correctness path rather than an incidental touch; the BR test looks like coverage for the same behavior.","target_modules":["session"]}