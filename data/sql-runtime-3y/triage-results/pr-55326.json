{"confidence":"medium","decision":"deep_review","pr":55326,"reason":"The title is test-scoped, but the PR also touches `pkg/session/session.go`, and the affected test name (`TestCursorWillBlockMinStartTS`) points at cursor/transaction blocking behavior in core executor-session runtime paths. From metadata alone this is plausibly session/executor correctness, not just test/infra churn, so conservative triage favors review.","target_modules":["session","executor"]}