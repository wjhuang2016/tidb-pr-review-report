{"confidence":"medium","decision":"skip","pr":66656,"reason":"Narrow fix for a single hidden column’s type metadata (`_tidb_commit_ts` unsigned flag) in `meta`/planner schema construction. It touches type semantics, but the metadata suggests a localized annotation change rather than broad optimizer, executor, session, privilege, parser, or KV behavior, so it is unlikely to hide Blocker/High issues worth a dedicated deep review.","target_modules":["planner"]}