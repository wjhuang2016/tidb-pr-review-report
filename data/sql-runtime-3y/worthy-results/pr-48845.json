{"confidence":"high","decision":"deep_review","pr":48845,"reason":"The PR touches shared range-derivation logic in `pkg/util/ranger` and changes whether a residual `Selection` is added for `LIKE` on PAD SPACE columns. That is planner query-correctness territory, with plausible wrong-result risk across indexed/range-scan plans rather than a purely local cleanup. Small diff, but the affected area is central enough to merit a focused deep review for semantic edge cases.","target_modules":["planner","pkg/util/ranger"]}