{"confidence":"high","decision":"skip","pr":56768,"reason":"This is a narrow expression-edge-case fix: one code path in `pkg/expression/builtin_compare.go` plus an integration test, focused on `NULL` equality behavior for duration vs constant `NULL`. It touches expression semantics, but the metadata suggests a localized bug fix rather than a broad change to comparison rules, type inference, planner behavior, executor correctness, or other high-blast-radius runtime paths. That makes it unlikely to hide Blocker/High issues worth a dedicated 20-40 minute deep review.","target_modules":["pkg/expression"]}