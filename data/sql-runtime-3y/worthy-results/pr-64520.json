{"confidence":"high","decision":"skip","pr":64520,"reason":"Single-file planner fix in `planbuilder.go` for a specific hint interaction (`index_lookup_pushdown`) with narrowly scoped integration tests. This does touch planner semantics, but from the metadata it looks like a localized hint-handling bug rather than a broad optimizer/runtime/session/storage correctness change with high blast radius. Strict second-stage bar is not met.","target_modules":["planner"]}