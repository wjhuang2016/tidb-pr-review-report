{"confidence":"high","decision":"deep_review","pr":63470,"reason":"This looks like a global planner-behavior default flip, not a narrow local fix. Even though the only code path listed is `pkg/sessionctx/vardef/tidb_vars.go`, the changed outputs span many planner/executor integration surfaces (`merge_join`, `point_get`, predicate pushdown, constant propagation, partition pruning, `EXPLAIN`, prepared statements). A default-on change for join-key retention can alter plan construction broadly, with real risk in optimizer semantics, execution behavior, and compatibility/performance regressions.","target_modules":["planner","executor","sessionctx"]}