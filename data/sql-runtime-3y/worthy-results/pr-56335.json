{"confidence":"high","decision":"skip","pr":56335,"reason":"The change is confined to auto-analyze scheduling internals (`heap` and `priorityqueue`) rather than direct statistics estimation logic, cost model/cardinality semantics, planner behavior, session/transaction correctness, or KV consistency. It could affect which tables get analyzed first, but from the metadata this looks like a narrow background-queue implementation change, not a central high-blast-radius semantic change that justifies a dedicated Blocker/High deep review.","target_modules":["statistics"]}