{"confidence":"high","decision":"deep_review","pr":49625,"reason":"Touches a central planner rewrite in `rule_predicate_push_down.go`: converting OUTER JOIN to INNER JOIN under nested `AND`/`OR` conditions. That is optimizer-semantics work with direct wrong-result risk if boolean null-reject analysis is even slightly off, and the affected rule can impact many query shapes beyond the reported case.","target_modules":["planner"]}