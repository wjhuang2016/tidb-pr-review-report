{"confidence":"high","decision":"skip","pr":56943,"reason":"Narrow fix in `pkg/planner/indexadvisor` for `schema_name` null-to-empty-string handling in the index advisor. This looks like planner-adjacent advisory/output normalization, not a change to core optimizer rules, executor correctness, session/transaction behavior, parser semantics, privilege enforcement, or KV/storage consistency. The extra files are test/Bazel support only, so the overall blast radius appears low.","target_modules":["pkg/planner/indexadvisor"]}