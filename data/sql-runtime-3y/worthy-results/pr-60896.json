{"confidence":"high","decision":"deep_review","pr":60896,"reason":"Touches core `INSERT`/`REPLACE` duplicate-key checking across `pkg/executor/*` and `pkg/table/tables/index.go`. Even though framed as a refactor, `FetchDuplicatedHandle()` sits on a correctness-critical path for unique-index conflict detection and handle resolution, which can hide high-severity regressions in write semantics, duplicate-key enforcement, and executor/table interaction. The added RealTiKV coverage also hints at storage-path sensitivity rather than a purely local cleanup.","target_modules":["executor","table"]}