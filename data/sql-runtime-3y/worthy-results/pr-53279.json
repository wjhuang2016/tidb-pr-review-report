{"confidence":"high","decision":"skip","pr":53279,"reason":"This looks like a narrow planner validation relocation: moving existing noop-function checks from logical plan building into preprocess for SELECT statements. Although it touches planner-core files, the scope is only two planner paths plus a targeted noop-functions integration test, which points to limited blast radius and mostly changes where/when validation happens rather than broad optimizer, executor, session, parser, or KV semantics.","target_modules":["planner"]}