{"confidence":"high","decision":"skip","pr":51219,"reason":"Single-file planner change scoped to `sys` schema reads when `tidb_isolation_read_engines=tiflash`. It does touch planner engine-selection semantics, but the scope is narrow, condition-specific, and covered by targeted integration tests, so it is unlikely to hide Blocker/High-severity issues compared with broader optimizer/session/runtime changes.","target_modules":["pkg/planner/core"]}