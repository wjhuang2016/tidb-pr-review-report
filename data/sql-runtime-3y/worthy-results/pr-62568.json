{"confidence":"high","decision":"deep_review","pr":62568,"reason":"This looks like a SQL correctness fix in a central type-resolution path: `CASE WHEN` semantics with prepared `NULL` values, implemented in `pkg/types/field_type.go` and covered by planner integration tests. Even with only one code file changed, `field_type.go` is shared infrastructure for expression/type inference, so a small patch can silently affect broad query semantics beyond the reported case. That is central and risky enough for a focused Blocker/High review.","target_modules":["expression","planner","types"]}