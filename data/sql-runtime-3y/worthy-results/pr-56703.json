{"confidence":"high","decision":"skip","pr":56703,"reason":"Narrow parser-only SQL syntax change for one specific DDL form (`ALTER TABLE ... DROP FOREIGN KEY IF EXISTS`), with matching parser/integration tests. It is user-visible parser semantics, but the blast radius appears limited to a single statement variant rather than broad planner/executor/session/storage behavior, so it is unlikely to hide Blocker/High issues worth a 20–40 minute deep review.","target_modules":["parser"]}