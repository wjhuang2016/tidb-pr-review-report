{"confidence":"high","decision":"deep_review","pr":49898,"reason":"Touches parser SQL semantics in `parser.y`/AST for set operations, specifically fixing lost `LIMIT` on `SetOprStmt`. Even though the diff is small, grammar/AST changes around `UNION`-style statements can have broad blast radius through parse tree shape, precedence, and `ORDER BY`/`LIMIT` attachment, so it is central and risky enough for a focused deep review.","target_modules":["pkg/parser"]}