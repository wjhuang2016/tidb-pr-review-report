{"confidence":"high","decision":"skip","pr":48477,"reason":"This looks like a narrow session-variable policy change: disallowing `tidb_read_staleness` in the `SET_VAR` hint path via a single `sessionctx/variable` file, with matching integration tests. Even though stale-read behavior is correctness-sensitive, the metadata suggests a scoped guardrail tweak rather than a broad change to planner/executor semantics, transaction behavior, or KV consistency, so it is below the bar for a 20-40 minute deep Blocker/High hunt.","target_modules":["sessionctx"]}