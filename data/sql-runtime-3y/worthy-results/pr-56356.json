{"confidence":"medium","decision":"skip","pr":56356,"reason":"Although it touches `executor` and `planner/core`, the metadata points to a localized wrong-result fix for the `slow_query`/cluster slow query virtual tables. This looks like narrow memtable predicate handling for diagnostic system tables, not broad planner/optimizer, session, KV, privilege, or parser semantics, so it is below the bar for a dedicated Blocker/High deep review.","target_modules":["pkg/executor/slow_query.go","pkg/planner/core/memtable_predicate_extractor.go"]}